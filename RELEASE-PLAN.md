# RELEASE-PLAN — две недели до релиза

Ревизия от **2026-09-01**. Три направления, которые Стас поставил: добить кит, разметить страницы
под дизайн, вычистить код. Дальше по этому файлу идут этапы (скилл `stage`), новейшее — в `WORKLOG.md`.

Даты: старт **01.09**, продукт готов **14.09**, релиз-кандидат с админкой **21.09** (см. §D «Про срок»).

---

## 0. Где мы сейчас (факты, не ощущения)

| Что | Цифра |
|---|---|
| Публичных страниц (продукт) | **24** |
| Служебных страниц (`/admin`, `/studio`, `/underbeer`) | **38** |
| API-роутов | 36 |
| Файлов `.tsx` в `app/` | 141 |
| Артбордов в Ките | **14** (после Э1) |
| Экранов, задизайненных целиком | **3** (Вход, Сайдбар, Профиль игрока) |

**Главное расхождение.** Кит — светлая система **Light Clay** (бумага `#E7E3D8`, мятный акцент,
пластилиновые тени, Nunito). Прод — **тёмная тема** на Tailwind с фиолетовым акцентом. То есть
дизайн-кит сейчас существует **отдельно от кода** и ни одной строкой в него не входит.

Отсюда вывод, который определяет весь план: **сначала кит становится кодом** (токены + компоненты),
и только потом переезжают страницы. Иначе 24 страницы придётся перекрашивать руками — это не две
недели.

---

## A. Кит элементов — что есть и чего не хватает

### Есть (14 артбордов)

| Артборд | Содержимое |
|---|---|
| Управление и формы | кнопки solid/quiet (5 состояний, иконочные), пилюли навигации L1/L2/L3, Segmented, Tabs, поля ввода (текст, поиск, фокус, textarea), чекбоксы, Select |
| Данные и обратная связь | чипы, статусы турнира, StatTile, элементы таблицы (зоны, место, лого, аватар, очки, счёт W–L, статус-пилюли), алерты, тосты, пустое состояние, скелет |
| Оверлеи и структура | SectionHeader, крошки, диалог, Sheet, выпадающее меню, тултип, RowCard, мини-карточка игрока, карточка команды |
| Ещё элементы | аккордеон, тумблер, radio, пагинация, аватар-группа, календарь |
| Состояния | интерактивные семейства: default / hover / press / focus / on / off / current / selected |
| Hero-шапка игрока | шапка профиля |
| Профиль · атомы | hero-подложка, карьера бубликом, строка игра/герой, карточка команды с мини-карточками, мини-блоки данных, ссылки-иконки, достижения |
| Сетка плей-офф | верхняя сетка на 8 команд |
| Турнирная таблица | шапка колонок с сортировкой, строка команды, зоны выхода, состояния строки, вид 390px |
| Карточка встречи | предстоящая / live / сыграна / предложено время, строка расписания, вид 390px |
| Групповой этап | блок группы с зоной выхода, кросс-таблица, состояния ячейки |
| Hero-шапка команды | лого, тег, чипы, плитки чисел, состав, вид 390px |
| Шапка сайта и меню | L1 гость и вошедший, L2 контекст турнира, L3 вкладки, сборка, мобильная шапка и Sheet-меню |
| Футер | четыре колонки, соцсети, правовая строка, мобильный вариант в аккордеонах |

### Чего не хватает (заводим артбордами в Ките)

Список выведен из того, что реально рисуют страницы продукта.

**Критично (без этого не собрать ни одной ключевой страницы) — закрыто на Э1 01.09:**
- [x] **Турнирная таблица целиком** — шапка колонок, строка команды, сортировка, зоны выхода.
      Сейчас в ките только атомы строки, самой таблицы нет. Нужна для `/standings` и `/tournaments/<slug>/<div>`.
- [x] **Карточка встречи (серии)** — две команды, счёт, дата/время, статус, ссылка на отчёт.
      Самый повторяемый элемент продукта: расписание, страница дивизиона, профиль команды.
- [x] **Групповой этап** — блок группы и кросс-таблица.
- [x] **Hero-шапка команды** — парная к `PlayerHero`, для `/roster/teams/<id>`.
- [x] **Шапка сайта L1 + мобильное меню** — в ките есть пилюли, но нет собранной навигации
      и её мобильного вида (кит рисовался на 1220px, 390px не проверен).
- [x] **Футер** — сделан, включая мобильный вариант.

**Важно (иначе часть экранов соберётся «на месте», а это расхождение стиля):**
- [ ] **Скорборд матча / постгейм-отчёт** — таблица KDA, герои, предметы, показатели.
      В ките по-прежнему не представлен. На Э6 экран собран **по месту** из атомов Кита
      (подушка карточки, `TeamMark`, `Chip`, `Meter`, вдавленные лунки) — см. шапку
      `app/(admin)/match/_components/report-scoreboard.tsx` и WORKLOG Э6. Артборд остаётся долгом:
      появится он — заменять надо будет один файл.
- [x] **Степпер формы** — вынесен атомом на Э8: `components/pouf/stepper.tsx`, рисунок из макета
      «Вход» (артборды «Шаг 1…3»). Первым его надел квиз анкеты в `/me`; мастера служебной части
      (заведение турнира, импорт таблицы) берут его же на Э9. Артборд доски заявки в Ките
      по-прежнему не заведён — на Э7 экран заявки собран по месту, шагов у него нет.
- [ ] **Загрузка файла** — лого команды, фото игрока. Нет.
- [ ] **Медаль ранга Dota + бейдж дивизиона** — повторяются в ростере и профилях.
- [ ] **Графики статистики** — бублик есть, столбчатого/линейного нет (`/tournaments/<slug>/<div>/stats`).
- [ ] **Расписание игрового дня / таймлайн**.

**Служебная часть (нужно, раз админку тоже дизайним):**
- [ ] **Таблица данных с действиями** — строка записи, чекбокс выделения, действия справа,
      пустое состояние. Основа половины экранов админки.
- [ ] **Панель инструментов таблицы** — поиск + фильтры + кнопка «создать» одной полосой.
- [ ] **Карточка очереди** — заявка/предложение времени с «принять» и «отклонить с причиной».
      Модерация, регистрации, встречи от капитанов — везде один рисунок.
- [ ] **Мастер в шаги** — заведение турнира и импорт таблицы идут шагами; степпер общий с заявкой.
- [ ] **Двухпанельный редактор** — холст + инспектор справа, для студии.
- [ ] **Зона drag-n-drop** — пул слева, слоты справа: жеребьёвка, заявка, драфты.

**Графику трансляции (HUD, оверлеи, плашки) не рисуем** — этим занимается другой человек.
Из плана и из `BACKLOG.md` §3 убрана.

### Правило работы с китом

По `.claude/skills/kit`: **атом → артборд в Ките, экран целиком → отдельный артефакт.**
Кит правится через извлечение `*.dc.html` + `canvas.json` и republish по тому же URL.

---

## B. Карта страниц — что дизайнить

### P0 · Продукт, лицо релиза (10 экранов)

| Экран | Маршрут | Статус дизайна |
|---|---|---|
| Главная | `/` | **готов** (Э7) |
| Турнир · обзор | `/tournaments/<slug>` → `/about` | **готов** (Э5) |
| Дивизион · таблица | `/tournaments/<slug>/<div>` | **готов** (Э5) |
| Плей-офф | `/tournaments/<slug>/<div>/playoff` | **готов** (Э5) |
| Встреча (серия) | `/series/<slug>` | **готов** (Э6) |
| Отчёт матча | `/match/<id>` | **готов** (Э6) |
| Команда | `/roster/teams/<id>` | **готов** (Э6) |
| Профиль игрока | `/roster/players/<id>` | макет готов, **код не переехал** → Э3b |
| Вход / регистрация | `/login/tg` | макет готов, код проверить → Э3b |
| Заявка на турнир | `/tournaments/<slug>/apply` | **готов** (Э7) |

### P1 · Продукт, второй эшелон (14 экранов)

~~Все переехали на Э8 (02.09)~~: `/tournaments` · `about` (Э5) · `/entrants` · `/tournaments/<slug>/tp` ·
`groups` (Э5) · `/stats` · `/roster` · `/roster/players` · `/tournaments/<slug>/roster/{teams,players}` ·
`/rules` · `/me` + `/me/profile` + `/me/security`. Три адреса из списка оказались **редиректами**
(`/tp`, `/roster/teams`, `/standings`) — рисовать в них нечего.

Эти экраны переезжают почти бесплатно, если P0 задал компоненты. Отдельного рисования требуют
только `/me` (личный кабинет — свой рисунок) и `/rules` (длинный текст — типографика).
Два уехали в Э5 вместе с турнирным блоком: `about` оказался тем самым «обзором турнира» из P0
(корень турнира — редирект), а `groups` вернулся из редиректа отдельной вкладкой.

### P2 · Служебная часть (38 страниц) — **редизайн, а не остаточный принцип**

Админка — рабочий инструмент лиги, в ней проводят каждый игровой день, поэтому она едет
на общих компонентах и в той же палитре. Сайдбар задизайнен, **в код не перенесён** → Э3b.

Хорошая новость: после Э2–Э3 (токены + одна библиотека) бо́льшая часть админки перекрасится
сама — она собрана из тех же кнопок, полей и таблиц. Отдельного рисования требуют пять групп:

| Группа | Страниц | Что рисуем |
|---|---|---|
| **Ядро операций** — дашборд, турниры (6), серии (2), ростер-редакторы (2), модерация, дубли, персонал | 13 | таблица с действиями, панель инструментов, карточка очереди, мастер в шаги |
| **Студия** — `/studio` + editor/render/generate/new | 6 | двухпанельный редактор: холст + инспектор |
| **Драфты и ивенты** — fearless (2), single-draft, 1x1, underbeer (2), overlay | 7 | драфт-борд (один на всех, см. C5) + зона drag-n-drop |
| **Отчёт матча** — `/match`, `/match/<id>` | 2 | скорборд; рисуется в P0, здесь только служебная обвязка |
| **Настройки** — bot, theme, quizzes (2), stats, tp, vision | 7 | формы на общих компонентах, своего рисунка не требуют |

### Тема продукта — решено 01.09

**Светлый Кит (Light Clay) — единственная тема на релиз.** Прод переезжает целиком: сайт и
админка. Тёмную припилим позже, отдельной задачей после релиза — сейчас две палитры не тянем.

Следствия: `pouf.css` перекрашивается по токенам Кита (Э2), `data-theme="dark"` из
`site-nav.tsx` и прочих мест снимается, тёмные утилиты Tailwind (`bg-*-500/10`,
`text-*-300` в драфтах и админке) вычищаются по ходу Э3.

Чтобы тёмная тема потом не стоила второго переезда: на Э2 цвета заводятся **семантическими**
токенами (`--surface`, `--ink`, `--accent`), а не именами оттенков. Тогда вторая палитра —
это один блок переопределений, а не правка компонентов.

---

## C. Код — что переработать, доработать, объединить

### C1. Три параллельные UI-библиотеки (главный долг) — ~~закрыто на Э3, 01.09~~

| Слой | Что там | Использований |
|---|---|---|
| `components/ui/` | shadcn: button, input, dialog, select, checkbox… | button ×23, input ×11 |
| `components/pouf/` | своя система: Button, Input, Segmented, surface, text… | Button ×18, text ×5 |
| `app/_components/ui.tsx` | SectionHeader, Chip, StatTile, Meter, токены ширины | — |

Две кнопки и два инпута живут бок о бок и делят экраны пополам. **Объединяем в одну систему**,
собранную по Киту: токены в CSS-переменных, компоненты — в `pouf/` (там уже правильная модель),
`components/ui/` вычищается, `_components/ui.tsx` вливается в `pouf/`.

Это не косметика: пока библиотек три, любой новый экран — лотерея, а перекраска невозможна.

### C2. Ширины литералами — ~~закрыто на Э4, 02.09~~

Из «20 файлов» колонкой страницы были пятеро: `/me`, `/me/profile`, `/me/security`, `/rules`,
`/login/tg`. Остальные литералы — мера абзаца, картинки карты и блок внутри драфт-борда; §4
стандарта их разрешает прямо, поэтому они остались как есть.

Токенов стало четыре: у карточки входа своя ширина из Кита (420px), и сводить её к `FORM_MAX_W`
(48rem) значило бы растянуть форму из двух полей на весь экран — поэтому `AUTH_MAX_W`, а не литерал.

### C3. Антипаттерны навигации (`UI-GUIDELINES.md` §9) — ~~разобрано на Э4, 02.09~~

Ревизия по коду показала, что список устарел: цепочка хабов уже разорвана (`/tournaments/<slug>`
редиректит в таблицу, `[div]/layout.tsx` рисует только цвет дивизиона), модель навигации в турнире
одна (`tournament-bar.tsx`), а плитки остались ровно там, где стандарт их разрешает — на домашней
`/admin` (§5).

Что было живым и починено на Э4:
- **два ряда дивизионов**: сегменты `D1 · D2` строки контекста показывались и на ростере, где
  рядом стоит собственный фильтр `D1 · D2 · Все`, ни на что не влияли и уводили с этапа. Теперь
  рисуются только на разделах, где дивизион есть в адресе;
- **«← Кабинет» как способ ориентации** в `/rules`, `/me/profile`, `/me/security` → крошки (§3).

Что осталось и куда отнесено:
- «← Все турниры», «← к команде» и подобные в админке — их полтора десятка, и по §5 они уходят
  вместе с переездом админки на сайдбар: **Э9**;
- мобильная шапка (L1 сжимается до лого и кнопки меню, §6) не сделана — вкладки уезжают в
  горизонтальный скролл, «Админ» на 375px виден буквой. Артборд для неё есть с Э1. Упирается в
  вопрос единого сайдбара, см. ниже;
- **вопрос Стасу: единый сайдбар.** Макет Кита «Сайдбар» рисует ОДНУ колонку, сливающую лигу,
  инструменты и кабинет, с брендом, профиль-блоком и поиском в шапке. `UI-GUIDELINES` §2 и §5
  описывают другое устройство: верхняя строка L1 на весь сайт, а сайдбар — только у операторской.
  Это не разница в объёме работы, а два разных решения; выбрать должен Стас, и от выбора зависят
  Э5–Э8 (весь хром продукта). Молча слить нельзя — стандарт пришлось бы переписывать целиком.

### C4. Монолиты (кандидаты на разбор)

| Файл | Строк |
|---|---|
| ~~`app/(admin)/match/_components/report.tsx`~~ — разобран на Э6 (5 файлов, максимум 314 строк) | ~~913~~ |
| `app/(admin)/underbeer/[id]/_components/draft-board.tsx` | 708 |
| `app/(admin)/admin/series/_components/series-admin.tsx` | 680 |
| `app/_components/postgame/blocks.tsx` — на Э6 расплетён по цветам (кожа `--pg-*`), длина та же | 620 |
| ~~`app/(public)/tournaments/[slug]/apply/apply-board.tsx`~~ — разобран на Э7 (5 файлов, максимум 332) | ~~589~~ |

`report.tsx` и `apply-board.tsx` — публичные, их всё равно трогать при дизайне; разбор совместить
с переездом (Э6, Э7). `draft-board` и `series-admin` — то же самое на Э11 и Э9: раз админку
дизайним, разбор идёт вместе с переездом, а не отдельной уборкой.

### C5. Три драфта — проверить на объединение

`/admin/fearless-draft` (fearless), `/underbeer` (жеребьёвка составов), `/admin/single-draft`
(рандомайзер героев по атрибутам). Первые два делят `lib/draft.ts` / `lib/fearless.ts`, третий —
чистый клиент без сервера. Похоже, объединять надо не все три, а свести UI драфт-борда к одному
компоненту. **Требует отдельного разбора, вслепую не решать.**

### C6. Долги из `BACKLOG.md` §2 — что чинить к релизу

Блокирует релиз:
- [x] ~~**`/login/tg` считает вошедшим по куке**~~ — Э4: страница спрашивает `currentAccount()`,
      то есть базу, тем же вопросом, что и `/me`.
- [ ] **Восстановление пароля отсутствует** — забыл пароль и всё. Нужен хотя бы сброс владельцем
      команды или организатором из админки. **Плана нет — этап под него не заведён** (Э12 «сброс
      пароля» назван одной строкой, а это форма, право и запись в аккаунт).
- [ ] **`TG_CHAT_ID` не заведён** — одна строка в `web/.env`, и вписать её может только Стас: ключи
      в репозиторий не едут. Э4 сделал молчание видимым — `/admin/bot?tab=settings` показывает
      предупреждение, пока переменная не задана.

Можно после релиза:
- статистика игрока не разрезана по турнирам · импорт матчит по нику (дубли) · реестр баллов без UI ·
  OG-картинки · опознание в боте по хендлу · архивные команды в пуле драфта · осиротевшие файлы лого.

### C7. Инфраструктура — нерешённое

`BACKLOG.md` §1: **не выбран VPS** и **не решено, кто главный по базе** после публикации.
Без этого «уходим в релиз» не наступит. Решение нужно на первой неделе, не на последнем дне.

---

## D. План

Один этап ≈ полконтекста чата, закрывается коммитом (`ship`). Порядок неслучаен: кит → токены →
компоненты → страницы → админка. Прыгать через ступень нельзя, иначе экраны придётся переделывать.

### Неделя 1 — фундамент (01.09 → 07.09)

| Э | Что | Выход |
|---|---|---|
| ~~**Э1**~~ | ~~Добить критичные пробелы Кита: таблица, карточка встречи, группы, hero команды, шапка+мобильное меню, футер~~ **сделано 01.09** | 6 новых артбордов, в Ките стало 14 |
| ~~**Э2**~~ | ~~**Кит → токены.** Палитра, тени, радиусы, шкала отступов, Nunito → семантические CSS-переменные в `pouf.css`~~ **сделано 01.09** | слой токенов Light Clay в `pouf.css` |
| ~~**Э3**~~ | ~~**Слияние UI-библиотек** (C1). `components/ui` + `_components/ui.tsx` → `pouf/`, всё на токенах Э2. Снять тёмные пины~~ **сделано 01.09** | одна библиотека `components/pouf/`, одна кнопка/поле/заголовок; сайт и админка светлые (Light Clay). Осталось на потом: третья ступень текста Кита даёт 1.81:1 (вопрос к Киту, Э5), тёмная графика пост-гейма не расплетена (Э6), body-шрифт пока Geist |
| ~~**Э3b**~~ | ~~**Готовые макеты → код.** Сайдбар и профиль игрока сверстать по каноническим артефактам (скилл `kit`), не по текущей вёрстке; заодно сверить `/login/tg` с макетом «Вход»~~ **сделано 01.09** | сайдбар (развёрнутый + свёрнутый рельс) и профиль игрока по макету, атомы профиля в `pouf/profile.tsx`, эмодзи инструментов заменены иконками Кита, `/login/tg` переодет в подушку. Осталось: у макета «Вход» нет экрана кода из бота — его почта/пароль и квиз регистрации это `/me` (Э8) |
| ~~**Э4**~~ | ~~Механика: ширины по токенам (C2), антипаттерны навигации (C3), `/login/tg` и `TG_CHAT_ID` (C6)~~ **сделано 02.09** | четвёртый токен ширины (`AUTH_MAX_W`), литералов на уровне колонки не осталось; убран второй ряд дивизионов, «← Кабинет» в продукте заменён крошками; `/login/tg` спрашивает базу, а не куку; `/admin/bot` вслух говорит про незаданный `TG_CHAT_ID`. **Осталось Стасу:** вписать `TG_CHAT_ID` в `web/.env` (кодом не закрыть) и решить вопрос единого сайдбара — см. §C3 |

### Неделя 2 — экраны продукта (08.09 → 14.09)

| Э | Что | Выход |
|---|---|---|
| ~~**Э4b**~~ | ~~**Единый сайдбар (решение 02.09).** Полный сайдбар из макета на всех страницах; профиль и поиск по лиге в его шапке; `site-nav.tsx` и верхняя строка убраны; поиск по лиге реализован; §2/§5/§6 `UI-GUIDELINES` переписаны; мобильный вид — рельс и Sheet~~ **сделано 02.09** | один хром на весь продукт: `app-shell.tsx` + `app-sidebar.tsx` в обеих группах маршрутов, `site-nav.tsx` и `admin-sidebar.tsx` удалены; поиск по лиге (`lib/search.ts` + `/api/search`) заведён с нуля; §0/§2/§4/§5/§6/§7/§9 стандарта переписаны. **Осталось Стасу:** решить, нужен ли в сайдбаре быстрый вход в текущий сезон («Таблица», «TP») — сознательно не заведён, см. WORKLOG |
| ~~**Э5**~~ | ~~P0, турнирный блок: обзор турнира, дивизион-таблица, плей-офф, группы~~ **сделано 02.09** | 4 экрана по Киту: обзор турнира — вкладка `/about` (корень турнира остаётся редиректом, см. §C3), таблица дивизиона с сортировкой по колонкам, зонами, разницей карт и формой, «Группы» вернулись отдельной вкладкой (блок группы + кросс-таблица), сетка плей-офф на карточках Кита. Атомы таблиц вынесены в `pouf/table.tsx`. **Третья ступень текста решена по месту:** на этих экранах `--subtle` (1.81:1) не осталось вовсе, всё читаемое — на `--muted`; Кит не правился |
| ~~**Э6**~~ | ~~P0, командно-игроцкий блок: команда, встреча, отчёт матча (+ разбор `report.tsx`)~~ **сделано 02.09** | 3 экрана по Киту: команда — hero-шапка с плитками и составом строками (тёмная обложка `TeamCover` снята), встреча — «Карточка встречи» Кита в крупном варианте плюс карты, отчёт матча — Light Clay целиком. `report.tsx` (913 строк) разобран на пять файлов, самый крупный — 314. Атомы вынесены в `pouf/series-card.tsx` и `pouf/hero.tsx`. **Пост-гейм расплетён:** экран светлый, выгрузка PNG тёмная на своей коже `--pg-*`. **Скорборд собран по месту** — артборда в Ките нет (см. §A) |
| ~~**Э7**~~ | ~~P0: главная + заявка на турнир (+ разбор `apply-board.tsx`)~~ **сделано 02.09** | 2 экрана по Киту: главная — hero-подушка с сезоном и плитками, дивизионы в таблицы, афиша ближайших встреч и последние результаты карточками, разделы ниже данных; заявка — пул и состав подушками, лунки слотов, кружок капитана из Кита, все сообщения алертами. `apply-board.tsx` (589 строк) разобран на пять файлов (`board-pool`, `board-slots`, `board-ready`, `board-player` + оболочка). Новые атомы: `pouf/feedback.tsx` (`Alert`, `EmptyState`) и общий адаптер встречи `app/_components/series-brief.tsx` — страница команды переехала на него же |
| ~~**Э8**~~ | ~~P1 пакетом — переезжают на готовых компонентах; отдельно `/me` и `/rules`~~ **сделано 02.09** | P1 закрыт: `/tournaments`, `entrants`, `tp`, `stats`, ростер (5 экранов), `/rules`, `/me` + `profile` + `security` (`/tp`, `/roster/teams`, `/standings` — редиректы, рисовать нечего). Новые атомы: `pouf/tabs.tsx` (пилюля навигации — было восемь копий одной строки классов, из них половина с ВДАВЛЕННОЙ активной), `pouf/stepper.tsx` (закрыл долг §A «степпер формы»), `pouf/auth.tsx` (оболочка окна входа, общая с `/login/tg`), `StatusPill` в `feedback.tsx`, компактный размер поля в `Input.tsx`. `/me` собран по канону «Вход»: сегмент вход/регистрация, соц-входы иконками, **анкета квизом в три шага**. **Осталось Стасу:** окно входа для невошедшего и `/me/profile` глазами не смотрели — под учёткой разработчика они недоступны, см. WORKLOG |

### Неделя 3 — админка и релиз (15.09 → 21.09)

| Э | Что | Выход |
|---|---|---|
| **Э9** | Админские атомы Кита (таблица с действиями, панель инструментов, карточка очереди, мастер, drag-n-drop) + ядро операций: дашборд, турниры, серии, ростер-редакторы, модерация, дубли, персонал | 4 артборда + 13 экранов |
| **Э10** | Студия: двухпанельный редактор (холст + инспектор), 6 экранов | студия в общем стиле |
| **Э11** | Драфты: разбор C5 и один драфт-борд на fearless / underbeer / 1x1 + оверлей | 7 экранов, минус дублирующий UI |
| **Э12** | Релиз: VPS и решение по базе (C7), сброс пароля, прогон на 390px и 1440px, чек-лист `UI-GUIDELINES.md` §8 | релиз-кандидат |

### Про срок

Двенадцать этапов в две недели не влезают — это надо сказать прямо, а не выяснить 15-го.
Раскладка честная: **продукт готов к 14.09** (Э1–Э8), **админка и релиз-кандидат — к 21.09**
(Э9–Э12). Если 15.09 — жёсткая дата, режется Э8 (P1-экраны переживут релиз в текущем виде,
они уже будут перекрашены токенами с Э2) и Э11 (драфты работают, страдает только единообразие).

### Что сознательно НЕ делаем

- **Графику трансляции (HUD, оверлеи, плашки)** — ей занимается другой человек, из плана убрана.
- **Тёмную тему** — после релиза, отдельной задачей; токены на Э2 заводятся так, чтобы она
  стоила одного блока переопределений.
- **Генеративный пайплайн** из `BACKLOG.md` §3 — после релиза.
- **Настроечные экраны админки** (bot, theme, quizzes, stats, tp, vision) отдельно не рисуем —
  переезжают на общих компонентах с Э3.

### Риски

1. **Э3 (слияние библиотек) — самый опасный этап:** трогает все 141 `.tsx`. Делать одним заходом,
   не растягивать; после него обязательный прогон `tsc` + `lint` + глазами по P0-экранам.
2. **Три недели вместо двух.** Если срок не двигается — режем Э8 и Э11 (см. «Про срок»).
3. **C5 (объединение драфтов) вслепую не решается.** Если на Э11 выяснится, что общего движка
   не выходит, — оставляем три борда на общих компонентах и идём дальше.
