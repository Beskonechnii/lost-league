# ТЗ: <название>

**Статус:** draft | ready | in progress | done
**Входит в MVP:** да / нет (решает `pm`, сверяясь с `MVP.md`)

## Цель

Одним абзацем: какую задачу человека это закрывает и по чему поймём, что закрыли.
Не «сделать страницу», а «капитан может вернуть себе доступ, не написав организатору».

## Контекст

Ссылки на код и доки точными путями — чтобы исполнитель не искал:
`web/src/lib/...`, `docs/ARCHITECTURE.md` §…, `DECISIONS.md` от <дата>.
Что уже есть и работает, чего нет. Известные грабли.

## Scope

**Входит:**
- …

**НЕ входит:**
- … (с причиной одной строкой — иначе вернётся спором на приёмке)

## Требования · DEV

Что меняется в коде: модели, миграции, слои `src/lib/`, маршруты, права.
Решения, которые уже приняты и не переигрываются.

## Требования · DESIGN

Заполняет роль `design`. Какие атомы Кита берём, чего в Ките нет и надо завести артбордом,
раскладка на 390px и 1440px, состояния (пусто / загрузка / ошибка / нет прав).

## Требования · SEO

Заполняет роль `seo`. Только для публичной части: `metadata`/OG, URL, индексируемость,
`noindex` где нужно, sitemap. Для `/admin`, `/me`, чата — строка «не индексируется».

## Acceptance criteria

Проверяемые пункты — по ним роль `qa` даёт pass/fail. Формулировка «действие → наблюдаемый
результат», без «работает корректно».

- [ ] …
- [ ] `npx tsc --noEmit` и `npm run lint` чисто, `npm run build` проходит
- [ ] 390px и 1440px без горизонтального переполнения
- [ ] права: чужое недоступно, admin без права получает отказ

## Открытые вопросы

Что не решено и кто решает. Пустой раздел — хороший знак.

---

**Отчёт о выполнении сюда НЕ пишется** — он идёт записью в `WORKLOG.md`.
Здесь меняется только строка «Статус».
