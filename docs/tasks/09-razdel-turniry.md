# ТЗ 09: раздел «Турниры»

**Статус:** готов к работе — серия и лимит решены Стасом 13.09.2026 (см. «Решения по развилкам»);
состав хронологии идёт по предложению `pm`. DESIGN и SEO заполняются до старта
**Входит в MVP:** нет — сезон открывается с нынешним списком турниров. Берётся третьим в
продуктовом блоке (`MVP.md` §0), после 08: раздел живёт под баром, и перекладывать его до смены
хрома значит перекладывать дважды.

## Цель

Турниров у лиги больше одного, и они разной природы: серия LOST и наши же турниры другой серии.
Сейчас у них один плоский список (`/tournaments`: «текущий» + «остальные»), а
`/tournaments/archive` редиректит на тот же список. Дивизион при этом виден чипом внутри карточки турнира, хотя играют и смотрят именно дивизион.
Задача закрыта, когда: у лиги есть **раздел** «Турниры» с разделением на серии, **дивизион имеет
свою карточку**, у турнира есть **ограничение по числу команд** и оно видно, а участники и составы
заводятся **из редактирования турнира**, а не из отдельного угла админки.

## Контекст

**Решения Стаса, принятые 13.09.2026 и не переигрываемые:**
- «Можно делать общую вкладку "Турниры", но внутри хотелось бы разделить турниры серии LOST
  и остальные (тоже наши, просто другая серия турниров)».
- «Каждый див своей карточкой. **Модель не надо переделывать**».
- «Нужно добавить ограничение по кол-ву команд, которые могут принять участие. Чтобы турнир имел
  ограничение и отображение по кол-ву команд».
- «Импорты вписать в редактирование турнира. Всю логику можно завернуть там, где будут отображаться
  турниры (кнопка редактировать)».
- «Добавить просто турниры. Внутри хронология турниров».
- **Серия — свободное текстовое поле у турнира**, не список в коде: раздел группирует турниры
  по значению поля, серия LOST идёт первой группой.
- **Лимит команд живёт у дивизиона**, турнир показывает сумму по дивизионам: запись команды идёт
  в дивизион, на уровне турнира проверять нечем.

**Дивизион остаётся разрезом внутри турнира — модель не трогаем.** Участие команды — это
`TournamentEntry(divisionId, teamId)`, у дивизиона висят `RosterSpot`, `Series`, `GroupEntry`,
`TeamApplication`. Имя дивизиона зеркалится строками в `Team.group`, `Series.division`,
`GroupEntry.division` — это принятое отступление (`BACKLOG.md`, «Принятые отступления»), по этим
строкам фильтруют полтора десятка выборок, единственный писатель зеркал — `src/lib/tournaments.ts`.
**«Дивизион своей карточкой» — это про представление, а не про модель.** Дивизион не становится
турниром, своего `slug` верхнего уровня не получает, адреса `/tournaments/<slug>/<div>/…` остаются.

**Код сейчас:**

| Что | Файл |
|---|---|
| Публичный список | `web/src/app/(public)/tournaments/page.tsx` — текущий + остальные, черновики скрыты |
| Архив | `web/src/app/(public)/tournaments/archive/page.tsx` — `redirect("/tournaments")` |
| Страница турнира и её оболочка | `web/src/app/(public)/tournaments/[slug]/page.tsx`, `layout.tsx` |
| Строка контекста L2/L3 | `web/src/app/_components/tournament-bar.tsx` |
| Заявленные команды | `web/src/app/(public)/tournaments/[slug]/entrants/page.tsx` — источник `listTeamRosters` по дивизионам |
| Служебный список | `web/src/app/(admin)/admin/tournaments/page.tsx` |
| Редактирование турнира | `web/src/app/(admin)/admin/tournaments/[slug]/page.tsx` — 321 строка: статус, описание, дивизионы, участники, удаление |
| Мастер заведения | `web/src/app/(admin)/admin/tournaments/new/[step]/page.tsx` |
| Слой данных | `web/src/lib/tournaments.ts` — `listTournaments`, `currentTournament`, `divisionTeams`, `setTeamDivision`, `drawGroups`, `TOURNAMENT_STATUS_LABELS` |
| Модель | `web/prisma/schema.prisma` — `Tournament` (17), `Division` (52), `TournamentEntry` (83), `TeamApplication` (101) |

Точка входа в импорт из турнира **уже есть**: `[slug]/page.tsx:75` — `PillLink` на
`/admin/roster/import?tournament=<slug>`. Статусы турнира: `draft` (не виден публично) ·
`registration` · `running` · `finished`; словарь подписей — `TOURNAMENT_STATUS_LABELS`, макеты рисуют свои слова, словарь главнее.

**Точка входа в раздел — пункт бара «Турниры», он заводится в ТЗ 08.** Решение Стаса 13.09.2026
(`DECISIONS.md`): пункт бара «LOST cup» переименован в «Турниры» и ведёт на `/tournaments`.
Текущий турнир из хрома напрямую больше не открывается — в турнир заходят с главной или из этого
раздела. Для 09 это значит две вещи: раздел обязан быть самодостаточным входом (идущий турнир
находится на нём с первого экрана, без поиска по хронологии) и **пункт бара 09 не трогает** —
его делает 08, которое идёт раньше.

**Граница с ТЗ 11 (импорт одним экраном).** Правило: **`/admin/tournaments/*` — это 09,
`/admin/roster/import*` — это 11.**
- 09 отвечает за то, **откуда** оператор попадает в импорт и **куда** возвращается: кнопки в
  редактировании турнира, контекст турнира в адресе, возврат на турнир после импорта, вид списка
  участников после того, как импорт отработал.
- 11 отвечает за **сам экран импорта**: два механизма (игроки · составы), превью, проверка,
  запись и шаг «распределить в турнир» после импорта составов.
- Спорная зона — **шаг распределения по дивизионам после импорта**. Он принадлежит **11**: это
  последний шаг того же экрана, разрывать его переходом нельзя. 09 обеспечивает, что распределённые
  команды видны в участниках турнира без ручной пересборки.

## Scope

**Входит:**

*Публичная часть*
- **Раздел «Турниры» с группировкой по серии**: группы идут в одном разделе, а не разными
  разделами и адресами. Серия LOST — первая группа, дальше остальные серии, последней — группа
  турниров без серии.
- **Серия — свободное текстовое поле у турнира.** Оператор вводит название руками в редактировании
  турнира; словаря серий, кода серии и справочника не заводится. Новая серия появляется вводом
  нового названия — без правки кода и без миграции. Пустое значение = турнир без серии,
  он не пропадает, а идёт в последнюю группу «Прочие турниры».
- **Дивизион — своя карточка** в карточке-турнире: у каждого дивизиона видны имя/подпись, число
  команд (и лимит, см. ниже), ссылки в таблицу дивизиона. Чипы-ссылки из нынешнего
  `TournamentCard` заменяются карточками.
- **Хронология турниров**: все турниры лиги в одной ленте по времени, черновики скрыты.
  Закрывает `/tournaments/archive`, который сегодня редиректит на список.
- **Лимит команд виден публично**: на карточке дивизиона — его собственный счётчик «8 из 12»,
  на карточке турнира — сумма по дивизионам. Заполненный лимит на этапе приёма заявок читается
  как «мест нет».

*Служебная часть*
- **Лимит команд заводится оператором у каждого дивизиона** в редактировании турнира
  и показывается рядом с фактом. Лимит необязателен: не задан — дивизион ведёт себя как сегодня,
  «без ограничения», а не «ноль мест».
- **Заполненный дивизион не выбирается в публичной заявке**: в выборе дивизиона он помечен
  «мест нет» и недоступен. Заполнены все дивизионы турнира — кнопка «Подать заявку» не отправляет
  форму и объясняет, почему.
- **Оператор может превысить лимит осознанно**: запись сверх лимита не запрещена жёстко, но
  показывает предупреждение с числами дивизиона («12 из 12, команда станет 13-й»). Касается всех
  трёх путей записи: апрув заявки, ручное добавление команды, перестановка команды между
  дивизионами. Причина: лимит — правило лиги, а не ограничение базы, последнее слово
  за организатором.
- **Импорт вызывается из редактирования турнира** — оба механизма (игроки и составы), с контекстом
  турнира и возвратом на турнир после записи. Сегодня из турнира ведёт только «Импорт составов».

**НЕ входит:**
- **Переделка модели дивизиона** — прямой запрет Стаса «модель не надо переделывать»; дивизион
  остаётся разрезом турнира, зеркала имени не трогаются (`BACKLOG.md`, «Принятые отступления»).
- **Сам экран импорта, его механизмы и распределение по дивизионам** — ТЗ 11, граница выше.
- **Новый разрез «серии» в других разделах** (ростер, TP, статистика) — серия появляется только
  в разделе турниров; расползание разреза по продукту это отдельная задача.
- **Формальная сущность «сезон»** — её в модели нет намеренно (`BACKLOG.md`, icebox), серия
  ею не становится.
- **Изменение L2/L3 внутри турнира** (`tournament-bar.tsx`, вкладки этапов) — задача про раздел,
  а не про страницы внутри турнира.
- **Архив встреч** — это ТЗ 10; здесь список турниров, а не список матчей.
- **Пункт бара «Турниры»** — его заводит ТЗ 08 (переименование «LOST cup» и смена адреса);
  09 делает раздел, в который этот пункт ведёт, и второй раз навигацию не правит.
- **Мастер заведения турнира** (`new/[step]`) — трогается только если у турнира появилось
  обязательное поле; переделка мастера в scope не входит.

## Требования · DEV

- Поля добавляются **аддитивно**, миграцией без переписывания существующих: признак серии и лимит
  команд. Ни одно существующее поле не переименовывается и не удаляется.
- **Серия — необязательная строка у `Tournament`** (пустое значение = «без серии»). Enum,
  фиксированного списка и отдельной таблицы серий не заводится. Группировка идёт по
  нормализованному значению (обрезка пробелов, регистронезависимо), подпись группы — как ввёл
  оператор: «LOST Cup» и «lost cup» дают одну группу, а не две.
- **Какая серия первая — одна константа** в `src/lib/tournaments.ts` (название серии LOST).
  Порядок групп: LOST → остальные серии (свежий турнир выше) → «Прочие турниры». Больше нигде
  название серии в коде не зашивается.
- **Оператор видит уже заведённые серии подсказкой** при вводе (существующие значения из базы) —
  иначе опечатка заводит вторую группу вместо попадания в существующую.
- Мастер заведения турнира не трогается: серия необязательна, новый турнир попадает
  в «Прочие турниры» и получает серию в редактировании.
- **Лимит живёт у дивизиона** (необязательное число у `Division`), турнир показывает сумму.
  Причина: участие заводится в дивизион (`TournamentEntry.divisionId`), и лимит, висящий
  на турнире, невозможно проверить в момент, когда команду кладут в конкретный дивизион.
  Турнир, у которого хотя бы у одного дивизиона лимит не задан, показывает просто число команд
  без «из …»: неполная сумма врёт.
- **Проверка лимита живёт в слое данных** (`src/lib/tournaments.ts`), рядом с записью участия —
  чтобы апрув заявки, ручное добавление и перестановка между дивизионами получили её одинаково,
  а не тремя копиями в формах.
- Факт «сколько команд» берётся из `TournamentEntry` (`divisionTeams` / `listTeamRosters`) — того
  же источника, что витрина «Заявленные команды» и жеребьёвка. Второго счётчика не заводится.
- Правки схемы: `npx prisma generate`, иначе типы разъедутся. `dev.db` коммитится (`CLAUDE.md`),
  миграция — как обычно.
- Словарь статусов — `TOURNAMENT_STATUS_LABELS`, своих слов в разметке не писать.
- Черновики (`status: "draft"`) наружу не показываются нигде, включая хронологию и блоки серий:
  это правило уже действует и не ослабляется.
- `/tournaments/archive` остаётся живым адресом (он стоял в подвале) — либо ведёт на хронологию,
  либо становится ею; 404 на нём не появляется.
- Разметка — `pouf/*` и токены; hex в коде не заводится.
- Грабли: `npm run build` обязателен перед сдачей — dev собирает маршрут за маршрутом.

## Требования · DESIGN

Заполняет роль `design`. Что решить: карточка дивизиона (что на ней и чем отличается от карточки
турнира), раскладка групп-серий на 1440 и 390 при неизвестном заранее числе групп, вид счётчика
«8 из 12», его состояния «мест нет» и «13 из 12» (превышение), вид суммы на карточке турнира,
хронология (лента, группировка по годам, порядок), пустые состояния — турниров нет, в группе
нет ни одного турнира, у турнира нет дивизионов, поле серии пустое. Атомы брать из Кита; новый — артбордом.

## Требования · SEO

Заполняет роль `seo`. Раздел публичный: адреса `/tournaments` и `/tournaments/<slug>` не меняются,
у хронологии должен быть свой стабильный URL и `metadata`. Проверить, что разделение на серии не
плодит дублирующего контента (один турнир — одна каноническая страница) и что блоки-серии не
превращают ссылки в кнопки с обработчиком. Связано с `docs/tasks/03-seo-bazovyi.md` (Б3),
который идёт позже и должен застать финальную карту URL.

## Acceptance criteria

- [ ] `/tournaments` группирует турниры по значению поля «серия»: группа серии LOST первая,
      остальные серии следом, турниры без серии — последней группой «Прочие турниры»;
      турнир не попадает в две группы одновременно
- [ ] Оператор вписывает в редактировании турнира название серии, которого раньше не было
      («LOST Minor»), сохраняет → в разделе появляется группа с этим названием; правки кода
      и миграции не потребовалось
- [ ] Два турнира с сериями «LOST Cup» и «lost cup» попадают в одну группу
- [ ] Оператор стирает серию у турнира → турнир уходит в «Прочие турниры» и из раздела
      не пропадает
- [ ] В карточке турнира каждый дивизион представлен отдельной карточкой; клик по карточке
      дивизиона открывает `/tournaments/<slug>/<div>`
- [ ] У турнира с одним дивизионом карточка дивизиона одна, пустого блока и дублирующего
      заголовка не появляется
- [ ] Оператор задаёт лимит **у дивизиона** в редактировании турнира (12 у D1), сохраняет,
      перезагружает страницу — значение на месте
- [ ] Карточка дивизиона показывает «8 из 12», карточка турнира — сумму по дивизионам («20 из 24»);
      после апрува ещё одной заявки оба числа растут на единицу
- [ ] У турнира, где хотя бы у одного дивизиона лимит не задан, карточка турнира показывает число
      команд без «из …»
- [ ] Дивизион заполнен (12 из 12), турнир в статусе «Приём заявок»: в публичной заявке этот
      дивизион помечен «мест нет» и не выбирается
- [ ] Заполнены все дивизионы турнира: кнопка «Подать заявку» не отправляет форму и на экране
      написано, что мест нет
- [ ] Оператор апрувит заявку в заполненный дивизион: показано предупреждение с числами
      («12 из 12, команда станет 13-й»); после подтверждения команда в дивизионе, а счётчик
      показывает «13 из 12» — фактическое число, не обрезанное до лимита
- [ ] То же предупреждение с числами показывается при ручном добавлении команды в заполненный
      дивизион и при перестановке команды в заполненный дивизион
- [ ] Лимит не задан: приём заявок работает как до задачи, счётчик показывает число команд
      без «из …»
- [ ] Идущий турнир виден на первом экране `/tournaments` без прокрутки до хронологии и без
      поиска по группам: короткого пути в него из бара больше нет (решение 13.09.2026)
- [ ] Хронология открывается по своему адресу и показывает все нечерновые турниры в порядке
      времени; `/tournaments/archive` не отдаёт 404
- [ ] Черновик (`status: "draft"`) не виден ни в блоках серий, ни в хронологии, ни у гостя,
      ни у вошедшего игрока
- [ ] Из редактирования турнира открываются **оба** импорта — игроков и составов — и оба
      приходят с контекстом этого турнира в адресе
- [ ] После записи импорта оператор возвращается на страницу турнира, и импортированные команды
      видны в списке участников без ручной пересборки
- [ ] `grep -n "group" web/prisma/schema.prisma` показывает `Team.group`, `Series.division`,
      `GroupEntry.division` на месте: зеркала имени дивизиона не удалены и не переименованы
- [ ] Существующий турнир, заведённый до задачи, открывается и показывает дивизионы и участников
      как раньше — миграция не потребовала ручной правки данных
- [ ] `npx tsc --noEmit` и `npm run lint` чисто, `npm run build` проходит
- [ ] 390px и 1440px без горизонтального переполнения
- [ ] права: `/admin/tournaments/<slug>` без `tournaments.edit` даёт отказ; публичный раздел
      открывается гостю

## Решения по развилкам — закрыто 13.09.2026

**1. Серия — свободное текстовое поле у турнира.** Решение Стаса; фиксированный список серий
в коде отклонён. Раздел группирует турниры по значению поля, серия LOST идёт первой группой,
турниры с пустым полем — последней группой «Прочие турниры». Новую серию оператор заводит вводом
названия в редактировании турнира: ни кода серии, ни словаря, ни миграции. Цена решения — опечатка
в названии; закрывается двумя требованиями в DEV (подсказка уже заведённых значений при вводе
и группировка по нормализованному значению).

**2. Лимит команд живёт у дивизиона, турнир показывает сумму.** Решение Стаса. Причина: запись
команды идёт в дивизион (`TournamentEntry.divisionId`), и на уровне турнира момент записи
проверять нечем. Сверх лимита запись не запрещается — оператор подтверждает превышение,
поведение расписано в Scope и в acceptance criteria.

**3. Что показывает «хронология турниров» — идём по предложению `pm`.** Все нечерновые турниры
лентой по времени, включая идущий; отдельного «архива прошедших» не заводим, иначе на сайте два
списка одних и тех же турниров. Возражение есть — скажи до старта, это меняет один экран.

---

**Отчёт о выполнении сюда НЕ пишется** — он идёт записью в `WORKLOG.md`.
Здесь меняется только строка «Статус».
