# ТЗ 23: Главная показывает все турниры, а не по одному на статус

**Статус:** done
**Входит в MVP:** да

## Цель

Капитан второй команды, набор в турнир которой открыт прямо сейчас, находит свой турнир с главной
и доходит до формы заявки. Сегодня витрина держит ровно один турнир на статус, и при двух открытых
наборах второй не виден на главной вовсе и не собирает заявки. Закрыто, когда с `/` видны оба
открытых набора и из каждого есть путь в его форму.

## Контекст

- `web/src/app/(home)/tournaments-block.tsx:21` — `SLOTS` (три состояния) и ниже
  `all.find((t) => t.status === status)`: слот занимает ровно один турнир состояния, остальные
  молча отбрасываются.
- `web/src/lib/tournaments.ts:254` — `openForRegistration()` возвращает **самый ранний** открытый
  (`open.find(registrationOpen)`). На неё завязаны три места: баннер главной
  (`(home)/banner.tsx:74`), сборный `(public)/apply/page.tsx` (редирект в форму) и экран ожидания
  `(public)/me/welcome.tsx:114`.
- В `web/prisma/dev.db` турниров в статусе `registration` **два** (`s4`, `S5`), но у `s4`
  `regCloseAt` = 05.09.2026 — приём истёк, и по правилу `registrationOpen` он закрыт. То есть
  «сегодня открытых наборов один». Многокарточный случай приёмка получает подготовкой данных, а не
  правкой кода — порядок в acceptance criteria.
- Полный список турниров живёт на `/tournaments` (ТЗ 09) и от этой работы не меняется.
- `MVP.md` §1, строка «Второй турнир в приёме заявок не виден на главной».

## Scope

**Допущение по листалке (названо явно, а не подразумевается).** Стрелки листают **карточки внутри
секции статуса**. Три секции (идёт · приём заявок · завершён) остаются на экране разом и стрелками
не листаются: их всего три, они видны одновременно уже сейчас, и прятать их за стрелку — терять
соседей ради экономии, которой нет. Листается то, чего может быть много, — турниры одного статуса.

**Входит:**
- Секция статуса на главной показывает **все** турниры своего статуса, а не первый.
- Внутри секции — листание стрелками вперёд/назад, когда турниров больше, чем помещается.
- Сборный `/apply`: при нескольких открытых наборах человек **выбирает турнир**, а не попадает
  в угаданный. При одном открытом поведение прежнее — редирект в форму.
- Экран ожидания `/me` (welcome): зовёт заявиться так, чтобы открытые наборы не терялись.
- Пустая секция по-прежнему показывает лунку (`EMPTY_SLOT`).

**НЕ входит:**
- Редактор баннера главной — отдельная работа, `docs/tasks/25-redaktiruemyi-banner.md`.
- Переделка баннера под несколько наборов — карточка секции уже даёт вход в каждый турнир,
  второй призыв на том же экране только спорит с первым.
- Фильтры и пагинация полного списка турниров — для этого есть `/tournaments` (ТЗ 09).
- Черновики на витрине — решение прежнее: турнир становится событием лиги, когда открыт на заявки.

## Требования · DEV

- «Какие турниры открыты на приём» — **одно место правды** в `lib/tournaments.ts`. Существующая
  `openForRegistration()` остаётся для случаев, где нужен ровно один, и выражается через общий
  список, а не через вторую копию правила `registrationOpen`.
- Порядок карточек внутри секции — тот же, что в источнике (`listTournaments` сортирует по старту
  вниз); своей сортировки секция не заводит.
- Серверный рендер главной сохраняется: листалка — поведение клиента поверх уже отданного списка,
  догрузки по стрелке нет.

## Требования · DESIGN

Заполняет роль `design`: вид стрелок и их место у секции, поведение на 390px (сейчас карточки
листаются свайпом — стрелки не должны его отменять), состояние «турнир один» (стрелки скрыты либо
неактивны — выбрать и назвать), выбор турнира на `/apply`.

## Требования · SEO

Главная уже индексируется, новых URL работа не заводит. `/apply` при выборе из нескольких остаётся
служебным перенаправлением и в `sitemap` не добавляется. Отдельного участия `seo` не требуется.

## Acceptance criteria

**Подготовка данных (делает `qa` перед прогоном, кода не касается).** Сегодня открытый набор в базе
один: у `s4` приём истёк 05.09. Чтобы проверить многокарточный случай, `qa` через админку
(`/admin/tournaments/s4`, поле «Заявки до») ставит `regCloseAt` на дату позже сегодняшней —
открытых наборов становится два (`s4` «test», `S5` «LOST LOST»). Последний пункт возвращает дату
обратно в прошлое, поэтому `dev.db` после приёмки совпадает с состоянием до неё.

- [ ] **До подготовки**, на исходных данных: в секции «Приём заявок» ровно одна карточка — `S5`
      («LOST LOST»). `s4` в секции нет: у него `regCloseAt` в прошлом, и «приём заявок» — про
      возможность подать заявку, а не про непереключённый статус.
- [ ] **После подготовки** (`regCloseAt` у `s4` в будущем): в секции «Приём заявок» видны имена
      **обоих** турниров — «test» и «LOST LOST», а не одного.
- [ ] Клик по стрелке «вперёд» в секции с несколькими турнирами показывает следующую карточку;
      клик «назад» возвращает предыдущую.
- [ ] В секции, где турнир один, стрелки не предлагают несуществующий переход (нажатие не меняет
      содержимое секции и не даёт пустой кадр).
- [ ] Секция без единого турнира показывает лунку с текстом из `EMPTY_SLOT` — как до работы.
- [ ] Переход по имени турнира из любой карточки секции открывает `/tournaments/<slug>` именно
      этого турнира.
- [ ] `/apply` при двух открытых наборах показывает выбор из двух названий; выбор ведёт на
      `/tournaments/<slug>/apply` выбранного. При одном открытом (исходные данные) `/apply`
      по-прежнему редиректит в форму этого турнира, выбора не показывает.
- [ ] **Возврат данных:** `regCloseAt` у `s4` переводится обратно в прошлое — карточка `s4` уходит
      из секции «Приём заявок» и из выбора на `/apply`, на главной снова одна карточка. Правок кода
      между двумя состояниями не делалось.
- [ ] `npx tsc --noEmit` и `npm run lint` чисто, `npm run build` проходит.
- [ ] 390px, **1280px** и 1440px без горизонтального переполнения; на 390px секция по-прежнему
      листается свайпом.
- [ ] Права: страница публичная, гость видит секции и выбор на `/apply` наравне с игроком.

## Открытые вопросы

Нет: допущение по листалке зафиксировано в Scope и обратно не открывается.

---

**Отчёт о выполнении сюда НЕ пишется** — он идёт записью в `WORKLOG.md`.
Здесь меняется только строка «Статус».
