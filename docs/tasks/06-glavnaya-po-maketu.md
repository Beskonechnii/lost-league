# ТЗ: главная по макету `design/home/`

**Статус:** draft
**Входит в MVP:** нет — главная уже в проде и работает (`MVP.md` §1, строка «Главная `/` — есть»),
ни один из блокеров Б1–Б4 переносом макета не закрывается. Сезон открывается с нынешней витриной.
Берётся после Б1–Б4 либо параллельно ими не занятым временем; на очередь блокеров не влияет.

## Цель

Витрина в коде и витрина в макете разошлись: макет `design/home/` описывает шапку тремя островами,
верхний ряд «герой + карточка игрока» и три блока на подложках, код — свою раскладку из шести
файлов. Пока расхождение живёт, каждая правка главной делается дважды и спорится «а как в макете».
Задача закрыта, когда главная на 1440 и 390 повторяет артборды `Main.dc.html` / `Guest.dc.html` /
`Mobile.dc.html` / `Menus.dc.html` по структуре и размерам, а `design/home/` остаётся эталоном
сверки — не источником разметки.

## Контекст

**Макет** (лежит в репо, побайтно совпадает с артефактом, сверено 12.09.2026):

| Файл | Что | Размер |
|---|---|---|
| `design/home/Main.dc.html` | главная, вошёл игрок лиги | 1440×1760 |
| `design/home/Guest.dc.html` | главная, гость | 1440×1100 |
| `design/home/Mobile.dc.html` | главная, телефон | 390×2040 |
| `design/home/Menus.dc.html` | шапка: меню аккаунта и панель уведомлений | 1440×760 |
| `design/home/canvas.json` | раскладка + аннотации автора (`n-bar`, `n-top`, `n-slab`, `n-mob`) | — |

Артефакт-превью: https://claude.ai/code/artifact/d0b1862a-d038-4b21-adc5-1fc250458bac
Цикл правки макета — `design/README.md`: экраны держат разметку в своём `build.mjs` и тянут `STYLE`
из кита. **Правка макета в эту задачу не входит** — макет считается принятым.

Аннотации, которые являются требованиями, а не украшением:
- `n-bar` — шапка тремя островами: бренд, меню, вход; «LOST cup» — акцентный пункт с названием турнира.
- `n-top` — карточка игрока фиксированной ширины 351px, герой добирает остаток, высоты равны по сетке.
- `n-slab` — Турниры, Матчи и Баллы лежат на подложках; шапка, герой и карточка игрока — сами острова,
  второй рамки им не нужно.
- `n-mob` — на 390 карточка входа поднята над героем, турниры уходят в горизонтальный скролл.

**Код сейчас** — `web/src/app/(home)/`, 822 строки:
`page.tsx` (214) · `mini-profile.tsx` (194) · `tournaments-block.tsx` (130) · `banner.tsx` (102) ·
`points.tsx` (87) · `shards.tsx` (79) · `layout.tsx` (16).
Шапка витрины — `web/src/app/_components/home-shell.tsx` (бренд + Турниры/Ростер/Правила + `AvatarMenu`),
пункты аккаунта собирает `web/src/app/_components/account-nav.ts` (общий для сайдбара и витрины).

**Перенос — не копипаста.** В артборде свой `<style>` внутри (`.tour`, `.mcard`, `.tl`, `.wr`,
пиксельные размеры, hex вместо токенов). Проект — Tailwind v4 + `web/src/components/pouf/*` на токенах
`pouf/pouf.css`. Переносятся **структура, порядок блоков и размеры**; CSS пересобирается на токенах
и компонентах Кита. Хардкод hex из артборда в код не едет.

**Что уже есть и не пишется заново:** `pouf/series-card.tsx`, `pouf/blocks.tsx` (`StatTile`, `SITE_MAX_W`),
`pouf/table.tsx`, `pouf/hero.tsx`, `pouf/tabs.tsx`, `pouf/Segmented.tsx`, `pouf/status.tsx`,
`pouf/player-line.tsx`, витрина Кита `/admin/kit` (заведена в `docs/tasks/05-kit-v-kod.md`).
Винрейт считается — `web/src/lib/player-record.ts` → `getPlayerRecord(playerId)` отдаёт
`{ games, wins, losses, winrate }`; на главной сейчас не запрашивается.
Адреса для пунктов макета «Команды» и «Игроки» существуют: `/roster` и `/roster/players`
(`app/(public)/roster/_components/pool-switch.tsx`).

**Служебный канал лиги уже работает и новой сущности под колокольчик не требуется:**
`web/src/app/(public)/chat/system/page.tsx` — адрес `/chat/system`, лента `readOnly` (писать
нельзя), у части сообщений есть кнопки выбора из реестра `web/src/lib/chat-actions.ts`
(«Иду / Не иду» и прочее). Собеседник — служебный аккаунт: `web/src/lib/system-chat.ts`
(`SYSTEM_NAME`, `systemAccountId()`), флаг `system Boolean` у аккаунта — `web/prisma/schema.prisma:279`.
Беседа, непрочитанное, живой канал и отметка о прочтении работают через общий `web/src/lib/chat.ts`
(`findConversation` · `messages` · `markRead`) — правок в нём задача не требует.
Счётчик «Сообщения» в аватар-меню сейчас считает `unreadTotal` по всем беседам сразу
(`web/src/app/_components/account-nav.ts:58`).

**Решения, которые задачу ограничивают и не переигрываются:**
- `DECISIONS.md` 10.09.2026 «у главной свой хром» — сайдбара на `/` нет, шапка строкой.
- `DECISIONS.md` 10.09.2026 «витрина показывает состояния воронки, а не разделы» — карточка игрока
  обязана показывать все состояния воронки, а не только «игрок лиги».
- `DECISIONS.md` 04.09.2026 «системные уведомления живут в чате, а не в колокольчике» —
  решение запрещает **второй список сообщений**, а не панель как таковую: «Колокольчик — тот же
  список сообщений, только второй». Поэтому колокольчик здесь — **витрина служебного канала**
  `/chat/system`, а группа «Личные» из `Menus.dc.html` не реализуется. Решение **не
  пересматривается**, блок в `DECISIONS.md` не трогается.
- `DECISIONS.md` 04.09.2026 «ростер режется дважды» — «Команды» и «Игроки» это два разреза одного
  раздела, а не два раздела.
- `MVP.md` §2 и §4 — осколки не развиваем и не выпиливаем.

## Scope

Работа делится на два подэтапа, они сдаются и коммитятся отдельно.

### Подэтап A · Кит и верхний ряд

**Входит:**
- Атом **метр винрейта** в Кит: артборд `design/kit/parts/WinrateMeter.html` (+ `.css` при нужде,
  строка в `design/kit/canvas.json`, пересборка `node build.mjs`) → `web/src/components/pouf/winrate-meter.tsx`
  → секция на витрине `/admin/kit`. Вид — из `Main.dc.html`, класс `.wr`: процент, счёт побед–поражений,
  полоса. Атом, потому что тот же показатель нужен профилю игрока и ростеру.
- **Шапка тремя островами** по `Menus.dc.html` — перекладка `web/src/app/_components/home-shell.tsx`:
  остров бренда, остров меню, остров входа/аккаунта. Пункты: Главная · **LOST cup** (акцентный,
  ведёт на текущий турнир, при отсутствии турнира пункт не рисуется) · Команды (`/roster`) ·
  Игроки (`/roster/players`) · Правила (`/rules`).
- **Меню аккаунта** по `Menus.dc.html` (левая панель артборда): шапка с ником и подписью
  «команда · позиция», пункты профиль / сообщения со счётчиком / админ / настройки / выйти.
  Источник пунктов остаётся `account-nav.ts` — вид меняется, состав нет.
- **Колокольчик и панель уведомлений** по `Menus.dc.html` (правая панель артборда) — **витрина
  служебного канала, а не новая сущность**. В панели одна группа — **Служебные**; источник строк —
  та же беседа с лигой (`systemAccountId()` → `findConversation` → `messages`), что открыта на
  `/chat/system`. Состав панели:
  - счётчик на колокольчике — **непрочитанное служебного канала**, не сумма по всем беседам;
  - строки — последние служебные сообщения, непрочитанные помечены; пустых групп панель не рисует;
  - «прочитать все» отмечает прочитанным **служебный разговор** той же отметкой, которую ставит
    открытая страница `/chat/system` (`markRead`): два пути — одно состояние;
  - низ панели — ссылка на `/chat/system`: панель это быстрый просмотр, полная лента там.
- **Счётчик «Сообщения» в меню аккаунта перестаёт включать служебный канал** — считает только личные
  беседы. Иначе одно и то же непрочитанное показывается дважды: на колокольчике и на «Сообщениях».
  Два счётчика не пересекаются, вместе дают прежнюю сумму.
- **Герой** по `Main.dc.html` / `Guest.dc.html`: место героя-новости занимает существующий баннер
  по данным (`(home)/banner.tsx`) — раскладка, размеры и картинка-полотно из макета, содержание
  прежнее (приём заявок → вступление → анонс).
- **Карточка игрока** 351px по `Main.dc.html`: аватар, ник, команда, позиция, три показателя
  (MMR · место в TR · винрейт за сезон метром), кнопка «Мой профиль». Высота равна герою на 1440.
- **Гостевой вариант карточки** по `Guest.dc.html`: «Войди в аккаунт» + кнопка «Войти».
- **Винрейт за сезон** — новый запрос `getPlayerRecord` в карточке для вошедшего игрока лиги.
- Все прочие состояния воронки (анкета не дописана / на модерации / отклонена / оператор без
  карточки) остаются работать, их в макете нет — см. DEV.

**НЕ входит:**
- Пункт меню **«Новости»** и раздел новостей — раздела в проекте нет, а завести его значит модель,
  редактор и страницу: это отдельный продукт, а не перенос макета. Строка в `BACKLOG.md`.
- **Группа «Личные» в панели уведомлений** (есть в `Menus.dc.html`) — это и есть второй список
  сообщений, запрещённый решением `DECISIONS.md` 04.09.2026. Личные беседы открываются пунктом
  **«Сообщения»** в меню аккаунта — он там уже есть, второго входа не заводим.
- **Ответ на служебное сообщение из панели** (кнопки выбора `chat-actions.ts`) — панель read-only,
  кнопки остаются на `/chat/system`. Меньше поверхности: состояние выбора живёт в сущности и
  перерисовывается лентой, дублировать этот механизм во всплывающей панели незачем.
- **Правка `web/src/lib/chat.ts`, `system-chat.ts`, `chat-actions.ts` по существу** — панель только
  читает то, что они уже отдают. Недостающий экспорт (непрочитанное одной беседы) — не новая
  логика, а вынос наружу существующего `unreadIn`.
- **Карточка турнира, карточка матча, таблица топ-10 как артборды Кита** — это композиции экрана,
  а не атомы; `design/README.md`: «кит не распухает композициями, композиция берёт готовый атом».
  Живут в `(home)/`, собираются из `pouf/*`.
- Правка самого макета `design/home/` — макет принят и является эталоном сверки.

### Подэтап B · Подложки и телефон

**Входит:**
- **Турниры** на подложке по `Main.dc.html` / `Guest.dc.html`: карточки «Идёт» / «Регистрация открыта» /
  «Завершён» со статусом, подписью сезона, парой показателей (Команд / Набрано и Призовой) и ссылкой
  «все турниры». Перекладка `(home)/tournaments-block.tsx`.
- **Матчи** на подложке по `Main.dc.html`: три разреза табами (Прошедшие · Сегодня · Будущие),
  карточка встречи с турниром, временем, двумя командами и счётом, ссылка «все матчи».
  Заменяет нынешние две колонки «Ближайшие встречи» / «Последние результаты» в `(home)/page.tsx`.
- **Баллы** на подложке по `Main.dc.html`: топ-10 TP, строка «место · ник · баллы», ссылка «весь топ».
  Перекладка `(home)/points.tsx`.
- **Телефон 390** по `Mobile.dc.html`: карточка входа над героем, турниры горизонтальным скроллом,
  матчи и баллы в одну колонку, укороченный топ.
- **Пустые состояния**, которых в макете нет: турниров нет, встреч нет, топ пуст — через
  `pouf/feedback.tsx` (`EmptyState`), без схлопывания подложки в ноль высоты. Образец формулировок
  уже в `(home)/page.tsx` («Встреч ещё нет», «Сезон ещё не заведён»).
- **Снятие блока осколков с главной**: `<ShardsBlock>` перестаёт рендериться в `(home)/page.tsx`.
  Файл `(home)/shards.tsx`, атом `pouf/shards.tsx` и витрина осколков в профиле игрока — **не трогаются**.

**НЕ входит:**
- **Таб «Shards» в блоке «Баллы»** (есть в `Main.dc.html` и `Mobile.dc.html`) — это топ-10 по осколкам,
  лидерборда по осколкам в `lib/shards.ts` нет, а заводить его запрещает `MVP.md` §2 («осколки не
  развиваем до ответа, на что тратятся»). Блок едет одним разрезом TP, без табов. Строка в `BACKLOG.md`.
- **Удаление `(home)/shards.tsx` и атома `pouf/shards.tsx`** — `MVP.md` §4: рабочий код перед релизом
  не выпиливаем, снимаем только показ на витрине.
- **Правка страниц `/tournaments`, `/series`, `/roster`** — задача про витрину; менять экраны, куда
  ведут ссылки, здесь нечего.
- **Тёмная тема** — компоненты пишутся на токенах, палитра отдельным блоком (`MVP.md` §2).

## Требования · DEV

- Разметка собирается из `web/src/components/pouf/*` и токенов `pouf/pouf.css`. **Ни одного hex
  из артборда** в новом коде; пиксельные размеры из макета (351px карточки, высоты рядов, радиусы
  подложек) переносятся как значения раскладки, цвета — нет.
- Ширина контента — существующий `SITE_MAX_W` из `pouf/blocks.tsx`, своей константы не заводить.
- Подложки блоков (`n-slab`) — один общий контейнер-компонент внутри `(home)/`, а не три копии.
  Шапка, герой и карточка игрока подложки не получают.
- Данные: карточка игрока добирает `getPlayerRecord` из `lib/player-record.ts`. Запросы страницы
  идут одним `Promise.all`, как сейчас в `page.tsx`; последовательных await-цепочек не добавлять.
- Карточка матча — расширение `pouf/series-card.tsx` вариантом, если вид отличается; второй карточки
  встречи в проекте не заводится.
- Состояния воронки из `mini-profile.tsx` сохраняются все: макет рисует два крайних (гость и игрок
  лиги), остальные три переносятся в новую карточку тем же текстом, что сейчас.
- Панель уведомлений берёт данные только из готового: `systemAccountId()` → `findConversation` →
  `messages` (`lib/chat.ts`). Своей модели, своей таблицы и своего понятия «уведомление» не
  заводится; служебное сообщение — это `ChatMessage`, у него есть текст и время, заголовка нет.
- Отметка о прочтении — существующий `markRead(conversationId, accountId)` (путь `/api/chat/read`
  уже есть). Второй механизм прочитанного не заводится.
- Счётчики разводятся по источникам: колокольчик — непрочитанное служебной беседы,
  «Сообщения» в `account-nav.ts` — `unreadTotal` **минус** служебная беседа. Одно и то же
  непрочитанное не попадает в два бейджа.
- Колокольчик рисуется только вошедшему; у гостя элемента нет в разметке.
- `home-shell.tsx` остаётся хромом только `(home)`; сайдбар `app-shell.tsx` не трогается.
- Подэтапы A и B коммитятся отдельно; между ними главная должна оставаться рабочей.
- Грабли: клиентские хуки в файле, который импортирует серверный компонент, ловит только
  `npm run build` (см. `pouf/password-field.tsx`).

## Требования · DESIGN

Заполняет роль `design` перед подэтапом A. Что нужно от неё:
- назвать токены-замены хардкоду из `Main.dc.html` / `Menus.dc.html` (фон подложек, рамка островов,
  цвет полосы винрейта, акцент пункта «LOST cup»);
- поведение шапки между 390 и 1440: что уходит первым при сужении, где ломается ряд меню;
- поведение ряда «герой + карточка 351px» на промежуточных ширинах (768–1200) — макета на них нет;
- состояния метра винрейта: игр нет, 0%, 100%;
- горизонтальный скролл турниров на 390 (`n-mob`): чем показывается, что есть продолжение;
- ряд уведомления: в макете он двухстрочный (заголовок + текст), а у служебного сообщения есть
  только текст и время — как раскладывается строка, сколько строк текста показываем, чем обрезаем;
- состояния панели: непрочитанного нет · служебных сообщений нет вовсе · длинный список
  (сколько строк держим до скролла);
- панель на 390: всплывающая панель или лист снизу.

## Требования · SEO

Заполняет роль `seo`. Публичный URL `/` не меняется; `metadata` главной уже есть в `(home)/page.tsx`.
Что проверить: заголовки блоков остаются текстом, а не картинкой; ссылки «все турниры» / «все матчи» /
«весь топ» — настоящие `<a href>`; смена структуры не ломает то, что закладывается в
`docs/tasks/03-seo-bazovyi.md`. Меню аккаунта и карточка игрока — состояние вошедшего, в индексацию
не попадают.

## Acceptance criteria

### Подэтап A

- [ ] `grep -rn "WinrateMeter" web/src/components/pouf/` находит экспорт; `design/kit/parts/WinrateMeter.html`
      существует и упомянут в `design/kit/canvas.json`
- [ ] `/admin/kit` показывает секцию метра винрейта живым компонентом: состояния «игр нет», «0%»,
      «67% · 12–6», «100%»
- [ ] На 1440 шапка `/` — три отдельных острова с зазором между ними, как в `Menus.dc.html`;
      единого сплошного блока на всю ширину нет
- [ ] Пункты шапки: Главная, LOST cup, Команды, Игроки, Правила. Клик по «Команды» открывает `/roster`,
      по «Игроки» — `/roster/players`, по «LOST cup» — страницу текущего турнира. Пункта «Новости» нет
- [ ] Когда текущего турнира нет (`currentTournament` вернул `null`), пункт «LOST cup» не отрисован
      и дырки на его месте не остаётся
- [ ] Клик по аватару открывает меню по `Menus.dc.html`: ник, подпись «команда · позиция», пункты
      профиль / сообщения / настройки / выйти; «Админ» виден только при наличии прав, у гостя меню нет
- [ ] У сообщений в меню виден счётчик непрочитанного, когда `unread > 0`, и не виден при нуле
- [ ] Клик по колокольчику открывает панель уведомлений; в ней есть группа «Служебные» и **нет**
      группы «Личные» — `grep -rn "Личные" web/src/app/_components/` ничего не находит
- [ ] Верхние строки панели совпадают по тексту и порядку с последними сообщениями на `/chat/system`
- [ ] Число на колокольчике равно числу непрочитанных служебных сообщений: прислать игроку одно
      служебное сообщение, перезагрузить `/` — число выросло на единицу
- [ ] Клик «прочитать все» → счётчик колокольчика пропадает, и после этого `/chat/system`
      не показывает непрочитанного
- [ ] Обратный путь той же отметкой: открыть `/chat/system`, вернуться на `/` — счётчика на
      колокольчике нет
- [ ] Одно непрочитанное от лиги и одно от живого игрока: на колокольчике «1» и у «Сообщения» «1»
      (не «2» в обоих местах)
- [ ] Непрочитанного нет: панель открывается, показывает последние служебные сообщения без пометки
      «непрочитано», счётчика на колокольчике нет
- [ ] Служебных сообщений нет вовсе: панель открывается и показывает пустое состояние текстом,
      высота панели не нулевая
- [ ] Внизу панели — ссылка на `/chat/system`, клик открывает служебный канал
- [ ] Кнопок выбора («Иду / Не иду» и прочие из `chat-actions.ts`) в панели нет; на `/chat/system`
      они на месте и нажимаются как прежде
- [ ] У гостя колокольчика в шапке нет: `/` без входа — элемента нет в DOM, а не «есть и пустой»
- [ ] На 1440 у вошедшего игрока лиги: герой слева, карточка игрока справа шириной 351px,
      нижние края обоих на одной линии (сверка с `Main.dc.html`)
- [ ] Карточка игрока показывает MMR, место в TR и винрейт за сезон: процент, счёт вида «12–6»
      и заполненную на этот процент полосу
- [ ] У игрока без сыгранных встреч вместо процента стоит «—», полоса пустая, блок не исчезает
- [ ] У гостя на месте карточки — «Войди в аккаунт» с кнопкой «Войти» (сверка с `Guest.dc.html`)
- [ ] Три оставшихся состояния воронки (анкета не дописана, на модерации, отклонена) открываются
      в карточке и ведут туда же, куда вели до переноса
- [ ] Ни один файл, изменённый в подэтапе A, не содержит hex-цвета — только токены `pouf.css`
- [ ] `npx tsc --noEmit` и `npm run lint` чисто, `npm run build` проходит
- [ ] 390px и 1440px без горизонтального переполнения (`document.scrollingElement.scrollWidth`
      равен ширине окна)
- [ ] права: гость не видит меню аккаунта и карточки игрока; аккаунт без прав оператора не видит
      пункт «Админ»

### Подэтап B

- [ ] На 1440 блоки Турниры, Матчи и Баллы лежат каждый на своей подложке, а шапка, герой и карточка
      игрока подложки не имеют (аннотация `n-slab`, сверка с `Main.dc.html`)
- [ ] Блок «Турниры» показывает карточки со статусом «Идёт» / «Регистрация открыта» / «Завершён»,
      у каждой — два показателя и подпись сезона; ссылка «все турниры» ведёт на `/tournaments`
- [ ] Блок «Матчи»: переключение табов Прошедшие / Сегодня / Будущие меняет список карточек;
      карточка показывает турнир, время, обе команды и счёт; «все матчи» ведёт в архив встреч
- [ ] Таб «Сегодня» при отсутствии сегодняшних встреч показывает пустое состояние, а не пустую
      подложку нулевой высоты
- [ ] Блок «Баллы» показывает топ-10 строками «место · ник · баллы»; «весь топ» ведёт на страницу TP
      турнира; таба «Shards» в блоке нет
- [ ] Блок осколков на `/` не отрисован; `web/src/app/(home)/shards.tsx` и
      `web/src/components/pouf/shards.tsx` в репозитории на месте, витрина осколков в профиле игрока
      открывается как прежде
- [ ] При пустой базе (турниров нет, встреч нет, баллов нет) главная открывается, каждый из трёх
      блоков показывает своё пустое состояние, ошибок в консоли нет
- [ ] На 390 порядок сверху вниз: шапка, карточка входа, герой, турниры, матчи, баллы
      (сверка с `Mobile.dc.html`)
- [ ] На 390 турниры листаются горизонтально, вертикального списка карточек нет
- [ ] `npx tsc --noEmit` и `npm run lint` чисто, `npm run build` проходит
- [ ] 390px и 1440px без горизонтального переполнения
- [ ] права: гость видит все три блока; ссылки из блоков ведут на публичные страницы и не требуют входа

## Открытые вопросы

Нет.

**Закрыт 12.09.2026 — колокольчик.** Панель уведомлений входит в подэтап A как витрина служебного
канала `/chat/system`; группа «Личные» не реализуется. Решение `DECISIONS.md` 04.09.2026 остаётся
в силе и не пересматривается: оно запрещает второй список сообщений, а не окно быстрого просмотра
того же канала.

---

**Отчёт о выполнении сюда НЕ пишется** — он идёт записью в `WORKLOG.md`.
Здесь меняется только строка «Статус».
