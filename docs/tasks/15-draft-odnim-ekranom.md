# ТЗ 15: Fearless draft одним экраном

**Статус:** **15а — ready** (раскладка активного драфта). Роль/доступ капитанов
(«это не я») в 15а не входит — отдельный вопрос, ждёт решения Стаса (см. «Открытые
вопросы»), в очередь не встаёт и отдельным пунктом заводится в `BACKLOG.md` по итогу ответа.
**Входит в MVP:** **нет.** `MVP.md` §1 «Админка — есть целиком... доделывать к релизу
нечего»: fearless-борд уже проводит любой драфт от монетки до последней карты серии —
`lib/fearless.ts` и текущий UI (`fearless-run.tsx` и соседи) работают. Этап меняет подачу
для эфира (насколько удобно оператору вести драфт на трансляции), а не способность лиги
провести серию. Сезон открывается и без него. При этом это **одобренная Стасом работа
общей очереди** (`MVP.md` §0, строка 15 — «следующим берётся 15») — ТЗ делается и идёт
в работу по этой очереди, а не по статусу MVP-блокера; строка в `MVP.md` §1 не добавляется.

## Цель

Оператор эфира ведёт fearless-драфт по одному экрану так, чтобы зритель и капитаны на
трансляции считывали ход серии тем же взглядом, что и в референсных драфт-комнатах
(WSS/lastbattle): кто банит/пикает прямо сейчас, что уже взято каждой стороной, на какой
карте серии идёт драфт. Закрыто, когда активный экран драфта (`/admin/fearless-draft/<id>`,
состояние «идёт драфт») перекладывает уже работающие данные в раскладку колонками по
сторонам с переключателем карты сверху и карточками капитанов снизу — **без изменения
правил драфта**.

## Контекст

**Движок — чужая земля, не трогаем.** `web/src/lib/fearless.ts`: чистые функции без БД,
уже согласованные правила (монетка, `SEQUENCE` — CM-lite, fearless-ограничение, таймеры
с банком). `FEARLESS_VERSION = 2`. `SEQUENCE.length === 20` (6 банов + 4 пика стадии 1,
4 бана + 6 пиков стадии 2) — в тексте задачи со слов Стаса «номера хода 1–16»: это дословная
цитата референса, у нашего движка 20 шагов, **не 16**. Нумерация на экране идёт 1..20,
менять `SEQUENCE` этот этап не может — это переигровка регламента, а не раскладки.

**Что сегодня в UI и что от него берём:**

| Файл | Роль сегодня | Что меняется |
|---|---|---|
| `fearless-board.tsx` | оболочка сессии, автосейв, полоса заголовка/статуса | не трогаем |
| `fearless-run.tsx` | часы банка, шапка карты, чей ход, компоновка `HeroPool`+`Sequence`+`PastMaps` | перекладывается под новую композицию (см. Scope) |
| `sequence.tsx` | **уже** два столбца (своя команда слева/справа) с номером по центру, 1..20 — именно это описывает референс. Сегодня стоит узкой колонкой (22rem) сбоку от пула | делится на две колонки, фланкирующие пул, вместо одной узкой панели сбоку |
| `hero-pool.tsx` | плитки героев по атрибуту, клик = выбор | не трогаем логику, меняется место в сетке |
| `past-maps.tsx` | архив взятых героев по картам, список ниже пула | заменяется переключателем карт сверху (см. Scope); данные те же (`picksOfGame`) |
| `fearless-setup.tsx` | экран до старта: команды, Bo, монетка | **не трогаем** — референс и жалоба про экран активного драфта, не про настройку |
| `types.ts` | `TeamRef = { id, name, color, logo }` | добавляется необязательное поле капитана (см. DEV) |
| `[id]/page.tsx` | собирает `TeamRef[]` через `listTeams()` | меняется на `listTeamRosters()` ради капитана |
| Строка текущего хода (`fearless-run.tsx:157-179`) | уже статусная строка (`Alert`/цветная плашка с обводкой команды), не модалка | **уже соответствует** требованию «статусная строка вместо модалок» — в активном экране драфта модалок нет вовсе. Ничего не удаляем, переносим на новое место в композиции |
| `api/fearless/[id]/route.ts` | `PATCH`/`DELETE` под `guard("tools")`, единая точка записи | не трогаем |

**Капитан уже есть как данные.** `RosterSpot.isCaptain` (`web/prisma/schema.prisma`),
резолвится в `src/lib/roster-data.ts`: `listTeamRosters()` отдаёт `TeamWithRoster.players[]`
с `isCaptain`, `nickname`, `photo`, `mmr`. Команда без отмеченного капитана — обычный случай
(поле проставляется руками в ростер-редакторе), не ошибка данных.

**Карточка игрока — уже атом Кита.** `components/pouf/player-line.tsx` (`PlayerLine`):
аватар/ник/подпись/число, без знания о сущности «игрок». Домен резолвится локальным
адаптером рядом с потребителем — прецедент уже есть: `(admin)/underbeer/[id]/_components/player-line.tsx`
(`DraftPlayerLine`, оборачивает `PlayerLine` под пул UNDERBEER). Для капитанов заводится
такой же локальный адаптер в `fearless-draft/_components/`, **не новый атом Кита**.

**Переключатель из равных вариантов — уже атом Кита.** `PillTrack`/`PillButton`
(`components/pouf/tabs.tsx`), уже используется в этом же инструменте для выбора Bo
(`fearless-setup.tsx:78-85`) и для FP/Radiant. Тот же язык — для переключателя карт серии.

**Публичного доступа к fearless-драфту нет вовсе.** В отличие от UNDERBEER (`/overlay/underbeer/[id]`,
группа `(bare)`, `docs/ARCHITECTURE.md` §«UNDERBEER 2.0»), у fearless-драфта нет ни публичного
оверлея, ни какого-либо доступа за пределами `guard("tools")`. `TeamRef` не знает о капитане
как о человеке с доступом — только как о карточке для показа. `api/fearless/[id]` пишет
payload целиком, одним PATCH, без понятия «чья половина хода».

## Scope

**Входит (15а):**

- **Переключатель карты серии сверху** — ряд из `bestOf` пилюль («Карта 1» … «Карта N»,
  `PillTrack`/`PillButton`). Активная (`state.current`) выделена. Уже сыгранные помечены
  как сыгранные (галочка/приглушённый тон). Ненаступившие — недоступны для клика
  (`i > state.current`, `disabled`).
- Клик по **сыгранной** карте показывает её расписание (`Sequence` + список взятых) в режиме
  чтения — без часов хода и без возможности выбрать героя. Это **локальное состояние
  просмотра** экрана (`viewing: number`, не персистится, не пишется в payload) —
  `state.current` (истина движка — какая карта сейчас реально драфтится) не меняется.
  Любое действие хода на активной карте (пик, бан, отмена, «следующая карта») сбрасывает
  просмотр обратно на активную.
- **Колонки последовательности карты по своей стороне.** `Sequence` перестаёт быть одной
  узкой панелью сбоку и делится на две колонки, фланкирующие пул героев: слева — слоты
  команды с первым пиком на этой карте, справа — слоты второй (как сегодня определяет
  `firstPickOf`/`teamOfSeq`, логика не меняется). Номер хода 1..20 — между двумя колонками
  или в самих слотах, точную сетку решает `design`.
- **Карточки капитанов** — под своей колонкой (или в общем ряду снизу, решает `design`):
  ник, аватар, MMR, по данным `RosterSpot.isCaptain` активной команды. Нет отмеченного
  капитана — нейтральная подпись вместо карточки («капитан не назначен»), экран не падает.
- Хero-пул остаётся кликабельной сеткой по центру композиции — логика и данные `hero-pool.tsx`
  не меняются, меняется только его место в разметке.
- Строка текущего хода (кто банит/пикает) остаётся на экране единственным способом показать
  состояние — переносится в новую композицию как есть по смыслу (см. Контекст: она уже не
  модалка).
- Часы (банк + основное время) остаются наверху композиции, логика `fearless-run.tsx` не
  трогается — переставляется по месту в новой сетке.

**НЕ входит:**

- **Правила драфта, очередь ходов, монетка, fearless-ограничение, тайминги** — `lib/fearless.ts`
  не меняется ни строкой. Это чужая земля даже для «просто раскладки» (§ Scope выше).
- **Смена роли/доступа капитанов («это не я»).** Референс подразумевает, что у каждой
  стороны есть свой человек за своим устройством, который может сказать «это не моя сторона»
  и переключиться. Сегодня у fearless-драфта **нет публичного доступа вообще** — это
  односторонний операторский инструмент за `guard("tools")` (см. Контекст), `TeamRef` не несёт
  личность капитана как учётку с правом хода. Добавить такое — не раскладка, а новая подсистема:
  публичный маршрут (по образцу `(bare)/overlay/underbeer`), идентификация стороны по ссылке,
  разграничение «кто может внести именно этот ход», вопросы модерации/честности хода без
  присмотра оператора. Решается отдельно — см. «Открытые вопросы».
- **Экран настройки (`FearlessSetup`)** — команды, Bo, монетка. Референс и разбор касаются
  экрана активного драфта, не подготовки к нему; менять то, что не жаловались, — риск без
  выигрыша.
- **Публичный оверлей для трансляции** (`/overlay/fearless/...` по аналогии с UNDERBEER) —
  этим ТЗ не заводится. Если нужен отдельно — новая идея, новое ТЗ.
- **Модалки-подтверждения** (например, у кнопки «Сбросить») — их сегодня на активном экране
  драфта нет вовсе, значит и убирать нечего. Не добавляем их и не изобретаем повод переспросить.
- **Изменение количества/порядка шагов `SEQUENCE`** — задача цитирует референс «1–16», у нас 20
  шагов; номерация на экране считается от факта (`SEQUENCE.length`), регламент не переигрывается.

## Требования · DEV

- `types.ts`: `TeamRef` получает необязательное поле
  `captain: { nickname: string; photo: string | null; mmr: number | null } | null`.
  Существующие потребители (`FearlessSetup`), которым капитан не нужен, поле игнорируют —
  не breaking change.
- `[id]/page.tsx`: `listTeams()` → `listTeamRosters()`, капитан команды —
  `team.players.find((p) => p.isCaptain) ?? null`, резолвится в `TeamRef.captain` при сборке
  `teamRefs`. Остальные поля (`id/name/color/logo`) собираются как сейчас.
- Просмотр прошлой карты — **состояние экрана**, не сессии: обычный `useState` в
  `fearless-run.tsx` (или в новом компоненте-композиции), не уходит в `payload` и не шлёт
  `PATCH`. Переключение карт не создаёт сетевых запросов.
- Капитанская карточка — локальный адаптер над `PlayerLine` (как `DraftPlayerLine` у UNDERBEER),
  заводится рядом с потребителем в `fearless-draft/_components/`, не в Ките: карточка знает
  про поле `captain` конкретно этого инструмента, второго потребителя у неё нет.
- Кит новых атомов не заводит: `PillTrack`/`PillButton`, `PlayerLine`, `StatusPill`, `Alert`,
  `Panel` — всё уже есть. Если в разборе `design` найдёт недостающий примитив — заводится
  в Ките по общему правилу, а не по месту; ожидание — что не понадобится.
- `fearless.ts`, `api/fearless/*`, `prisma/schema.prisma` — без изменений.

## Требования · DESIGN

Заполняет роль `design`. Ориентир — драфт-комната WSS/lastbattle (раскладка и логика подачи,
**не палитра** — оформление Light Clay/Кит, решение Стаса). Готового скриншота/ссылки в
репозитории нет — принести референс перед версткой (в `docs/brand/` или по месту в этом файле).

Нужно решить и зафиксировать здесь:
- точную сетку колонок (слоты команды · пул · слоты команды) на 1280/1440 и как она
  складывается на 390 (см. «Мобильная раскладка» ниже — не рабочий сценарий, но не должна течь);
- место переключателя карт (шапка композиции, над часами или под ними) и вид пилюли для
  «сыгранная»/«активная»/«заблокированная» карты;
- место карточек капитанов — под своей колонкой слотов или отдельным рядом снизу через всю
  ширину;
- состояние «капитан не назначен» — как выглядит пустая карточка;
- состояние «не рабочий 390» — как складывается композиция без горизонтального переполнения.

### Мобильная раскладка — тот же экран, не отдельный разговор

Оператор ведёт драфт с рабочего места трансляции (десктоп/ноутбук), это не сценарий с телефона —
в отличие от публичного продукта (`UI-GUIDELINES.md` §6, «больше половины аудитории с телефона»),
здесь аудитория другая. Решение: **отдельный мобильный дизайн не проектируем**, но 390 обязан
пройти стандартный чек-лист — без горизонтального переполнения, колонки складываются в вертикальный
стек тем же адаптивом, что уже есть у соседних панелей этого инструмента (`Panel`), а не
специальной раскладкой под палец. Тач-цели 44×44 (§6) к этому экрану не применяются — это не
целевое устройство.

## Требования · SEO

Не требуется. `/admin/fearless-draft/*` — служебный маршрут под `guard("tools")`,
группа `(admin)` уже отдаёт `robots: noindex` (`docs/UI-GUIDELINES.md` §5). Новых публичных
URL этап не создаёт, sitemap не меняется.

## Acceptance criteria

- [ ] На экране активного драфта (`state` не пуст) сверху виден переключатель карт серии:
      число пилюль равно `bestOf`, активная карта (`state.current`) выделена
- [ ] Сыгранные карты (`i < state.current`) помечены как сыгранные и кликабельны; карты
      `i > state.current` недоступны для клика (`disabled`)
- [ ] Клик по сыгранной карте показывает её расписание в режиме чтения (герои те же, что
      и раньше в «Взято в прошлых картах»); часов хода и кликабельных слотов пула на ней нет
- [ ] После просмотра сыгранной карты один пик/бан на активной карте возвращает экран в режим
      «активная карта» без ручного клика по её пилюле
- [ ] Просмотр сыгранной карты не создаёт сетевых запросов (вкладка Network пуста при клике по
      пилюле прошлой карты)
- [ ] Слоты последовательности карты стоят двумя колонками у своей команды (команда первого
      пика слева, вторая справа — как сегодня определяет `firstPickOf`), с номером хода от 1
      до `SEQUENCE.length` (20)
- [ ] Under каждой (или под обеими) стороной видна карточка капитана: ник, аватар, MMR — по
      `RosterSpot.isCaptain` активной команды
- [ ] Команда без отмеченного капитана показывает нейтральную подпись вместо карточки, ошибки
      в консоли и падения страницы нет
- [ ] На экране активного драфта нет ни одного модального окна/поповера, перекрывающего борд;
      состояние хода читается одной строкой на странице
- [ ] Экран настройки (`FearlessSetup`) визуально и функционально не изменился
- [ ] `web/src/lib/fearless.ts` в диффе этапа не изменён
- [ ] `web/prisma/schema.prisma` и `web/prisma/migrations/` в диффе этапа не изменены
- [ ] `npx tsc --noEmit`, `npm run lint` чисто, `npm run build` проходит
- [ ] 390px, 1280px и 1440px без горизонтального переполнения на `/admin/fearless-draft/<id>`
      в состоянии «идёт драфт»
- [ ] `/admin/fearless-draft/<id>` без права `tools` — отказ (поведение сегодняшнее, не менялось)

## Открытые вопросы

**Смена роли/доступа капитанов («это не я») — решает Стас, три варианта:**

1. **Не делаем вовсе.** Инструмент остаётся односторонним: карточка капитана — декоративная,
   для эфира; оба хода вносит один оператор. Дешевле всего, референс закрывается частично
   (раскладка — да, роль — нет).
2. **Декоративный переключатель сторон, без доступа.** Кнопка у оператора, которая разворачивает
   раскладку (лево/право меняются местами) — без какой-либо идентификации, прав или отдельной
   ссылки. Дёшево, входит в 15а без нового маршрута и без вопросов доступа — но это не то же
   самое, что «это не я» в референсе (там про личность второго человека, не про зеркалирование
   у одного).
3. **Публичная драфт-комната для двух капитанов** — отдельная подсистема после 15а: маршрут по
   образцу `(bare)/overlay/underbeer`, ссылка на свою сторону, право вносить ход только за неё,
   «это не я» как выход из роли/переключение. Тянет за собой вопросы модерации хода без оператора
   и доверия к капитанам — до них не размечается отдельным ТЗ.

Пока ответа нет, 15а идёт как описано (карточки капитанов — только карточки, без роли и доступа).

---

**Отчёт о выполнении сюда НЕ пишется** — он идёт записью в `WORKLOG.md`.
Здесь меняется только строка «Статус».
