# UI-GUIDELINES — навигация и раскладка LOST

Стандарт, по которому кладутся **новые** разделы и переделываются старые. Правило одно:
**если экран нарушает этот документ — прав документ, а не экран.** Расхождение либо чинится,
либо документ правится осознанно и с абзацем «почему».

Здесь только навигация и раскладка. Цвета, токены и «подушки» pouf — в [`BRENDBOOK.md`](./docs/brand/BRENDBOOK.md)
и `src/components/pouf/pouf.css`. Что где лежит в коде — [`CLAUDE.md`](./CLAUDE.md).

---

## 0. Пять принципов

1. **Экран без данных — не экран.** Страница, у которой единственное содержимое — ссылки на другие
   страницы, допустима **ровно один раз** на маршруте, и только на самом верху (`/`, `/tournaments`,
   `/admin`). Хаб над хабом запрещён.
2. **Соседи всегда достижимы.** Из любого экрана видно и достижимо в один клик всё, что лежит на том
   же уровне. Боковое движение важнее глубины: считать клики бессмысленно, терять соседей — нет
   (NN/g, «The 3-Click Rule Is False»).
3. **Один уровень — одна модель.** У каждого уровня иерархии ровно один вид элемента управления
   (см. §2). Вкладки и плитки за один и тот же выбор в одном месте не соревнуются.
4. **Где я — видно из хрома, а не из памяти.** Контекст (турнир, дивизион, этап) читается из шапки
   и крошек. Кнопка «Назад» контекстом не является.
5. **Хром платит за место.** Каждый ряд навигации должен зарабатывать свои ~50px. Глобальная
   навигация вертикальная (сайдбар) и по высоте не стоит ничего; горизонтальных рядов до первых
   данных — не больше **одного** (строка турнира) плюс заголовок страницы.

---

## 1. Целевая карта (IA)

Навигация проектируется от IA, а не наоборот (NN/g, «IA vs. Navigation»). Вот дерево, к которому идём.

```
/                                лендинг: что за лига + вход в текущий турнир
/tournaments                     список турниров (единственный «список ради списка»)
/tournaments/<slug>              → редирект на дивизион по умолчанию (или обзор, если дивизионов >2)
/tournaments/<slug>/<div>        ТАБЛИЦА дивизиона — данные, не плитки
/tournaments/<slug>/<div>/groups         сетка личных встреч
/tournaments/<slug>/<div>/playoff        сетка плей-офф (full-bleed)
/tournaments/<slug>/<div>/stats          рейтинги
/tournaments/<slug>/roster/{teams,players}   составы турнира
/tournaments/<slug>/tp                   зачёт TP турнира
/tournaments/<slug>/apply                заявка команды
/roster/{teams,players}/<id>     карточки: команда и игрок переживают турнир
/series/<slug>                   встреча
/me, /me/profile, /me/security   кабинет
/rules                           правила
/admin/**                        служебная часть (§5)
```

Что меняется относительно текущего состояния: `/tournaments/<slug>` и `/tournaments/<slug>/<div>`
перестают быть плиточными хабами, `/tp` переезжает под турнир (он и так турнирный).

---

## 2. Четыре уровня навигации

Уровней ровно четыре. Больше не заводим: пятый — признак того, что раздел спроектирован неверно.

| Уровень | Что выбирает | Элемент | Где живёт |
|---|---|---|---|
| **L1 Глобальный** | раздел лиги / инструмент / кабинет | **левый сайдбар**, один на весь продукт | `_components/app-shell.tsx` |
| **L2 Контекст** | какой турнир и какой дивизион | переключатель турнира + сегменты дивизионов | layout турнира |
| **L3 Раздел** | этап внутри контекста: таблица / плей-офф / статистика / ростер / TP | ряд вкладок (`SubNav`), sticky к верху колонки страницы | layout турнира |
| **L4 Разрез** | фильтр внутри страницы: группа A/B, верхняя/нижняя сетка, «все/D1/D2» | `Segmented` / чипы в теле страницы, **состояние в query** | сама страница |

### L1 — сайдбар

**Одна колонка на все страницы продукта — публичные и служебные** (решение 02.09 в `DECISIONS.md`).
Верхней строки сайта нет: она и полный сайдбар из макета Кита не совмещаются — это два бренд-блока
и два входа в кабинет на одном экране. Сверху вниз:

```
[знак] SPIRIT/CTRL                    ‹        ← бренд + свернуть
┌────────────────────────────────┐
│ (АВ)  Ник                    › │             ← профиль-блок: вход в кабинет
│       Капитан · Guzliki        │               (гостю — «Войти»)
└────────────────────────────────┘
[ Поиск по лиге…             🔍 ]             ← поиск: команды, игроки, турниры

ЛИГА          Турниры · Ростер
МОДЕРАЦИЯ     Турниры · Модерация ③ · …       ← секции инструментов, по правам
…
КАБИНЕТ       Профиль · Безопасность · Моя команда
─────────────
Правила лиги · Выйти
```

- Состав режется правами: пункт без права **не рисуется** (витрина, а не защита; сами роуты
  проверяют право у себя). Источник инструментов — один массив `_components/tools.ts`, из него же
  строится плитка-хаб `/admin`.
- Счётчик очереди (модерация) — бейдж на пункте, и в развёрнутом виде, и в рельсе.
- Профиль-блок — **единственный** вход в аккаунт. Второго «Кабинет / Войти» на экране быть не должно.
- Ничего турнирного в L1 нет: турнир — это L2. В секции «Лига» ровно то, что турниру не принадлежит,
  — «Турниры» и «Ростер». Таблица, плей-офф, статистика, ростер сезона и TP — этапы **внутри**
  турнира, то есть L3; их копия в колонке была бы «один уровень — две модели» (принцип 3).
- Три состояния ширины: полная колонка 300px (332 с полем под тень), рельс из значков 76px (108),
  и на мобильном — узкий рельс с подписями в `Sheet` (§6). Выбор развёрнуто/свёрнуто — привычка
  человека, живёт в `localStorage`, а не в адресе.
- Инструменты-заглушки («в разработке») стоят в колонке с пометкой `скоро` и не открывают пустой
  экран без объяснения — компонент `WorkInProgress` остаётся.

#### Исключение — главная: аватар-меню вместо колонки (Э21, 10.09)

На `/` сайдбара нет. Колонка отвечает на вопрос «где я стою и что рядом» — на витрине стоять
негде, активный пункт в ней всегда один и тот же («Главная»), а 300px первого экрана лиги она
съедает под список ссылок. Вместо неё — **единственная в продукте верхняя строка**: знак слева,
разделы (`Турниры · Ростер · Правила`), аватар справа. Собирает её `_components/home-shell.tsx`,
живёт она только у группы маршрутов `(home)`.

- **Профильные функции собраны в аватар.** Профиль, сообщения (со счётчиком непрочитанного),
  настройки, «моя команда», операторская и выход — выпадашка из аватара
  (`_components/avatar-menu.tsx`); гостю на его месте кнопка «Войти». Правило «вход в аккаунт
  один» этим не нарушено: аватар-меню — тот же профиль-блок, показанный там, где нет колонки.
  Заводить рядом второй «Кабинет / Войти» нельзя, как и в сайдбаре.
- **Пункты у обоих видов общие.** И секцию «Кабинет» колонки, и меню аватара собирает один
  `_components/account-nav.ts`. Свой список пунктов для витрины не заводим — на первой же правке
  разъедутся.
- **Верхняя строка не расползается дальше.** Она есть у главной и больше нигде: строка на
  остальных маршрутах вернула бы двухэтажный хром, который убирала Э4b (два бренд-блока и два
  входа в кабинет на одном экране).

### L2 — контекст турнира

Появляется **только внутри `/tournaments/<slug>`** и заменяет собой оба удалённых хаба:

```
LOST Season 2 ▾   [ D1 | D2 ]        Идёт · 28 команд
```

- Переключатель турнира (`▾`) — меню со списком турниров; из него же виден статус.
- Дивизионы — сегменты, не отдельные страницы. Переключение дивизиона **сохраняет текущий этап**:
  из таблицы D1 переходим в таблицу D2, а не на хаб D2.
- Акцент секции = цвет дивизиона (`--accent` на обёртке, как сейчас в `[div]/layout.tsx`) —
  это остаётся, приём хороший.

### L3 — вкладки раздела

`Таблица · Группы · Плей-офф · Статистика · Ростер · TP · О турнире`

- Ряд одинаков на всех страницах турнира — он и есть «соседи в один клик» из принципа 2.
- «Группы» вернулись отдельной вкладкой на Э5 (до этого `/groups` был редиректом на корень
  дивизиона): «Таблица» — турнирная таблица группы целиком, «Группы» — кросс-сетка личных
  встреч. Рядом на одном экране они делили ширину пополам и обе теряли колонки.
- Активна ровно одна вкладка, по самому точному совпадению префикса (логика `matchScore`
  в `sub-nav.tsx` — её сохраняем).
- Вкладок в ряду **не больше семи**. Не влезает — значит, часть пунктов на самом деле L4.

### L4 — разрезы

- Живут в теле страницы, у данных, к которым относятся, а не в шапке.
- **Состояние — в query** (`?cut=group-a`), чтобы ссылка на разрез была отправляемой.
- Два ряда фильтров за один и тот же выбор — ошибка. Так было на ростере команд (`D1/D2/Все` в
  теле страницы и `D1 · D2` в строке контекста); с Э4 сегменты дивизионов рисуются только там, где
  дивизион есть в адресе, и на ростере остаётся один ряд — его собственный.

---

## 3. Крошки вместо «Назад»

`BackButton` / `BackBar` из продукта убираются. Причины: кнопка не сообщает, где ты, дублирует
браузер и на глубоких экранах ведёт не туда, куда ждёшь.

Взамен — крошки **над заголовком** там, где путь длиннее двух шагов, и **только там**:

```
Турниры / LOST Season 2 / LOST D1        ← крошки
ГРУППОВАЯ СТАДИЯ                          ← eyebrow (роль страницы)
Групповая стадия                          ← H1
```

- На страницах, где L2+L3 уже показывают весь путь (таблица дивизиона), крошки не нужны — путь
  и так на экране.
- Крошки кликабельны все, кроме последней. Последняя = текущая страница, дублирует H1 — её опускаем.
- Карточки (`/roster/teams/<id>`, `/series/<slug>`) крошки **обязаны** иметь: у них нет L2/L3.

---

## 4. Скелет страницы

Каждая страница продукта собирается из одних и тех же слоёв, сверху вниз:

```
сайдбар (L1)  │  L2+L3 строка турнира (sticky, top-0)   ← если раздел её имеет
во всю        │  main.mx-auto.SITE_MAX_W.px-4.py-8
высоту окна   │    ├─ крошки            (если нужны, §3)
              │    ├─ SectionHeader     eyebrow + H1 + aside (счётчик/действие справа)
              │    ├─ L4 разрезы        (если есть)
              │    └─ контент
```

Ряд хрома над данными теперь ровно один (строка турнира) или ноль: глобальный уровень ушёл влево
и по вертикали ничего не стоит. Принцип 5 («хром платит за место») от этого не смягчается —
второго горизонтального ряда в продукте нет и заводить его нельзя. Главная — задокументированное
исключение (§2): у неё нет колонки, и вместо неё одна верхняя строка, второго ряда там тоже нет.

Жёсткие правила:

- **Ширина.** Колонка страницы всегда называется **токеном**, литералов `max-w-*` на уровне `<main>`
  быть не должно — по имени видно, к какому типу страница себя относит:

  | Токен | Ширина | Для чего |
  |---|---|---|
  | `SITE_MAX_W` | 96rem | витрина: таблицы, сетки, списки, карточки |
  | `READ_MAX_W` | 72rem | страница одной сущности, которую читают сверху вниз (встреча) |
  | `FORM_MAX_W` | 48rem | анкеты, мастера, настройки, регламент — поле во всю ширину экрана неудобно |
  | `AUTH_MAX_W` | 420px | одинокая карточка входа по центру пустой страницы (`/login/tg`, `/me` до входа) |

  Все четыре — в `components/pouf/blocks.tsx`. Ширина карточки входа названа отдельно, а не сведена
  к `FORM_MAX_W`: она взята из макета Кита «Вход», и на 48rem форма из двух полей растекается.
  Внутри страницы узкая мера у абзаца, картинки или блока — нормально: правило про колонку
  страницы, а не про каждый элемент.
  Исключение из ширин ровно одно, документированное: сетка плей-офф — full-bleed.
- **Отступы.** Только шкала pouf (`--s1..--s6` = 6/8/12/16/20/24px) и её тейлвиндовые эквиваленты.
  Между блоками страницы — `--s6`, внутри блока — `--s4`, внутри строки — `--s2`.
  Магических `mt-7`, `pb-[13px]` быть не должно.
- **Заголовок один.** `H1` на странице ровно один, его рисует `SectionHeader`. Блоки внутри — `H2`
  (`Heading level={2}`). Своими руками `<h1 className="text-[28px]...">` не пишем — это уже
  расползлось по трём файлам, дальше не тиражируем.
- **Действия страницы** — в `aside` у `SectionHeader`, справа от заголовка. Не под контентом,
  не в двух местах сразу.
- **Пустое состояние** обязательно: что это за экран, почему пусто, что сделать. «Нет данных» —
  не пустое состояние.
- **404 и ошибка — тоже экраны продукта**, а не системный вывод фреймворка. Оба собираются
  `_components/stub-page.tsx`: что случилось, одна фраза почему, и ссылки на соседей верхнего
  уровня (уйти «назад» с несуществующего адреса некуда). Текст ошибки посетителю не показываем —
  только `digest`, по которому запись ищется в логе.

### Плотность

Продукт — про таблицы и цифры, а не про воздух. На экране данных:

- карточка-«подушка» — для сущностей (команда, турнир, встреча), **не** для строк таблицы;
- таблица — таблица, с `tabular-nums`, липкой шапкой и горизонтальным скроллом **внутри**
  своего контейнера (`overflow-x-auto`), а не всей страницей;
- ряд из 4+ плиток-показателей (`StatTile`) уместен один раз на странице, сверху.

---

## 5. Служебная часть (/admin)

Модель та же, что у продукта, и это осознанно: с Э4b колонка одна на обе группы маршрутов, а
инструменты оператора — просто её секции, срезанные правами. Отдельного «операторского хрома»
больше нет — граница проходит по правам, а не по виду экрана.

```
┌──────────────┬───────────────────────────────┐
│ [знак] SPIRIT│                               │
│ (АВ) Ник   › │                               │
│ Поиск…       │   рабочая область             │
│ ЛИГА         │   (SITE_MAX_W внутри)         │
│  Турниры     │                               │
│  Ростер      │                               │
│ МОДЕРАЦИЯ    │                               │
│  Модерация ③ │                               │
│ …            │                               │
│ КАБИНЕТ      │                               │
└──────────────┴───────────────────────────────┘
```

- Инструменты приходят из **одного массива** `_components/tools.ts` — из него же строится
  плитка-хаб `/admin`. Новый инструмент добавляется только туда.
- `/admin` остаётся плиточным хабом как «домашняя» — это допустимый единственный хаб по принципу 1.
- `BackBar` в админке исчезает: с сайдбаром возвращаться некуда — все инструменты видны всегда.
  В ядре операций (турниры, серии, модерация, дубли, персонал, ростер-редакторы) ссылок
  «← Все турниры» / «← к команде» больше нет — их заменили крошки (Э9). Остались только в
  студии (Э10), драфтах (Э11) и анкетах; уходят вместе с переездом этих экранов.
- Шапку служебного экрана рисует `(admin)/_components/admin-header.tsx` (крошки → eyebrow → H1 →
  одна поясняющая фраза), блок внутри — `(admin)/_components/panel.tsx`. Своих `<h1>` и своих
  рамок служебные экраны не верстают.
- Служебные экраны отличаются от публичных только правами и `robots: noindex` в метаданных
  группы `(admin)`. Ни своей навигации, ни своей палитры у них нет.

## 6. Мобильный

Больше половины аудитории киберспорта смотрит с телефона — мобильная раскладка не «потом».

- **L1 не превращается в шапку с кнопкой меню.** Сайдбар сжимается в **рельс** значков (60px,
  тач-цель внутри — 44px) и остаётся слева; подписи открываются в `Sheet` по кнопке-бренду
  наверху рельса. Горизонтальная строка была бы тем самым рядом хрома, который этим этапом убран.
  Всплывающих подписей у значков на телефоне нет (наведения нет) — за них отвечает `Sheet`.
- L2+L3 строка турнира остаётся горизонтальным скроллом (это нормально), но активная вкладка при
  загрузке **проматывается в видимую область**.
- L2-сегменты дивизионов остаются на виду: это самый частый переключатель.
- Таблицы: липкая первая колонка (команда/игрок), остальное скроллится внутри контейнера.
  Считать ширину надо от **315px**, а не от 375: 60px забирает рельс.
- Тач-цель — минимум **44×44px**. Вкладки (`py-[9px]`, ~35px) добираются до 44px паддингом
  (`TOUCH_TARGET` в `nav-scroll.ts`).

## 7. Чем строим (не изобретать заново)

| Нужно | Берём |
|---|---|
| глобальная навигация | `app/_components/app-shell.tsx` (собирает) + `app-sidebar.tsx` (рисует) |
| реестр инструментов оператора | `app/_components/tools.ts` — один массив на сайдбар и хаб `/admin` |
| вкладки раздела | `app/_components/sub-nav.tsx` (`SubNav`) |
| поиск по лиге | `app/_components/league-search.tsx` + `lib/search.ts` + `/api/search` |
| ширина колонки | `SITE_MAX_W` из `components/pouf/blocks.tsx` |
| заголовок секции | `SectionHeader`, `Eyebrow` (там же) |
| карточка-сущность | `components/pouf/surface.tsx` (`Card`, `RowCard`) |
| заголовки, текст | `components/pouf/text.tsx` (`Heading`, `Text`) |
| переключатель разреза | `components/pouf/Segmented.tsx` |
| статусы, бейджи | `components/pouf/status.tsx`, `media.tsx` |
| раскладка, отступы | `components/pouf/layout.tsx` (`Stack`/`Row`) — не `display:flex` руками |
| модалка, мобильное меню | `components/pouf/sheet.tsx` |
| показатель числом | `StatTile` из `ui.tsx` |
| состояние загрузки | `components/pouf/skeleton.tsx` (`Skeleton`, `SkeletonList`, `SkeletonHeader`) в `loading.tsx` раздела |
| «страницы нет» и «страница упала» | `app/_components/stub-page.tsx` |

Нужного компонента нет → он заводится в `pouf/` или `_components/`, а не собирается на месте из
классов. Второй экран, собравший то же самое руками, — это уже расхождение стиля.

---

## 8. Чек-лист нового раздела

Перед тем как считать раздел готовым:

- [ ] Раздел встал в §1 (карта IA) — маршрут дописан в карту, а не выдуман по месту.
- [ ] Понятно, какой это уровень (L1–L4), и взят элемент управления **этого** уровня.
- [ ] Соседи по уровню достижимы в один клик с экрана раздела.
- [ ] Нет нового хаба-из-плиток (принцип 1).
- [ ] Ширина — `SITE_MAX_W`, локальных `max-w-*` нет.
- [ ] Ровно один `H1`, собранный `SectionHeader`.
- [ ] Крошки есть, если путь длиннее двух шагов и L2/L3 его не показывают.
- [ ] Разрезы и фильтры — в query, ссылка на состояние отправляема.
- [ ] Есть пустое состояние и состояние загрузки.
- [ ] Проверено на 390px и 1440px; таблица скроллится внутри себя, страница по горизонтали — нет.
- [ ] Отступы со шкалы `--s*`, магических чисел нет.
- [ ] `npx tsc --noEmit` и `npm run lint` чистые.

---

## 9. Антипаттерны — то, что уже есть в коде

Список того, что чинится при переезде. Новый код так писать нельзя.

| Антипаттерн | Где | Чем заменяется | Статус |
|---|---|---|---|
| Цепочка хабов `/` → `/tournaments` → `/<slug>` → `/<div>` | `tournaments/[slug]/page.tsx`, `[div]/page.tsx` | L2-контекст + L3-вкладки, дивизион открывает таблицу | вычищено |
| Соседние этапы недостижимы («плитки, не ряд вкладок») | `[div]/layout.tsx` | `SubNav` уровня L3 | вычищено (`tournament-bar.tsx`) |
| Три модели навигации в одной секции: `SubNav` / плитки / ничего | roster · division · tp layouts | одна модель L3 на весь турнир | вычищено |
| Два ряда фильтров за один выбор | ростер команд | один ряд | вычищено на Э4 |
| «Назад» как способ ориентации | `back-button.tsx`, все layout'ы | крошки (§3) | в продукте вычищено (Э4); в ядре операций админки — на Э9; остались студия (Э10), драфты (Э11), анкеты |
| Мобильный хром — шапка с кнопкой меню | верхняя строка до Э4b | рельс значков + `Sheet` (§6) | вычищено на Э4b |
| ~200px хрома до первых данных | ростер | один горизонтальный ряд + заголовок, остальное — влево в сайдбар | вычищено (Э4b) |
| Два хрома сразу: верхняя строка сайта + сайдбар операторской | `site-nav.tsx` + `(admin)/layout.tsx` | один сайдбар на обе группы маршрутов (§2) | вычищено на Э4b |
| Найти команду или игрока можно только глазами по ростеру | поиска не было вовсе | поле поиска в шапке сайдбара (`league-search.tsx`) | закрыто на Э4b |
| Ширина колонки литералом (`max-w-3xl`, `max-w-[96rem]`) | админка, встреча | токен `SITE_MAX_W` / `READ_MAX_W` / `FORM_MAX_W` / `AUTH_MAX_W` | вычищено на Э4; на Э9 ростер-редакторы получили `<main>` и токен — до этого рисовались голым `<div>` вовсе без колонки |
| `H1` собран классами в трёх местах | `ui.tsx`, `hub-tiles.tsx` ×2 | один `SectionHeader` | вычищено на Э3; в ядре операций — на Э9 (`AdminHeader`); остались студия, драфты и настроечные экраны (`/admin/bot`, `/admin/quizzes`, `/admin/stats`, `/admin/vision`) — Э10/Э11 |
| Хардкод отступов `pb-[calc(0.75rem+5px)]` | `StatTile` | шкала `--s*` | вычищено на Э3 |
| Одно слово — два разных экрана: «Ростер» в колонке (пул лиги) и «Ростер» во вкладках турнира; «Турниры» в «Лиге» и «Турниры» в «Модерации» | сайдбар · `tournament-bar.tsx` · `tools.ts` | разные имена по тому, что открывается: «Составы» у турнира, «Управление турнирами» в служебной части | вычищено 08.09 |
| Пункт обещает не то, что открывает: «Профиль» ведёт на `/me` с заголовком «Личный кабинет» | `app-shell.tsx` | подпись по цели: «Профиль» у игрока с карточкой, «Кабинет» у аккаунта без профиля | вычищено 08.09 |
| Знак бренда работает кнопкой меню (на телефоне — единственный вход в подписи, но обещает «на главную») | `app-sidebar.tsx`, `MobileRail` | значок `menu`; знак переехал внутрь листа | вычищено 08.09 |
| Липкое начало ряда съедает активную вкладку: имя турнира уезжает за левый край при промотке | `tournament-bar.tsx` | переключатель `sticky left-0`, промотка через `scroll-padding-left` по замеру | вычищено 08.09 |
| Своих 404 и «страница упала» нет вовсе — битый адрес отдаёт системный экран Next (чёрный фон, английский текст) поверх светлой колонки | не было | `_components/stub-page.tsx` → `not-found.tsx` (корень и обе группы) + `error.tsx` | вычищено 08.09 |
| Подвал на две трети раздаёт заглушки «скоро» и сам же — единственный вход в эти адреса | `footer.tsx` + девять `ComingSoon` | в подвале только живое; у кого есть живой адрес — редирект (`/playoffs`, `/apply`, `/tournaments/archive`) | вычищено 08.09 |
| Три ряда фильтров подряд над данными | `/roster` (вид · пул/архив · поиск с выбором турнира) | разрезы одного уровня — в одну строку | вычищено 08.09 |
| Состояния загрузки нет ни у одного раздела: тяжёлая витрина висит белым | не было | `loading.tsx` на витринах + `pouf/skeleton.tsx` | закрыто 08.09 на четырёх витринах |

Плитки-хаб (`hub-tiles.tsx`) из этого списка выбыли: единственный оставшийся их пользователь —
домашняя `/admin`, а её §5 разрешает прямо.

**Два хрома сразу** (закрыто на Э4b). До него §2 описывал верхнюю строку на весь сайт, а макет Кита
«Сайдбар» — колонку с брендом, профилем и поиском; перенести сайдбар по макету и оставить строку
значило бы два бренд-блока и два входа в кабинет на экране. Стандарт правится под макет: он
нарисован целиком и проверен глазами. Разбор — в `DECISIONS.md`, решение от 02.09.

**Два ряда дивизионов** (закрыто на Э4). Сегменты `D1 · D2` в строке контекста рисуются только на
разделах, где дивизион есть в адресе: таблица, плей-офф, статистика. На общих разделах турнира
(ростер, TP, «О турнире») они не значили ничего — активного среди них нет, а нажатие уводило с
текущего этапа в таблицу; на ростере рядом при этом стоял его собственный фильтр `D1 · D2 · Все`.
Осталась одна пара: та, что относится к содержимому страницы.

---

## 10. Источники

- [NN/g — The 3-Click Rule for Navigation Is False](https://www.nngroup.com/articles/3-click-rule/) —
  решает не глубина, а информационный запах, крошки, подменю и хабы-остановки.
- [NN/g — The Difference Between IA and Navigation](https://www.nngroup.com/articles/ia-vs-navigation/) —
  сначала инвентаризация и таксономия, потом выбор рисунка навигации.
- [NN/g — Intranet IA, Navigation and Menus](https://www.nngroup.com/reports/intranet-information-architecture-navigation/) —
  служебные разделы: устойчивая левая колонка вместо блужданий через хаб.
- [NN/g — Basic Patterns for Mobile Navigation](https://www.nngroup.com/articles/mobile-navigation-patterns/) —
  что переживает сжатие до 390px.
- [Nested Tab UI — guidelines](https://www.designmonks.co/blog/nested-tab-ui) — вложенные вкладки
  работают, пока уровней не больше двух; отсюда потолок «L2 + L3, не глубже».
- [Admin Dashboard UI/UX Best Practices](https://medium.com/@CarlosSmith24/admin-dashboard-ui-ux-best-practices-for-2025-8bdc6090c57d)
  и [примеры сайдбаров](https://www.navbar.gallery/blog/best-side-bar-navigation-menu-design-examples) —
  почему у операторской части сайдбар, а не плитки.
- Отраслевые ориентиры по устройству турнирной страницы (вкладки этапов внутри одного турнира,
  переключатель турнира в шапке): Liquipedia, FACEIT, start.gg, HLTV.
