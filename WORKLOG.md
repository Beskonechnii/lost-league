# WORKLOG — журнал работ

Одна запись на этап. Новейшее сверху, формат **≤5 строк**: дата · что · файлы · дальше.
Новый чат читает только верхнюю запись — `sed -n '1,30p' WORKLOG.md`.
Здесь держим **последние 10 записей**; когда набирается больше, нижние уезжают в
`docs/archive/WORKLOG-2026-09.md` (там же полные, неурезанные версии всех записей ниже)
и `docs/archive/WORKLOG-2026.md` (лето–август).
Отчёт о выполнении ТЗ пишется сюда, а не в файл ТЗ.

---

## 2026-09-26 — сцена Mix Cup: организаторы в шапке

Иерархия местом: LEAGUE OF SPIRITS × ECLIPSE неподвижно в правом верхнем углу (72px), в бегущей строке только
партнёры (56px). `partners.ts`: `OVERLAY_ORGANIZERS`/`OVERLAY_PARTNERS`, поле `glow` — оранжевое свечение
делает чёрный круг Eclipse затмением. Проверено: сессия 42, 1920×1080, tsc/lint.
Дальше: qa ТЗ 44; защитить эталон eclipse от удаления; у aprel MMR в таблице — ссылка.

## 2026-09-26 — сцена Mix Cup: бегущая строка партнёров, пул из гугл-таблицы

Знаки на сцене без скругления, SPIRIT/CTRL в левом верхнем углу, партнёры и организаторы — бегущей
строкой без подписей (`overlay-mixcup.tsx`, `partners.ts`: `OVERLAY_PLATFORM`/`OVERLAY_MARKS`). Эталон `eclipse` был
удалён кнопкой в `/admin/mixcup` → новые Mix Cup пустые; `import-mixcup-eclipse.ts` теперь читает гугл-таблицу анкет
(`--also=` доливает в уже заведённые), нормализует tgId вида `2.1E8`; пул 40 в eclipse(61)/58/59/60. Проверено: сессия 42, 1920×1080.
Дальше: qa ТЗ 44; защитить эталон eclipse от удаления; у aprel MMR в таблице — ссылка; знак Eclipse чёрный на тёмном.

## 2026-09-26 — stage: ТЗ 44 — эфирная сцена Mix Cup 1920×1080

Оверлей драфта разведён по `tournament.kind`: у Mix Cup своя сцена (8 команд сеткой 4×2 без фото,
пустые слоты пунктиром, колонка свободных игроков в две колонки по ролям, нижняя полоса знаков
списком из `partners.ts`), у UNDERBEER вид прежний; плашка «Организатор» снята, MMR на Mix Cup
виден мимо флага лиги. Опрос сессии вынесен в общий хук. Проверено `tsc`/`lint`/`build` (чисто,
прежние замечания в чужих файлах) и глазами на 1920×1080: стенд-драфт турнира eclipse (8×5 занято
+ пул 42 — без скролла), состояние до старта, пик уходит из пула за ~2с, UNDERBEER не изменился;
стенд из `dev.db` убран, файл вернулся к состоянию HEAD. Скриншот:
`web/public/uploads/generated/tz44-overlay-mixcup.png`.
Файлы: `(bare)/overlay/draft/[id]/{page.tsx,_components/{overlay-mixcup.tsx,use-live-draft.ts,overlay-live.tsx}}`, `src/lib/partners.ts`.
Дальше: `qa` — приёмка 44 по acceptance criteria. Открыто: при пуле больше ~52 человек (сессия без
выбранных участников — весь ростер) хвост колонки обрезается молча, счётчик в шапке честный.

## 2026-09-26 — stage: скрипт eclipse-импорта стал синком, актуализирован список

Новая ссылка на таблицу (та же вкладка «UNDERBEER v2.1», другой документ) — 40 актуальных
участников вместо 42. Скрипт `import-mixcup-eclipse.ts` доработан до полноценного синка: снимает
с турнира тех, кого в таблице больше нет (сравнение по `UserAccount.tgId`, не по нику), и
обновляет MMR/роли у СВОИХ теневых профилей прошлых прогонов (`verified:false` +
`sourceTournamentId = eclipse`) — реальных игроков лиги по-прежнему не трогает. Прогнан: −3
(`nohara`, `sexmachine`, `TwilightNest`), +1 (`q w e`), 17 теневых обновлено. Итог — 40 записей.
Файлы: `web/scripts/import-mixcup-eclipse.ts`, `web/prisma/dev.db`.

## 2026-09-25 — stage: кнопка «Убрать выделение» на фазе «Участники»

`RosterSelect` (фаза выбора участников драфта, общая для UNDERBEER и Mix Cup) — кнопка рядом с
«Далее к командам», снимает отметку сразу со всех и чистит капитанов/пики/локи команд (та же
зачистка, что у поштучного снятия). Новая `clearParticipants` в `src/lib/draft.ts`. Проверено
живьём: ad hoc-драфт в `/underbeer`, отметил → снял → 0, кнопка задизейблилась. Тестовый драфт
за собой удалил из `dev.db`.
Файлы: `web/src/lib/draft.ts`, `.../roster-select.tsx`, `.../draft-board.tsx`, `web/prisma/dev.db`.

## 2026-09-25 — stage: снесён стенд лобби из пула Mix Cup

Стас увидел в списке ролей драфта «Стенд A · Софт-саппорт» / «Стенд B · Софт-саппорт» — это 12
тестовых ботов «стенда лобби» (ТЗ 42ж, `src/lib/lobby-stand.ts`), не Mix Cup: `draftPool()` не
фильтрует по турниру и подмешивает вообще всех игроков лиги. `LOBBY_STAND` сейчас выключен (нет в
`.env`) — фича не тестируется, снёс штатной `removeStand()` (12 аккаунтов + 12 профилей).
Файлы: `web/prisma/dev.db`.

## 2026-09-25 — stage: наследование пула Eclipse + чистка тестовых данных

`POST /api/tournaments` (создание mixcup) при `kind: "mixcup"` копирует `TournamentRegistration`
турнира-эталона `eclipse` в новый — оператору не нужно снова звать скрипт импорта на каждое
событие (все Mix Cup — один бренд-партнёр Eclipse, `src/lib/partners.ts`). Логика проверена
прогоном на временном турнире (42 записи скопировались, турнир удалён после проверки).
Почищены тестовые остатки из dev.db: 4 пустых Mix Cup-заглушки (id 48/50/51/53, без регистраций)
и 4 тестовых лобби (id 4/11/15/16, без сообщений/участников) — созданы кликами по кнопкам
«Новый…» во время ручного тестирования. Сезонные тестовые турниры `s4 "test"` и `s8 "TESET TSET"`
не трогали — вне рамки инструмента для драфтов.
Файлы: `web/src/app/api/tournaments/route.ts`, `web/prisma/dev.db`.

## 2026-09-25 — stage: ТЗ 43 сделано упрощённо (срочно, под сборку)

Скрипт `web/scripts/import-mixcup-eclipse.ts` (не `import-mixcup-signups.ts` из рамки pm — имя другое). Сведение: `UserAccount.tgId` → `Player.telegram` → иначе новый профиль (по нику не сводим). Прогон на 42 записях: 26 сведено с уже существующими verified-игроками лиги (телеграм совпал), 16 новых теневых профилей (`verified:false`, `sourceTournamentId`), 42 записи в `TournamentRegistration`. Турнир `eclipse` («Mix Cup by Eclipse», kind: mixcup, status: registration) заведён самим скриптом — упрощение против рамки ТЗ (там предполагался ручной мастер). Поле «капитан» из анкеты никуда не легло — в модели такого нет, решили не делать под срочность. 1 битый MMR (`velesevich99`) — `null`, в отчёте скрипта.
Файлы: `web/scripts/import-mixcup-eclipse.ts` (новый), `web/prisma/dev.db`, `docs/tasks/43-import-uchastnikov-mix-cup.md` (статус done).
Дальше: если понадобится колонка «капитан» в консоли турнира — отдельный этап (правка схемы).

## 2026-09-25 — pm: заведено ТЗ 43 «Импорт участников Mix Cup из гугл-таблицы»

Вводная Стаса: залить в Mix Cup «Eclipse» 42 человека из формы регистрации (лист «UNDERBEER v2.1»). Вердикт — **не в MVP** (весь Mix Cup later с 22.09). Рамка: скрипт `scripts/import-mixcup-signups.ts` (образец разбора — `sheet-to-roster.ts` + `xlsx.ts:loadWorkbook`), сведение по `UserAccount.tgId` → `Player.telegram`, по нику не сводим; недостающим — аккаунт `source: "telegram"` и профиль `verified: false` + `sourceTournamentId` (исключение 22.09.2026), запись upsert'ом с `desiredRoles`; лимит и `joinOpen` не проверяем, кривые MMR идут с `null` в блок «требует внимания». UI массового добавления и уведомления в телеграм — вне Scope.
Файлы: `docs/tasks/43-import-uchastnikov-mix-cup.md` (новый), `MVP.md`, `BACKLOG.md`.
Дальше: два вопроса Стасу — признак «хочет капитанить» (хранить в модели или только в отчёте) и параметры турнира Eclipse (название, slug, лимит мест); до ответов ТЗ в `draft`. Очередь работ не меняется: следующим по-прежнему `qa` — приёмка 42а–42ж, затем `stage` 42е.

---

## 2026-09-25 — ТЗ 42ж: удаление лобби и «Заполнить стенд» с ботами

Комнату удаляет `tools` или создатель — `deleteLobby` + `DELETE /api/lobby/<id>` (не намерение: снимка после него нет), кнопка внизу «Двери комнаты» за `AlertDialog` Кита; каскад схемы уносит состав и переписку, будильники часов и ботов гасятся руками, открытым вкладкам уходит новое событие живого канала `lobby-gone` → `/lobby?gone=1` с «Комната удалена». Стенд: сид двенадцати аккаунтов переехал из скрипта в `lib/lobby-stand.ts` (скрипт стал обёрткой; `server-only` там нельзя — файл тянет tsx), кнопка «Заполнить стенд» (намерение `fill`) сажает нажавшего капитаном стороны A, четырёх ботов к нему и пятёрку с тренером на сторону B, заводя аккаунты сама, если их в базе нет. Боты (`lib/lobby-bots.ts`) шлют ТЕ ЖЕ намерения через тот же `applyIntent` без права `tools`: «Готов», блок после монетки, ход случайным доступным, назначение героя себе — одно решение на вызов, планировщик-будильник 1–2 с в `lobby.ts` просыпается на каждой рассылке снимка. Замок двойной: `LOBBY_STAND=1` (в отличие от `DEV_AS` действует и в прод-сборке — тест идёт через туннель) плюс право `tools`; сторона с живым капитаном ботом не управляется.
Файлы: `lib/{lobby.ts,lobby-bots.ts,lobby-stand.ts,chat-events.ts}`, `api/lobby/[id]/route.ts`, `(public)/lobby/{page.tsx,[id]/page.tsx,_components/room.tsx}`, `scripts/seed-lobby-stand.ts`, `.env.example`, `docs/{MAP,ARCHITECTURE,DEPLOY-MANUAL}.md`.
Проверено: `tsc`/`lint`/`build` чисто (прежний warning `pouf/media.tsx`). Dev 3000, временные комнаты 12–14 (снесены, в базе снова только 4, 5, 11): одним аккаунтом Bo3 целиком — заполнить → бот-капитан B сам «Готов» → монетка → мой блок, второй взял бот → драфт (бот отходил свои восемь ходов сам, без таймера) → девять героев боты разобрали сами, десятый выбрал я → `next` → карта 2 → карта 3 → `done`, в нём pick/assign/next отказаны текстом. Повторное «Заполнить стенд» — no-op (11 участников, 5+6). Права: участник без `tools` получает «Комнату удаляет её создатель или админ лиги» на DELETE и «Стенд заполняет админ лиги» на `fill`, кнопок у него нет. Удаление кликом из диалога: вкладка нажавшего и ВТОРАЯ открытая вкладка ушли на `/lobby?gone=1`, строки `LobbyMember`/`LobbyMessage` комнаты исчезли. Стенд сносил скриптом (`--remove`) и заводил заново кнопкой — 12 аккаунтов создались сами, **id сменились на 217–228** (адреса `/dev/as/<id>` печатает скрипт). Прод-контейнер `serve` (3001, `LOBBY_STAND=1` через временный `docker-compose.override.yml`, туннель не поднимал; контейнер погашен, override удалён): `/dev/as/1` → 404, кнопка на месте, Bo1 пройден целиком ботами и комната удалена через диалог. Ширины: 390 — кнопка 143px, подпись переносится, `scrollWidth` 390; 1280 и 1440 — без переполнения.
Дальше: `qa` — приёмка 42ж (и 42а–42д, если ещё не принято), затем `stage` 42е (кожа). Открыто: в контейнере `DATABASE_URL` берётся из `.env` (`file:./prisma/dev.db`) и база тома `/app/data/dev.db` не используется — расходится с `DEPLOY-MANUAL.md` §3/§9, к `ship`/`qa` как отдельный пункт; `prisma/dev.db` изменена (временные комнаты заведены и снесены, у стенда новые id).

---

## 2026-09-25 — ТЗ 42д: свой герой после драфта, `done`, переключатель карт; webm с CDN Valve

Карта закрывается не 16-м ходом, а десятью назначениями: у `GameDraft` появилось необязательное поле `assigns: {heroId, memberId, byAdmin?}` (`FEARLESS_VERSION` 4, версия 3 читается и нормализуется на чтении — идущие драфты не роняем), герой занимается первым нажавшим (`applyAssign` + `commitAssign` тем же сравнением-и-записью по payload, что и ход), свой выбор переезжает нажатием на другую плитку, чужое назначение переставляет админ комнаты (`byAdmin` → бейдж «переставил админ»). Заведено намерение `assign`; `next` до десяти отказан текстом, последняя карта серии → `Lobby.status = "done"` (`closeIfAssigned`) — в нём закрыты ходы, назначения и чат (`lobby-chat.ts`), чтение остаётся. Панель стадии — новый `lobby/_components/assign.tsx` на `DraftHeroButton big` (у атома появилось состояние `taken`: занятый не гасится, на нём читают ник); центр борда на стадии назначения включается флагом `data-after` — иначе на узком он спрятан вместе с пулом. Переключатель карт — `PillTrack`/`PillButton` над бордом, только сыгранные и текущая, состояние в `?game=N` через `history.replaceState`; прошлая карта — тот же борд read-only плюс `DataTable` «Сторона · Игрок · Герой». Два решения Стаса 25.09: **webm берутся с CDN Valve** (`draft-heroes.ts` отдаёт `cdn.cloudflare.steamstatic.com/.../<slug>.webm` всем героям, манифест с диска убран; `onError` видео гасит попытку и оставляет PNG; CSP приложения — только `frame-ancestors`, media не режет) и **статусы сторон до драфта — «ГОТОВ» / «ЖДЁМ»** на доске сбора и в строке статусов борда, причина отдельной строкой ниже.
Файлы: `lib/{fearless,lobby,lobby-turn,lobby-chat,draft-heroes}.ts`, `api/lobby/[id]/route.ts`, `(public)/lobby/[id]/page.tsx`, `(public)/lobby/_components/{assign.tsx,room.tsx,draft-board.tsx,gather.tsx}`, `components/pouf/{draft.tsx,pouf.css}`, `(admin)/admin/fearless-draft/[id]/page.tsx` (тот же терпимый разбор версии).
Проверено: `tsc`/`lint`/`build` чисто (прежний warning `pouf/media.tsx`). Стенд 42а, временные комнаты 9 и 10 (снесены, в базе снова только комнаты Стаса 4 и 5): Bo3 пройден целиком — готова одна сторона → драфт не стартует, обе → монетка; на карте 1 спор за героя («Этого героя уже взяли» второму), чужой пик отказан, смена выбора переносит одно назначение, не-админ чужого не переставляет, админ переставляет с `byAdmin`, `next` до десяти отказан; 10 назначений → `next` → карта 2 → карта 3 → `done`, в нём assign/pick/next и чат отказаны, GET участнику 200. В браузере: панель на 390 — список «плитка 96px + ник справа», на 1440 — сетка плиток 145.5px с бейджем ника поверх, 1280 — колонки 176/841/176, горизонтального переполнения нет ни на одной ширине; клик по плитке даёт `chosen` и «Вы», меню админа «Отдать <ник>» переставляет и рисует «переставил админ»; `?game=1` открывает прошлую карту с таблицей назначений и баннером «Идёт карта 3 — вернуться», текущая остаётся рабочей; «ГОТОВ»/«ЖДЁМ» приезжают живым каналом обеим сторонам; ОБС-вид по ключу рисует борд как прежде. Ролик с CDN: `viper.webm` доехал (`readyState` 4, играет), событие `error` убирает видео и оставляет PNG.
Дальше: `qa` — приёмка 42д (и 42а–42г, если ещё не принято), затем `stage` 42е (кожа). Открыто: в переключателе нет знака результата `W`/`L` из DESIGN-6 — лобби не знает, кто выиграл карту (запись в `Series` вне Scope); `Capacity` при 10/10 рисует чип «Мест нет», для счётчика выбравших формулировка чужая — это к `design`; `prisma/dev.db` изменена (временные комнаты заведены и снесены).

## 2026-09-25 — ТЗ 42г: экран драфта по WSS — слоты 1–16, один таймер, пул с webm

Борд лобби собран из НОВЫХ атомов Кита (`pouf/draft.tsx`: `DraftSlot` · `DraftRail` · `DraftHeroButton` · `TurnClock` · `CaptainPlate` рядом с `CoinFlip`) и композиции `lobby/_components/draft-board.tsx`; `FearlessRun` оператора из лобби и из ОБС-вида убран совсем — борд оператора остался как был (Scope). Раскладка — одна сетка `pouf-draft` (секция DRAFT в `pouf.css`): ширины токенами `--draft-rail`/`--draft-tile` (240/216/176/148 и плитка 92/80/68/56), ≥1024 три колонки «сторона · пул · сторона» + подвал, ниже — стек, липкая строка хода `top:64` и пул в `Sheet` по кнопке, видной только капитану на своём ходу (подтверждение хода уехало в ту же шторку — в центре борда оно на телефоне невидимо). Таймер ОДИН и переезжает к стороне хода анимацией `margin-left` (`justify-self` не анимируется). Слот: пустой · наведён · нажат · пульс обводки текущего хода · появление героя `key={heroId}` + вспышка · бан (ч/б + диагональ + «Бан») · последний пик; в слотах только PNG. В `pouf.css` заведён глобальный блок `prefers-reduced-motion: reduce` (его не было, хотя комментарий Кита утверждал обратное); монетка держит свои 300 мс через `!important`. Скрипт `scripts/sync-hero-videos.ts` (идемпотентный, `--check`/`--force`/`--only`) качает рендеры Valve в `public/assets/hero-renders`, `lib/draft-heroes.ts` отдаёт `video` только тем, кто лежит на диске. Монетка добавлена в ОБС-вид (`LobbyBoard.coin`).
Файлы: `components/pouf/{draft.tsx,pouf.css}`, `(public)/lobby/_components/{draft-board.tsx,room.tsx}`, `(public)/lobby/[id]/{page.tsx,loading.tsx}`, `(bare)/overlay/lobby/[key]/{page.tsx,_components/obs-board.tsx}`, `lib/{draft-heroes.ts,lobby.ts,lobby-room.ts}`, `scripts/sync-hero-videos.ts`.
Проверено: `tsc`/`lint`/`build` чисто (прежний warning `pouf/media.tsx`; первый прогон build упал на 11 «Error while requesting resource» — сеть, повтор прошёл). Стенд 42а, временная комната «ПРОГОН 42г» (снесена, в базе снова только комнаты Стаса 4 и 5): карта 1 отдрафчена целиком руками капитанов через `/dev/as/142` и `/dev/as/148` — 6 банов и 10 пиков встали по `SEQUENCE` в свои слоты, один ход сервер сделал сам по истечении банка и объяснил баннером «Время вышло», таймер переключался между сторонами без перезагрузки, у не-капитана пул только на просмотр и кнопки шторки нет. `next` админа открыл карту 2, в её пуле нет взятых на карте 1. Ширины замерены в браузере: 1920 рельс 240 (ОБС), 1440 — 216, 1280 — 176 (полоска банка появляется), 390 — стек, пул в шторке, горизонтального переполнения нет нигде. Ролик по наведению: качается один файл на героя через 120 мс удержания, `canplay` → проявление, `currentTime` пошёл; в скрытой вкладке браузер сам ставит его на паузу. Режим `reduce` глазами не проверен — включить его в этом браузере нечем, сверено по CSSOM (правило на `*` в стилях есть).
Дальше: **вопрос Стасу** — webm всех 127 героев весит 500 МБ (в среднем 3.9 МБ, других размеров Valve не отдаёт), в git такое не кладём; решить — качать подмножество, хранить вне репозитория или отказаться от webm совсем. До решения ролики не скачаны, борд работает на PNG. Потом `qa` — приёмка 42г, затем `stage` 42д (назначение своего героя, `done`, переключатель карт). Открыто: `prisma/dev.db` изменена (пересев стенда 42а + временная комната), статусы сторон на борде читаются «ходит/ждёт», а не «ГОТОВ/ЖДЁМ» из DESIGN-1 — на драфте обе стороны всегда готовы; артборды атомов в Ките не заведены (DESIGN-10 снят Стасом, догоняет `design`).

