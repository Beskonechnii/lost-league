# WORKLOG — журнал работ

Одна запись на этап. Новейшее сверху, формат **≤5 строк**: дата · что · файлы · дальше.
Новый чат читает только верхнюю запись — `sed -n '1,30p' WORKLOG.md`.
Здесь держим **последние 10 записей**; когда набирается больше, нижние уезжают в
`docs/archive/WORKLOG-2026-09.md` (там же полные, неурезанные версии всех записей ниже)
и `docs/archive/WORKLOG-2026.md` (лето–август).
Отчёт о выполнении ТЗ пишется сюда, а не в файл ТЗ.

---

## 2026-09-25 — stage: ТЗ 43 сделано упрощённо (срочно, под сборку)

Скрипт `web/scripts/import-mixcup-eclipse.ts` (не `import-mixcup-signups.ts` из рамки pm — имя другое). Сведение: `UserAccount.tgId` → `Player.telegram` → иначе новый профиль (по нику не сводим). Прогон на 42 записях: 26 сведено с уже существующими verified-игроками лиги (телеграм совпал), 16 новых теневых профилей (`verified:false`, `sourceTournamentId`), 42 записи в `TournamentRegistration`. Турнир `eclipse` («Mix Cup by Eclipse», kind: mixcup, status: registration) заведён самим скриптом — упрощение против рамки ТЗ (там предполагался ручной мастер). Поле «капитан» из анкеты никуда не легло — в модели такого нет, решили не делать под срочность. 1 битый MMR (`velesevich99`) — `null`, в отчёте скрипта.
Файлы: `web/scripts/import-mixcup-eclipse.ts` (новый), `web/prisma/dev.db`, `docs/tasks/43-import-uchastnikov-mix-cup.md` (статус done).
Дальше: если понадобится колонка «капитан» в консоли турнира — отдельный этап (правка схемы).

## 2026-09-25 — pm: заведено ТЗ 43 «Импорт участников Mix Cup из гугл-таблицы»

Вводная Стаса: залить в Mix Cup «Eclipse» 42 человека из формы регистрации (лист «UNDERBEER v2.1»). Вердикт — **не в MVP** (весь Mix Cup later с 22.09). Рамка: скрипт `scripts/import-mixcup-signups.ts` (образец разбора — `sheet-to-roster.ts` + `xlsx.ts:loadWorkbook`), сведение по `UserAccount.tgId` → `Player.telegram`, по нику не сводим; недостающим — аккаунт `source: "telegram"` и профиль `verified: false` + `sourceTournamentId` (исключение 22.09.2026), запись upsert'ом с `desiredRoles`; лимит и `joinOpen` не проверяем, кривые MMR идут с `null` в блок «требует внимания». UI массового добавления и уведомления в телеграм — вне Scope.
Файлы: `docs/tasks/43-import-uchastnikov-mix-cup.md` (новый), `MVP.md`, `BACKLOG.md`.
Дальше: два вопроса Стасу — признак «хочет капитанить» (хранить в модели или только в отчёте) и параметры турнира Eclipse (название, slug, лимит мест); до ответов ТЗ в `draft`. Очередь работ не меняется: следующим по-прежнему `qa` — приёмка 42а–42ж, затем `stage` 42е.

---

## 2026-09-25 — ТЗ 42ж: удаление лобби и «Заполнить стенд» с ботами

Комнату удаляет `tools` или создатель — `deleteLobby` + `DELETE /api/lobby/<id>` (не намерение: снимка после него нет), кнопка внизу «Двери комнаты» за `AlertDialog` Кита; каскад схемы уносит состав и переписку, будильники часов и ботов гасятся руками, открытым вкладкам уходит новое событие живого канала `lobby-gone` → `/lobby?gone=1` с «Комната удалена». Стенд: сид двенадцати аккаунтов переехал из скрипта в `lib/lobby-stand.ts` (скрипт стал обёрткой; `server-only` там нельзя — файл тянет tsx), кнопка «Заполнить стенд» (намерение `fill`) сажает нажавшего капитаном стороны A, четырёх ботов к нему и пятёрку с тренером на сторону B, заводя аккаунты сама, если их в базе нет. Боты (`lib/lobby-bots.ts`) шлют ТЕ ЖЕ намерения через тот же `applyIntent` без права `tools`: «Готов», блок после монетки, ход случайным доступным, назначение героя себе — одно решение на вызов, планировщик-будильник 1–2 с в `lobby.ts` просыпается на каждой рассылке снимка. Замок двойной: `LOBBY_STAND=1` (в отличие от `DEV_AS` действует и в прод-сборке — тест идёт через туннель) плюс право `tools`; сторона с живым капитаном ботом не управляется.
Файлы: `lib/{lobby.ts,lobby-bots.ts,lobby-stand.ts,chat-events.ts}`, `api/lobby/[id]/route.ts`, `(public)/lobby/{page.tsx,[id]/page.tsx,_components/room.tsx}`, `scripts/seed-lobby-stand.ts`, `.env.example`, `docs/{MAP,ARCHITECTURE,DEPLOY-MANUAL}.md`.
Проверено: `tsc`/`lint`/`build` чисто (прежний warning `pouf/media.tsx`). Dev 3000, временные комнаты 12–14 (снесены, в базе снова только 4, 5, 11): одним аккаунтом Bo3 целиком — заполнить → бот-капитан B сам «Готов» → монетка → мой блок, второй взял бот → драфт (бот отходил свои восемь ходов сам, без таймера) → девять героев боты разобрали сами, десятый выбрал я → `next` → карта 2 → карта 3 → `done`, в нём pick/assign/next отказаны текстом. Повторное «Заполнить стенд» — no-op (11 участников, 5+6). Права: участник без `tools` получает «Комнату удаляет её создатель или админ лиги» на DELETE и «Стенд заполняет админ лиги» на `fill`, кнопок у него нет. Удаление кликом из диалога: вкладка нажавшего и ВТОРАЯ открытая вкладка ушли на `/lobby?gone=1`, строки `LobbyMember`/`LobbyMessage` комнаты исчезли. Стенд сносил скриптом (`--remove`) и заводил заново кнопкой — 12 аккаунтов создались сами, **id сменились на 217–228** (адреса `/dev/as/<id>` печатает скрипт). Прод-контейнер `serve` (3001, `LOBBY_STAND=1` через временный `docker-compose.override.yml`, туннель не поднимал; контейнер погашен, override удалён): `/dev/as/1` → 404, кнопка на месте, Bo1 пройден целиком ботами и комната удалена через диалог. Ширины: 390 — кнопка 143px, подпись переносится, `scrollWidth` 390; 1280 и 1440 — без переполнения.
Дальше: `qa` — приёмка 42ж (и 42а–42д, если ещё не принято), затем `stage` 42е (кожа). Открыто: в контейнере `DATABASE_URL` берётся из `.env` (`file:./prisma/dev.db`) и база тома `/app/data/dev.db` не используется — расходится с `DEPLOY-MANUAL.md` §3/§9, к `ship`/`qa` как отдельный пункт; `prisma/dev.db` изменена (временные комнаты заведены и снесены, у стенда новые id).

---

## 2026-09-25 — ТЗ 42д: свой герой после драфта, `done`, переключатель карт; webm с CDN Valve

Карта закрывается не 16-м ходом, а десятью назначениями: у `GameDraft` появилось необязательное поле `assigns: {heroId, memberId, byAdmin?}` (`FEARLESS_VERSION` 4, версия 3 читается и нормализуется на чтении — идущие драфты не роняем), герой занимается первым нажавшим (`applyAssign` + `commitAssign` тем же сравнением-и-записью по payload, что и ход), свой выбор переезжает нажатием на другую плитку, чужое назначение переставляет админ комнаты (`byAdmin` → бейдж «переставил админ»). Заведено намерение `assign`; `next` до десяти отказан текстом, последняя карта серии → `Lobby.status = "done"` (`closeIfAssigned`) — в нём закрыты ходы, назначения и чат (`lobby-chat.ts`), чтение остаётся. Панель стадии — новый `lobby/_components/assign.tsx` на `DraftHeroButton big` (у атома появилось состояние `taken`: занятый не гасится, на нём читают ник); центр борда на стадии назначения включается флагом `data-after` — иначе на узком он спрятан вместе с пулом. Переключатель карт — `PillTrack`/`PillButton` над бордом, только сыгранные и текущая, состояние в `?game=N` через `history.replaceState`; прошлая карта — тот же борд read-only плюс `DataTable` «Сторона · Игрок · Герой». Два решения Стаса 25.09: **webm берутся с CDN Valve** (`draft-heroes.ts` отдаёт `cdn.cloudflare.steamstatic.com/.../<slug>.webm` всем героям, манифест с диска убран; `onError` видео гасит попытку и оставляет PNG; CSP приложения — только `frame-ancestors`, media не режет) и **статусы сторон до драфта — «ГОТОВ» / «ЖДЁМ»** на доске сбора и в строке статусов борда, причина отдельной строкой ниже.
Файлы: `lib/{fearless,lobby,lobby-turn,lobby-chat,draft-heroes}.ts`, `api/lobby/[id]/route.ts`, `(public)/lobby/[id]/page.tsx`, `(public)/lobby/_components/{assign.tsx,room.tsx,draft-board.tsx,gather.tsx}`, `components/pouf/{draft.tsx,pouf.css}`, `(admin)/admin/fearless-draft/[id]/page.tsx` (тот же терпимый разбор версии).
Проверено: `tsc`/`lint`/`build` чисто (прежний warning `pouf/media.tsx`). Стенд 42а, временные комнаты 9 и 10 (снесены, в базе снова только комнаты Стаса 4 и 5): Bo3 пройден целиком — готова одна сторона → драфт не стартует, обе → монетка; на карте 1 спор за героя («Этого героя уже взяли» второму), чужой пик отказан, смена выбора переносит одно назначение, не-админ чужого не переставляет, админ переставляет с `byAdmin`, `next` до десяти отказан; 10 назначений → `next` → карта 2 → карта 3 → `done`, в нём assign/pick/next и чат отказаны, GET участнику 200. В браузере: панель на 390 — список «плитка 96px + ник справа», на 1440 — сетка плиток 145.5px с бейджем ника поверх, 1280 — колонки 176/841/176, горизонтального переполнения нет ни на одной ширине; клик по плитке даёт `chosen` и «Вы», меню админа «Отдать <ник>» переставляет и рисует «переставил админ»; `?game=1` открывает прошлую карту с таблицей назначений и баннером «Идёт карта 3 — вернуться», текущая остаётся рабочей; «ГОТОВ»/«ЖДЁМ» приезжают живым каналом обеим сторонам; ОБС-вид по ключу рисует борд как прежде. Ролик с CDN: `viper.webm` доехал (`readyState` 4, играет), событие `error` убирает видео и оставляет PNG.
Дальше: `qa` — приёмка 42д (и 42а–42г, если ещё не принято), затем `stage` 42е (кожа). Открыто: в переключателе нет знака результата `W`/`L` из DESIGN-6 — лобби не знает, кто выиграл карту (запись в `Series` вне Scope); `Capacity` при 10/10 рисует чип «Мест нет», для счётчика выбравших формулировка чужая — это к `design`; `prisma/dev.db` изменена (временные комнаты заведены и снесены).

## 2026-09-25 — ТЗ 42г: экран драфта по WSS — слоты 1–16, один таймер, пул с webm

Борд лобби собран из НОВЫХ атомов Кита (`pouf/draft.tsx`: `DraftSlot` · `DraftRail` · `DraftHeroButton` · `TurnClock` · `CaptainPlate` рядом с `CoinFlip`) и композиции `lobby/_components/draft-board.tsx`; `FearlessRun` оператора из лобби и из ОБС-вида убран совсем — борд оператора остался как был (Scope). Раскладка — одна сетка `pouf-draft` (секция DRAFT в `pouf.css`): ширины токенами `--draft-rail`/`--draft-tile` (240/216/176/148 и плитка 92/80/68/56), ≥1024 три колонки «сторона · пул · сторона» + подвал, ниже — стек, липкая строка хода `top:64` и пул в `Sheet` по кнопке, видной только капитану на своём ходу (подтверждение хода уехало в ту же шторку — в центре борда оно на телефоне невидимо). Таймер ОДИН и переезжает к стороне хода анимацией `margin-left` (`justify-self` не анимируется). Слот: пустой · наведён · нажат · пульс обводки текущего хода · появление героя `key={heroId}` + вспышка · бан (ч/б + диагональ + «Бан») · последний пик; в слотах только PNG. В `pouf.css` заведён глобальный блок `prefers-reduced-motion: reduce` (его не было, хотя комментарий Кита утверждал обратное); монетка держит свои 300 мс через `!important`. Скрипт `scripts/sync-hero-videos.ts` (идемпотентный, `--check`/`--force`/`--only`) качает рендеры Valve в `public/assets/hero-renders`, `lib/draft-heroes.ts` отдаёт `video` только тем, кто лежит на диске. Монетка добавлена в ОБС-вид (`LobbyBoard.coin`).
Файлы: `components/pouf/{draft.tsx,pouf.css}`, `(public)/lobby/_components/{draft-board.tsx,room.tsx}`, `(public)/lobby/[id]/{page.tsx,loading.tsx}`, `(bare)/overlay/lobby/[key]/{page.tsx,_components/obs-board.tsx}`, `lib/{draft-heroes.ts,lobby.ts,lobby-room.ts}`, `scripts/sync-hero-videos.ts`.
Проверено: `tsc`/`lint`/`build` чисто (прежний warning `pouf/media.tsx`; первый прогон build упал на 11 «Error while requesting resource» — сеть, повтор прошёл). Стенд 42а, временная комната «ПРОГОН 42г» (снесена, в базе снова только комнаты Стаса 4 и 5): карта 1 отдрафчена целиком руками капитанов через `/dev/as/142` и `/dev/as/148` — 6 банов и 10 пиков встали по `SEQUENCE` в свои слоты, один ход сервер сделал сам по истечении банка и объяснил баннером «Время вышло», таймер переключался между сторонами без перезагрузки, у не-капитана пул только на просмотр и кнопки шторки нет. `next` админа открыл карту 2, в её пуле нет взятых на карте 1. Ширины замерены в браузере: 1920 рельс 240 (ОБС), 1440 — 216, 1280 — 176 (полоска банка появляется), 390 — стек, пул в шторке, горизонтального переполнения нет нигде. Ролик по наведению: качается один файл на героя через 120 мс удержания, `canplay` → проявление, `currentTime` пошёл; в скрытой вкладке браузер сам ставит его на паузу. Режим `reduce` глазами не проверен — включить его в этом браузере нечем, сверено по CSSOM (правило на `*` в стилях есть).
Дальше: **вопрос Стасу** — webm всех 127 героев весит 500 МБ (в среднем 3.9 МБ, других размеров Valve не отдаёт), в git такое не кладём; решить — качать подмножество, хранить вне репозитория или отказаться от webm совсем. До решения ролики не скачаны, борд работает на PNG. Потом `qa` — приёмка 42г, затем `stage` 42д (назначение своего героя, `done`, переключатель карт). Открыто: `prisma/dev.db` изменена (пересев стенда 42а + временная комната), статусы сторон на борде читаются «ходит/ждёт», а не «ГОТОВ/ЖДЁМ» из DESIGN-1 — на драфте обе стороны всегда готовы; артборды атомов в Ките не заведены (DESIGN-10 снят Стасом, догоняет `design`).

## 2026-09-25 — ТЗ 42в: состав и готовность — «Неопределившиеся», лимит 5+1, монетка

«Вне сторон» и «администрация» разведены: `side = null` у игрока/тренера — это «неопределившийся», администрация — роли `admin`/`caster` (`isStaff`, `staffIn`, `undecided` вместо `roomOnly`; карточка сайдбара стала «Администрация»). Стадия сбора — доска трёх лунок на атоме Кита `pouf/board.tsx` (`_components/gather.tsx`): человек ставит себя кнопками (`sit`: `Segmented` «Игрок · Тренер» + две кнопки сторон), админ комнаты двигает любого (`seat`) мышью или меню строки — на телефоне перетаскивания нет; `leave` удаляет членство, пока идёт сбор, и уводит на `/lobby`. Лимит `SIDE_PLAYERS`=5 + `SIDE_COACHES`=1 держит сервер отказом текстом; администрацией сам встаёт только `tools`. Готовность жмёт капитан один (`sideBlocker`: «нужно ещё игроков: N» → «нет капитана» → «ждём капитана», тренер не считается), админ переназначает капитана `unseat` с `memberId` одной транзакцией. Монетка: новое поле `Lobby.coinAt` (миграция `20260925075116_lobby_coin_at`) — от серверной отметки все вкладки играют одну анимацию (`CoinFlip`, новый файл Кита `components/pouf/draft.tsx` + секция в `pouf.css`, DESIGN-4: 400/1600/400 мс, отрицательная задержка для опоздавших, `reduce` — fade). Карточки сторон в `room.tsx` остались только после сбора и потеряли кнопки.
Файлы: `prisma/schema.prisma` + миграция, `lib/{lobby,lobby-room}.ts`, `api/lobby/[id]/route.ts`, `(public)/lobby/_components/{gather.tsx,room.tsx}`, `components/pouf/{draft.tsx,pouf.css}`, `docs/{ARCHITECTURE,MAP}.md`.
Проверено: `tsc`/`lint`/`build` чисто (прежний warning `pouf/media.tsx`). Прогон на стенде 42а через сессии `/dev/as` (временная комната «ПРОГОН 42в» и временный админ `stand-admin@lost.test` — снесены после прогона, комнаты Стаса 4 и 5 не тронуты): 12 вошли по паролю в «Неопределившиеся», разобрались по сторонам, шестой игрок и второй тренер получили отказ текстом, сам-в-администрацию без `tools` отказан, админ перевёл человека в ОБС; капитан — первый нажавший, второму «Капитан уже выбран»; сторона вчетвером «Готов» не жмёт, не-капитан не жмёт вовсе; админ переназначил капитана — готовность перешла к новому; сторона из капитана и пятёрки БЕЗ тренера стартовала, обе готовы → монетка, выбор блоков, `draft`. Анимацию снял семплером в браузере: класс `pouf-coin--play` с задержкой −296 мс (компенсация круга), `pouf-coin-spin`/`land` идут, монетка садится гранью победителя; вкладка, открытая после броска, не крутит. В браузере: 1280 — три лунки, 390 — стек, «Неопределившиеся» первой, `scrollWidth === 390`; перетаскивание мышью и меню «Назначить капитаном» проверены кликами. `leave` на стадии `coin` отказан, в `gather` — членство удалено, страница ему 404, повторный вход по паролю вернул в «Неопределившиеся».
Дальше: `qa` — приёмка 42в (и 42а/42б, если ещё не принято), потом `stage` 42г (экран драфта по WSS). Открыто: DESIGN-7 называл для роли `ChoiceChips`, поставлен `Segmented` (тот серверный, множественного выбора и через FormData, а роль одна и меняется мгновенно) — на подтверждение `design`; артборд `CoinFlip` в Ките не заведён, это роль `design` и ТЗ требует его до старта 42г; монетки в ОБС-виде нет — `LobbyBoard` про неё не знает, это 42г. `DEV_AS` в `web/.env` отсутствует: dev-сервер на 3000 поднят с `DEV_AS=1` в окружении, после перезапуска стенд снова закроется.

## 2026-09-25 — ТЗ 42б: дверь лобби — пароль, список комнат, приглашение

Комнату заводит только `tools` (`canCreateLobby`), форма `/lobby/new` — название, пароль и имена двух сторон свободным текстом: привязка сторон к `Team` отменена (миграция `20260925063604_lobby_door` — `sideAName`/`sideBName` с переносом имён из связанных команд, `sideATeamId/B` стали необязательными, новое поле `password` открытым текстом). `LobbySide` потерял `teamId` и `logo` (цвет считается из имени, лого даёт кожа 42е), борд и ОБС-вид адресуют сторону номером. `/lobby` показывает одобренному игроку лиги и админу ВСЕ незакрытые комнаты и спрашивает пароль прямо в строке (`_components/rooms.tsx`) — на странице комнаты формы входа нет намеренно: чужая комната обязана отвечать 404. Новые намерения `enter` (проверяется ДО гейта членства; пять неудач подряд с аккаунта → минута паузы, счётчик в памяти процесса), `settings` (название и пароль всегда, имена сторон только в `gather`; смена пароля никого не выбрасывает) и `invite` (членство + сообщение «Spirit CTRL»; шлём `tellAccount`, а не `tellPlayer` — тот молчит на неодобренной анкете, а это ровно адресат приглашения). Пароль и ключ эфира едут мимо снимка (`roomSecrets`), в комнате у админа карточка «Дверь комнаты».
Файлы: `prisma/schema.prisma` + миграция, `lib/{lobby,lobby-room}.ts`, `api/lobby/route.ts`, `api/lobby/[id]/route.ts`, `(public)/lobby/{page,new/page,[id]/page}.tsx`, `_components/{rooms,new-lobby-form,room}.tsx`, `(bare)/overlay/lobby/[key]/_components/obs-board.tsx`, `docs/{ARCHITECTURE,MAP}.md`.
Проверено: `tsc`/`lint`/`build` чисто (прежний warning `pouf/media.tsx`). Прогон на стенде 42а (dev-сервер пришлось перезапустить — вчерашний процесс держал в памяти Prisma-клиент до миграции): админ завёл комнату формой, сменил название/пароль/имена — вошедший не выпал и увидел новые имена, старый пароль отказан, новый пустил; игрок лиги нашёл комнату в списке и вошёл паролем из строки; пять неверных → шестая «Подождите 60 с.» (верный пароль в паузу тоже не пускает); аккаунт `pending` (временно снятый статус у стенда B·Тренер, вернул) списка не видит, паролем не входит, `/lobby/3` → 404, а по кнопке приглашения в личке вошёл без пароля; POST создания от игрока лиги → 403, `/lobby/new` → 404; ОБС-вид показывает «Синие — Красные». Бэкфилл миграции проверен на копии БД из HEAD с заведённой вручную старой комнатой: имена подставились из команд. 390px — переполнения нет (`scrollWidth === 375`). Тестовая комната, приглашение и беседа удалены, `Lobby`/`LobbyMember` в базе пусты.
Дальше: `qa` — приёмка 42а и 42б. Потом `stage` 42в (слот «Неопределившиеся», выбор стороны и роли, лимит 5+1, перенос админом, готовность капитана, монетка с анимацией). Открыто: второй админ комнаты видит изменённый другим админом пароль только после перезагрузки (в снимок он не едет намеренно); счётчик перебора живёт в памяти процесса и обнуляется рестартом.

## 2026-09-24 — ТЗ 41: основные роли игрока — до двух, у самого игрока

`Player.mainRoles` (CSV ключей `roles.ts`, максимум `MAIN_ROLES_MAX`=2) + `mainRolesAt` — миграция `20260924190000_player_main_roles`, она же переносит `position` уже поданных анкет (из ростера роли НЕ берём, решение Стаса 24.09). Анкета `/me`: шаг «Позиция» стал группой «Роли» на `ChoiceChips` (1–2, проверка в `applicationProblems`, старая анкета с одним ключом читается как одна роль), апрув и добивка профиля при привязке кладут роли в `mainRoles`. Кабинет `/me/profile`: новое поле «Основные роли» после MMR — пишется сразу, но не чаще раза в сутки (`mainRolesRefusal` → «через N ч.», группа выключена ещё до клика, отказ не роняет остальную форму). Бот спрашивает до двух ролей одним шагом `reg_position` в два прохода («Достаточно» закрывает), сводка печатает «Роли». `/join/<slug>` предзаполняется основными ролями, обратно в них не пишет. Порядок источников роли теперь один и в `draftPool()`: место в дивизионе ЭТОГО турнира → `desiredRoles` этого турнира → `mainRoles` (место прошлого сезона больше не влияет — замечание `qa` к 38); ad hoc — `mainRoles`, иначе свежайшее место. В Кит добавлено свойство `max` у `ChoiceChips`: лимит рисует браузер (`:has(input:checked ~ input:checked)`), для этого флажок переехал из `<label>` в соседи по `for` — вложенные `:has()` запрещены.
Файлы: `prisma/schema.prisma` + миграция, `lib/{roles,application,account,draft-data,tg-register}.ts`, `pouf/{choice-chips.tsx,pouf.css}`, `(public)/me/{actions.ts,application-form.tsx,welcome.tsx}`, `(public)/me/profile/{page,profile-form,actions}`, `(public)/join/[slug]/page.tsx`, `_components/application-summary.tsx`, `scripts/export-db.ts` (снимок v20), `docs/ARCHITECTURE.md`.
Проверено: `tsc`/`lint`/`build` чисто (прежний warning `pouf/media.tsx`). В браузере на 390/1280/1440 (переполнения нет, семь чипов — три ряда в колонке формы, два на широком `/join`): временная анкета (draft-аккаунт + `/dev/as`) с двумя ролями → в сводке модерации «Роли: Мид, Хард-саппорт», после «Одобрить» у профиля `mainRoles=mid,hard-support`, `mainRolesAt` пуст (первое заполнение лимит не запускает); без ролей анкета не уходит («Отметьте хотя бы одну роль»); третий чип мышью не нажимается, под рядом «Выбрано 2 из 2…». Кабинет: сохранил две роли → в базе канонический порядок, перезагрузка показывает их же, группа выключена и «Следующая правка — через 24 ч.»; повторное сохранение — «Анкета сохранена. Роли не изменены.», роли прежние; четыре роли мимо формы → сохранены две и плашка «Не больше двух ролей». `/join/test41`: чипы предзаполнены, снял одну — запись хранит одну, `mainRoles` прежние. `draftPool` скриптом: спот этого турнира > запись > `mainRoles`, ad hoc — `mainRoles`, иначе место. Бот прогнан скриптом: два прохода, «Достаточно», повтор роли, мусор. Миграция данных проверена на копии БД: битый JSON не роняет, одна роль переносится, заполненное поле повторный прогон не трогает. Тестовые турнир, два аккаунта, профиль и записи удалены, роли у `BSK` сняты.
Принято `qa` 25.09 (18/18), статус `done`, блок в `DECISIONS.md`. После приёмки: кабинет больше не стирает роли, если суточный лимит истёк при открытой странице (метка `mainRolesSent`, проверено в браузере); бот предлагает все семь ролей («Замена»); бот делит «Имя и фамилия» на два поля — без этого регистрация через бота не доходила до отправки (было до 41, проверено скриптом); шаг анкеты «Рейтинг и роли».
Дальше: `data/snapshot.json` пересобрать (`db:export`, v20), когда уберут стенд ТЗ 42а; на решение Стаса — первое заполнение ролей в кабинете сразу включает суточный лимит; состояние «лимит» в артборд «Чипы выбора» Кита; `canRegister()` дублирует поиск профиля; 35 ждёт `qa`, 33 закрыть как поглощённое 37.

---

## 2026-09-24 — ТЗ 42а: стенд для прогона лобби («войти как» + 12 тестовых аккаунтов)

`/dev/as/<accountId>` (route handler вне групп) ставит настоящую подписанную куку сессии и уводит на `/` — двенадцать вкладок под двенадцатью людьми открываются копипастой. Замка два: `NODE_ENV !== production` **и** `DEV_AS=1`, иначе 404; кука настоящая, поэтому смена аккаунта не требует перезапуска процесса (`devSessionCache` живёт в пути `DEV_LOGIN_EMAIL`, где куки нет вовсе, и сюда не лезет). `scripts/seed-lobby-stand.ts` заводит 12 аккаунтов с профилями — по 5 игроков и тренеру на стороны «Стенд A» и «Стенд B»; ключ аккаунта — почта `stand-<сторона><место>@lost.test`, профиля — слаг `stand-a1`. Аккаунт `status: "active"` (иначе в лобби не пускают), профиль `verified: false` — стенд не должен всплывать в поиске, на публичных карточках и в sitemap. Печатает таблицу «ник → id → адрес», `--remove` сносит.
Файлы: `src/app/dev/as/[id]/route.ts`, `scripts/seed-lobby-stand.ts`, `.env.example` (`DEV_AS`), `docs/MAP.md` §2.
Проверено: `tsc`/`lint`/`build` чисто (прежний warning `pouf/media.tsx`), в сборке виден `ƒ /dev/as/[id]`. Скрипт дважды подряд — та же таблица из 12 строк, в БД 12 профилей и 12 аккаунтов, дублей нет. `next start` (production) на 3005 даже с `DEV_AS=1` → 404 на `/dev/as/142` и `/dev/as/1`. В браузере на dev-сервере 3000: `/dev/as/142` → редирект на `/`, в аватар-меню «Стенд A · Керри · Игрок»; `/dev/as/148` в той же вкладке — «Стенд B · Керри», процесс не перезапускался; несуществующий id и мусор в адресе → 404. Вышел из стенда — автовход `DEV_LOGIN_EMAIL` вернулся сам. `DEV_AS=1` лежит в **`web/.env.local`** (в `.gitignore`), `.env` не трогал. `db:export` НЕ гонял: в дереве чужая незакоммиченная работа (41), а стенд восстанавливается запуском скрипта.
Дальше: `qa` — приёмка трёх критериев 42а. Потом `stage` 42б (дверь: создание админом, список комнат, вход по паролю, приглашение в личку). Открыто: снимок БД стенда не содержит — на облачной сессии скрипт гоняется руками.

## 2026-09-24 — ТЗ 39: лимит записи на индивидуальный турнир + роли словами в эфире

`Tournament.registrationLimit Int?` (миграция `20260924160000_tournament_registration_limit`, снимок v19): `registerForTournament()` перед созданием записи считает строки `TournamentRegistration` и при достижении лимита отдаёт новую причину `JoinResult.reason: "full"` — отмена освобождает место сама, листа ожидания нет, статус турнира лимит не трогает. `/join/<slug>` печатает «X из N» тем же `Capacity`, что дивизионы (`unit="players"`), а при нуле мест показывает «Мест не осталось» вместо формы — и гостю, и вошедшему. Оператор задаёт число в «Правилах» консоли турнира тем же `PATCH /api/tournaments/[id]`, что тумблеры; меньше уже записанных поставить нельзя (тост с текстом отказа, поле откатывается). Отдельной правкой по замечанию `qa` к ТЗ 38: в оверлее OBS роли пишутся словами через слеш («Керри / Мид») — флажок `roleWords` у общего `DraftPlayerLine`, на борде и в пуле остались номера («поз. 1 · 2»).
Файлы: `prisma/schema.prisma` + миграция, `lib/mixcup.ts`, `api/tournaments/[id]/route.ts`, `(public)/join/[slug]/{page.tsx,actions.ts}`, `admin/tournaments/_components/{rules-panel,draft-console,participants-panel}.tsx`, `underbeer/[id]/_components/player-line.tsx`, `(bare)/overlay/draft/[id]/_components/overlay-live.tsx`, `scripts/{export,import}-db.ts`, `data/snapshot.json`, `docs/ARCHITECTURE.md`.
Проверено: `tsc`/`lint`/`build` чисто (прежний warning `pouf/media.tsx`). В браузере на 1440 и 375, временный турнир `test39` (underbeer, приём открыт, лимит 2, две записи): «2 из 2 · Мест нет» и «Мест не осталось» без формы; попытка поставить лимит 1 — тост «Уже записано 2 — лимит меньше поставить нельзя», поле вернулось к 2; снял одну запись — «1 из 2» и форма вернулась, записался сам и снова стало «2 из 2»; отмена вернула «1 из 2»; пустой лимит — «1 участник» и запись без отказа. Оверлей: на живом `#27` под ником «Керри»/«Мид», на временной сессии с двумя желаемыми ролями — «Керри / Мид», борд того же турнира — «поз. 1 · 2». Турнир, две учётки, два профиля и временная сессия удалены, записей на турниры в базе снова 0; `db:export` пересобран (v19).
Принято `qa` 24.09 (и оверлей), статус `done`; не проверен админ без `tournaments.edit` — нет второй учётки. ТЗ 41 (основные роли, до двух) заведено `pm`, развилки и DESIGN закрыты Стасом — `ready`.
Дальше: `stage` 41 (он же чинит «прошлый сезон → Несколько ролей» из 38). Открыто: `canRegister()` дублирует поиск профиля; 35 ждёт `qa`, 33 закрыть как поглощённое 37; состояние «лимит» в артборд «Чипы выбора» Кита.

---

## 2026-09-24 — ТЗ 38: запись на индивидуальный турнир спрашивает роли

Форма `/join/<slug>` обязательно спрашивает несколько желаемых ролей новым атомом Кита `ChoiceChips` (серверный: нативные флажки `sr-only` + чип по `pillClasses`, класс `.pouf-choice-chip`); ни одной не отмечено — `?err=roles` и плашка под рядом. Роли ложатся в `TournamentRegistration.desiredRoles` (CSV, миграция `20260924120000_registration_desired_roles`, снимок v18), разбор — `parseRoleKeys`/`joinRoleKeys` в `roles.ts`. `PoolPlayer.role`/`.position` → `roles: string[]`; `segmentPool` завёл сегмент «Несколько ролей» первым слева, подпись в пуле — «поз. 1 · 5»; `draftPool(tournamentId)` подмешивает роли только тем, у кого нет `RosterSpot`. Кука-намерение больше не записывает сама (`canRegister`), а возвращает на страницу записи — иначе гость въезжал бы без ролей.
Файлы: `pouf/{choice-chips.tsx,pouf.css}`, `lib/{roles,draft,draft-data,mixcup}.ts`, `(public)/join/[slug]/{page,actions,loading}`, `(public)/me/{actions.ts,mixcup-intent.tsx}`, `admin/tournaments/_components/{draft-console,participants-panel,result-grid}.tsx`, `admin/tournaments/[slug]/draft/page.tsx`, `(bare)/overlay/draft/[id]/page.tsx`, `underbeer/[id]/_components/{player-line,pool-columns}.tsx`, `prisma/**`, `scripts/{export,import}-db.ts`, `docs/{MAP,ARCHITECTURE}.md`.
Проверено: `tsc`/`lint`/`build` чисто (прежний warning `pouf/media.tsx`). В браузере на 1440 и 390, временный турнир `test38` (underbeer, приём открыт): отправка без ролей — плашка «Отметьте хотя бы одну роль» под чипами; две отмеченные роли сохранились, записанному показаны `Chip` только чтением; консоль печатает «Керри · Хард-саппорт» второй строкой; на борде участник без ростерного места лежит одной строкой в «Несколько ролей» (`поз. 1 · 5`), а рострованный `sdd` с `desiredRoles=coach,standin` остался в «Керри (поз. 1)» — место в составе сильнее желания. На 390 семь чипов легли в три строки, переполнения нет. Временный турнир, три записи и его сессия драфта удалены, база как была; `db:export` пересобран (v18).
Принято `qa` 24.09, статус `done`; отмена «без второго клика» ТЗ 34 — блок в `DECISIONS.md`. Артборд «Чипы выбора» добавлен в Кит (страница «Атомы»).
Дальше: `stage` 39 (лимит записи). Открыто по замечаниям `qa` — оверлей OBS теперь пишет «поз. 1» вместо «Керри»; игрок с `RosterSpot` прошлого сезона попадает в «Несколько ролей»; `canRegister()` дублирует поиск профиля из `registerForTournament()`. 35 ждёт `qa`, 33 закрыть как поглощённое 37.

## 2026-09-23 — ТЗ 37: турнир получает формат (Mix Cup и UNDERBEER — `Tournament.kind`)

`MixCupEvent` удалена, `mixcup-6` (2 команды, 10 пиков, `DraftSession#27`) перенесён миграцией `20260923150000_tournament_kind`; спутники `TournamentDraftSettings`/`TournamentRegistration`, `Player.sourceTournamentId`, снимок v17. Консоль обоих форматов — карточка турнира `/admin/tournaments/<slug>` (+`/draft`), публичная запись — `/join/<slug>`; `/admin/mixcup/<id>`, `/mixcup/<slug>` и `/tournaments/<slug>` при kind≠season редиректят. Право `mixcup` → `tournaments.edit`, `mixcup-section.tsx` удалена, `pouf/capacity.tsx` получил `unit`.
Файлы: `prisma/**`, `lib/{tournaments,mixcup,account,permissions,auth}.ts`, `admin/{tournaments,mixcup}/**`, `(public)/{join,mixcup,me}/**`, `api/{tournaments,join,underbeer}/**`, `tournament-card.tsx`, `(home)/**`, `scripts/{export,import}-db.ts`, `sitemap.ts`, `docs/{MAP,ARCHITECTURE}.md`, `DECISIONS.md`, `BACKLOG.md`.
Проверено: `tsc`/`lint`/`build` чисто; в браузере на 1024 и 390 — мастер с полем «Формат» в два шага, консоль UNDERBEER-турнира, запись и отмена на `/join/<slug>`, обе карточки на `/tournaments` и главной, результат `mixcup-6`; `db:export` → `db:import` на чистой базе восстановил турнир с результатом. Тестовый турнир и запись удалены, база как была.
Правки после `qa` (той же датой): плита главной у индивидуального формата не рисует «Призовой —», `openForRegistrationAll()` отдаёт только `kind: season` (баннер, `/apply` и кабинет новичка больше не зовут «Заявить команду» на UNDERBEER-турнир), `TournamentRegistration` вошла в снимок отдельным списком (турнир и игрок слагами, аккаунт по почте/tgId — round-trip проверен на временной записи), `/admin/tournaments` печатает у таких турниров «записалось: N» вместо «дивизионов: 0». `/join/<несуществующий>` → 200 не чинил: причина унаследованная (`loading.tsx` + стриминг), строка в `BACKLOG.md`.
Принято `qa` со второго захода, статус `done`; не проверена только гостевая ветка AC №6 (нужна учётка). Решения Стаса по DESIGN: `/join/<slug>`, «Формат»/«Регламент строкой», право `tournaments.edit`.
Дальше: `design` на 38 (`ChoiceChips` в Ките), затем `stage` 38 и 39; 35 ждёт приёмки `qa`, 33 закрыть как поглощённое 37.
