# WORKLOG — журнал работ

Одна запись на этап. Новейшее сверху, формат **≤5 строк**: дата · что · файлы · дальше.
Новый чат читает только верхнюю запись — `sed -n '1,30p' WORKLOG.md`.
Здесь держим **последние 10 записей**; когда набирается больше, нижние уезжают в
`docs/archive/WORKLOG-2026-09.md` (там же полные, неурезанные версии всех записей ниже)
и `docs/archive/WORKLOG-2026.md` (лето–август).
Отчёт о выполнении ТЗ пишется сюда, а не в файл ТЗ.

---

## 2026-09-22 — ТЗ 32: приватность витрины — телеграм и MMR гасятся тумблером лиги

Два флага на лигу (`PrivacySetting`, строка `id=1`, дефолт «скрыто») решают, видит ли публика
телеграм игроков и их MMR; источник правды один — `lib/privacy.ts`, его спрашивают и витрины, и
открытые роуты, и бот. Гасится в слое данных, до клиентских компонентов: `roster-data` (состав,
средний MMR, страница игрока), `apply/pool`, страница оверлея; открытый `GET /api/underbeer/[id]`
и payload оверлея чистятся скребком по ключу — в снимках старых сессий MMR уже записан. Экран
`/admin/privacy` под новым правом `privacy`: смотрит оператор, переключает только владелец.
**Файлы.** `lib/privacy.ts` (нов.), `prisma/schema.prisma` + миграция, `(admin)/admin/privacy/*` (нов.),
`api/privacy/route.ts` (нов.), `lib/{roster-data,profiles,permissions,tg-tournaments}.ts`,
`_components/tools.ts`, `players/[key]`, `roster/_components/team-cards`, `apply/{pool,board-player,board-ready}`,
`tournaments/[slug]/roster/players`, `overlay/underbeer/[id]/*`, `api/underbeer/[id]`, `pouf/player-line`.
**Проверено.** tsc/lint/build чисто (прежний warning `pouf/media.tsx`). Прод-сборка на 4203 + curl по
всему телу ответа: при выключенных флагах на `/players/<slug>`, `/roster`, `/roster/players`,
`/roster/teams/<id>`, `/tournaments/*/roster/players`, `/apply`, `/overlay/underbeer/<id>` и
`/api/underbeer/<id>` ни `t.me`, ни хендла, ни чисел MMR (включая `self.__next_f.push`); включил —
вернулись без перезапуска. Временная сессия драфта со старым снимком (MMR в payload) — скребок
сработал. `/api/lobby/obs/<key>` MMR не отдавал и до правки (`readBoard` берёт только ник и фото).
Бот: «Средний MMR основы» и телеграм капитана гаснут, флаг перечитывается внутри одного процесса.
Свои данные на месте (`/me/profile`, мини-профиль), операторские экраны и админский драфт — как
прежде. Запись админом без прав владельца — 403. 390/1280/1440 без переполнения. Временные сессия,
лобби, право и капитанство сняты, строка настройки удалена (дефолт «скрыто»).
**Дальше** — приёмка `qa` по ТЗ 32; в `PlayerLine` появился `hideValue` — кандидат в артборд Кита.

## 2026-09-22 — ТЗ 30: формы говорят, что не так

Анкета и регистрация объясняют отказ текстом вместо молчаливого фокуса: сводка плашкой над шагом
(«Не заполнено 8 полей — они отмечены ниже»), у каждого поля своё сообщение по существу, фокус
уходит в первый отвергнутый контрол с промоткой. Ссылка на профиль проверяется на шаге 2, где
вводится, а не отказом на шаге 3. Отказ сервера больше не стирает введённое: пароли на регистрации
и позиция с MMR на шаге 3 остаются на месте. Кит доучен под это: `Field` знает `required` и отдаёт
`FieldError` отдельным узлом, `inputClasses` и `DateField` принимают `invalid`; обе локальные копии
`Field` в `me/` удалены. Тексты правились серверные — клиент своего словаря не сочиняет.
**Файлы.** `pouf/{Input,date-field}.tsx`, `(public)/me/{application-form,auth-forms,actions}.tsx`,
`lib/{application,account,password-rules}.ts`.
**Проверено.** tsc/lint/build чисто (прежний warning `pouf/media.tsx`). На dev под временным
аккаунтом-новичком: пустой шаг 1 — сводка на 8 полей и восемь адресных подписей; «какой-то текст»
в ссылке ловится на шаге 2 («Нужна ссылка на Dotabuff, Stratz или Steam — например
dotabuff.com/players/123456»); MMR буквами на шаге 3 — позиция `offlane` и введённое на месте;
регистрация с паролем в 7 символов — «Минимум 10 символов — сейчас 7», оба пароля сохранены.
На 390 фокус уходит в MMR, переполнения нет (0px). Временный аккаунт удалён, `DEV_LOGIN_EMAIL`
возвращён в комментарий.
**Дальше** — приёмка `qa` по ТЗ 29 и 30.

---

## 2026-09-18 — ТЗ 29: предрелизная чистка публички

Настоящее имя ушло с трёх публичных поверхностей: строка под ником у игрока, поиск лиги (`realName`
больше не выбирается из базы — ни в подписи находки, ни в ранжировании) и оверлей UNDERBEER (пул
публичной страницы отдаётся с `realName: null`; в админском драфте имя осталось). Заодно финал мастера
турнира не ведёт в 404 (у черновика вместо публичной ссылки — «Переключить статус на карточке»), шапка
заявленных команд берёт статус турнира вместо константы «приём заявок», счётчик группы серии считает
видимые карточки, у страницы статистики дивизиона появились свои title, description и канон.
**Файлы.** `lib/search.ts`, `(public)/players/[key]/page.tsx`, `(bare)/overlay/underbeer/[id]/{page,_components/overlay-live}.tsx`,
`(public)/tournaments/{(index)/page,[slug]/entrants/page,[slug]/[div]/stats/page}.tsx`, `(admin)/admin/tournaments/new/[step]/page.tsx`.
**Проверено.** tsc/lint/build чисто (прежний warning `pouf/media.tsx`). На dev: `/api/search` по
«Ларин»/«Дмитрий» — пусто, по «sett» — игрок без подписи; в HTML оверлея все `realName` = null;
`s2/entrants` — «LOST Season 2 · идёт», `s4` — «приём заявок»; «Прочие турниры 2 турнира» над двумя
карточками; титул «LOST D1 — статистика»; мастер на черновике — обе ссылки 200, публичной нет.
390/1280/1440 — переполнения нет. Временный админ и черновик `zz-tz29` удалены, в `dev.db` остались
только сдвинутые счётчики autoincrement. **Дальше** — приёмка `qa` по ТЗ 29.

---

## 2026-09-17 — редактор темы перестал обещать чужой акцент

Панель `/admin/theme` красит `--accent`, а кнопки, вкладки, бейджи и акцентные ссылки продукта залиты
мятным `--accent-fill`/`--accent-ink` из Кита (`pouf.css:69`) — оператор менял цвет и не видел разницы.
Заводить акцент Кита в тему не стал (за заливкой тянутся `--on-accent`, `--grad-accent`, `--sh-accent` —
это работа `design`, строка в `BACKLOG.md`): вместо этого панель говорит границу прямо — подписи полей
акцента переписаны на реальные места, добавлен абзац про Кит и подпись у кнопки pouf в превью. Заодно
убрано переполнение на 390 (+44px): грид-элемент получил `min-w-0`, строка цвета — `flex-wrap` и
минимум 8rem у подписи, поле hex уезжает на вторую строку целиком.
**Файлы.** `lib/theme.ts`, `(admin)/admin/theme/_components/theme-admin.tsx`, `BACKLOG.md`.
**Проверено.** tsc/lint/build чисто (прежний warning `pouf/media.tsx`). Прод-сборка `next start -p 4203`:
390/1280/1440 — переполнения нет (390: документ ровно 390 против прежних 434), гость на `/admin/theme` →
307 в `/me`, PUT `/api/theme` гостю → 401, админ без права `tp.edit` получает плашку. Смена канвы через
панель перекрасила публичные страницы и вернулась назад; `data/theme.json` и `dev.db` — в исходном
состоянии, временный админ удалён.
**Дальше.** Находки обзора 5–13; ближайшая — финал мастера турнира со ссылкой на 404.

## 2026-09-17 — служебный канал лиги открыт тому, кому лига пишет

Заявитель получал «заявка отправлена» и «заявка одобрена» в беседу со Spirit CTRL, а `/chat` и
`/chat/system` показывали ему заглушку «получите одобрение»: гейт стоял на `isActiveAccount`, тогда как
`tellAccount` пишет ЛЮБОМУ аккаунту. `liveIdentity` теперь = любой вошедший (личка по-прежнему
`chatIdentity`), список бесед режет новая `visibleConversations`: не игроку лиги — только канал лиги.
Третья находка `docs/FLOW-REVIEW.md`.
**Файлы.** `lib/chat.ts`, `(public)/chat/page.tsx`, `chat/system/page.tsx`, `_components/account-nav.ts`,
`api/chat/read/route.ts`, `api/chat/stream/route.ts`.
**Проверено.** tsc/lint/build чисто (прежний warning `pouf/media.tsx`). Прод-сборка `next start -p 4203`:
временный аккаунт в статусе `draft` подал заявку «QA CHAT TEAM» на S5, временный оператор её одобрил —
заявитель видит оба сообщения в `/chat/system` и строку в `/chat`, колокольчик считает и гаснет после
прочтения; личка закрыта (`/chat/<id>` — заглушка, POST `/api/chat/messages` — 401). Временные аккаунты,
команда и заявка убраны, `dev.db` возвращён к коммиту.
**Дальше.** По обзору остаются находки 4–13: ближайшие — акцент темы из `data/theme.json` и финал
мастера турнира со ссылкой на 404.

## 2026-09-17 — приватность: две находки обзора (ПДн в API и в пуле заявки)

`GET /api/roster/players` отдавал строку `Player` целиком (имя, телефон, телеграм, дата рождения) кому
угодно без куки — закрыт правом `roster.edit`; витринам он не нужен, они читают `listPlayers()` на
сервере. Пул формы заявки (`/tournaments/<slug>/apply`) больше не везёт настоящее имя — ни в карточках
пула, ни в «ваших командах»: остаётся ник, команда, роль и MMR, то же, что на публичном `/roster/players`.
Поиск по пулу — по нику и команде, подпись поля поправлена. Порог входа в форму не меняли (решение 23.08).
**Файлы.** `api/roster/players/route.ts`, `apply/pool.ts`, `apply/apply-board.tsx`, `apply/board-player.tsx`,
`apply/board-ready.tsx`, `apply/board-pool.tsx`.
**Проверено.** tsc/lint/build чисто (прежний warning `pouf/media.tsx`). Прод-сборка `next start -p 4203`:
гость на `/api/roster/players` → 403, аккаунт с `roster.edit` → 200 и полные поля; свежий аккаунт в статусе
`draft` открывает заявку — 186 карточек пула, в разметке ноль `realName/telegram/birthday/phone`, поиск и
раскладка по слотам живые. Временный аккаунт и привязка удалены, `dev.db` возвращён к коммиту (в дампе
расходился только счётчик `sqlite_sequence`).
**Дальше.** Осталась открытой третья дыра того же рода — `GET /api/search` отдаёт `realName` подписью
к находке (`lib/search.ts:61,96`); публичный профиль игрока имя тоже показывает, так что это решение pm,
а не правка stage.

## 2026-09-17 — смоук: три находки приёмки закрыты

Служебный аккаунт «Spirit CTRL» перестал быть аккаунтом владельца — заводится своим
(`system@spirit-ctrl.local`, входа нет, в списке аккаунтов скрыт): владельцу вернулись имя, возможность
получать сообщения и все четыре строки очереди. Ссылка «отчёт №…» на `/match/<id>` (право `tools`) теперь
видна только тому, кто её откроет — страница встречи, игрок и команда. Список игроков уехал в группу
`(list)`: его `loading.tsx` накрывал Suspense'ом легаси-редирект, и `/roster/players/<id>` отдавал 200 с
пустой оболочкой вместо 308 (soft-404).
**Файлы.** `lib/system-chat.ts`, `lib/account.ts`, `_components/account-nav.ts`, `chat/system/page.tsx`,
`series/[slug]/page.tsx`, `players/[key]/page.tsx`, `roster/teams/[id]/page.tsx`, `_components/series-brief.tsx`,
`roster/players/(list)/*`, `prisma/schema.prisma`, `prisma/dev.db`, DECISIONS/MAP/ARCHITECTURE.
**Проверено.** tsc/lint/build чисто (прежний warning `pouf/media.tsx`). Прод-сборка `next start -p 4203`
(автовхода нет): гость — 0 ссылок `/match/` на трёх страницах, владелец — 2/14/6; `/roster/players/<битый>`
→ 308 на список, валидный → 308 на `/players/<slug>`. Уведомление прогнано живьём: правка профиля от
второго аккаунта → строка в колокольчике и в `/chat/system` у владельца. Заявка, сообщение, беседа и коды
входа удалены, в `dev.db` изменение одно — новый служебный аккаунт (у id 1 снят `system`, имя очищено:
прежний код его затёр).
**Дальше.** qa: перепринять три пункта смоука. Не проверено на боевом (ТЗ 01 не сделано).

## 2026-09-17 — ТЗ 04: smoke-чек-лист перед выкладкой

`docs/SMOKE.md` — прогон по всему продукту: гость · новый игрок · капитан · оператор · права · SEO · техника,
плюс раздел «Где гонять» с граблями среды (главная: **дев-сервер с `DEV_LOGIN_EMAIL` пускает запрос без куки
в `/admin`** — права на нём проверять нельзя, нужна прод-сборка). Роль `qa` уже была: правки — три ширины
вместо двух и ссылка на смоук. Кода не трогал.
**Файлы.** `docs/SMOKE.md` (новый), `.claude/agents/qa.md`, `docs/MAP.md`, `CLAUDE.md`, `BACKLOG.md`, ТЗ 04.
**Проверено.** tsc/lint/build чисто (прежний warning `pouf/media.tsx`). Прогон локальный: гость, права и SEO —
на прод-сборке `next start -p 4203` (там автовхода нет), ширины оператора — на 3000. Права pass по всем семи
пунктам (гость и игрок на `/admin` → `/me`; админ с одним правом видит одну группу; запись без права 403;
чужая беседа 403; кука удалённого аккаунта = гость). 390/1280/1440 без переполнения, консоль чистая, данные
переживают перезапуск. Временные учётки и скрипт убраны, в базе снова 2 аккаунта.
**Дальше.** **На боевом не проверено — ТЗ 01 не сделано.** Анкета, Steam, телеграм, бот, апрув заявки, лимиты
дивизионов и превью в телеграме ждут боевого адреса; в `BACKLOG.md` строка про куку удалённого админа.

## 2026-09-16 — ТЗ 02: сброс забытого пароля без писем

Одноразовая ссылка `/login/reset/<token>` (`PasswordResetToken`, 30 мин, одна живая на аккаунт,
механика `tg-link.ts`). Два пути: сам — через `/login/reset` (ссылка уходит в привязанный телеграм,
ответ формы одинаков для любой почты, лимит 5/15 мин) и оператор — кнопка в `/admin/staff` под правом
`accounts.admins`, владельцу и себе нельзя, пароля оператор не видит. Пароль — общий `passwordProblem`.
Смена пароля гасит прежние сессии: кука несёт `iat`, аккаунт — `sessionsFrom`. Открытый вопрос ТЗ закрыт
выборкой: аккаунтов без почты и без телеграма в базе 0 (строка записана в ТЗ).
**Файлы.** `prisma/schema.prisma` + миграция `20260916185356_password_reset`, `lib/password-reset.ts` (новый),
`lib/{player-auth,player-session,account,rate-limit,tg-notify,telegram}.ts`, `(public)/login/reset/**`,
`(public)/me/auth-forms.tsx`, `(admin)/admin/staff/{page,actions}.ts(x)` + `_components/reset-password.tsx`,
`next.config.ts` (Referrer-Policy), `docs/{MAP,ARCHITECTURE}.md`, `DECISIONS.md`, `BACKLOG.md`.
**Проверено.** tsc/lint/build чисто (прежний warning в `pouf/media.tsx`). В браузере на 3000: выдача ссылки
оператором, смена пароля, вход новым, повтор по той же ссылке и истёкшая — отказ экранами, слабый пароль —
тем же текстом, что при регистрации, владельцу — «через админку не сбрасывается», старая кука после смены
пароля отдаёт дверь (curl), `Referrer-Policy: no-referrer` и `noindex` на обеих страницах, 390 и 1440 без
переполнения. Временный аккаунт и все токены удалены, в базе снова 2 аккаунта.
**Дальше.** qa: приёмка 02. Ветку «нет почты и нет телеграма» отдали в BACKLOG — путь через оператора
опознаёт такого человека только по id.

---

## 2026-09-16 — ТЗ 03: базовое SEO публичной части

`metadataBase` из `siteUrl()` (BASE_URL → туннель → localhost, домен в код не зашит), шаблон
`%s — SPIRIT/CTRL`, OG и дефолтная карточка `app/opengraph-image.tsx` (1200×630, знак и начертание
Кита на бумаге). `robots.ts` и `sitemap.ts` — force-dynamic, 353 адреса, служебного нет. Живые
метаданные у игрока, команды, турнира, дивизиона и встречи; выборка общая с страницей через
`cache()`. `noindex` — layout'ами `me`, `login`, `chat`, `(bare)`. Битый слаг отдавал 200: стрим
начинала `loading.tsx` над карточкой — списки уехали в группы `(index)`/`(pool)`, скелет профиля
игрока снят (строка в BACKLOG). `/contact` наполнена, `/about`, `/partners`, `/media-kit`,
`/privacy` и `_components/coming-soon.tsx` удалены.
**Файлы.** `app/{layout.tsx,robots.ts,sitemap.ts,opengraph-image.tsx}`, `(public)/{contact,me,login,chat}/`,
`(public)/{players/[key],roster/teams/[id],roster/(pool),tournaments/(index),tournaments/[slug]/**,series/[slug]}`,
`(home)/layout.tsx`, `(bare)/layout.tsx`, `lib/tournaments.ts`, `.env.example`, `docs/MAP.md`, `BACKLOG.md`.
**Проверено.** tsc/lint/build чисто (прежний warning в `pouf/media.tsx`). На прод-сборке: битый слаг
игрока, команды, турнира, дивизиона и встречи — 404; списки и `/contact` — 200; `/me` и `/admin` —
noindex; sitemap без служебных адресов; `/contact` снят на 1440 и 390. Данных не трогал.
**Дальше.** qa: приёмка 03. Канон домена (www, http→https) — за ТЗ 01, боевого адреса ещё нет.

---
