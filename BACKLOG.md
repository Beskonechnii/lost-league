# BACKLOG — открытые пункты

Одна строка на пункт. Статус: **MVP** (без этого не запускаем сезон) · **later** (после релиза) ·
**icebox** (идея без решения, трогать только по запросу Стаса).
Что входит в релиз и почему — `MVP.md`, его ведёт роль `pm`. Закрытые пункты и их разборы —
`docs/archive/BACKLOG-2026-09.md`. Почему так решили — `DECISIONS.md`. Что делали — `WORKLOG.md`.

Правило: сюда попадает пункт, а не описание. Как только пункт берут в работу — под него
заводится ТЗ в `docs/tasks/`, и строка здесь ссылается на файл ТЗ.

---

## Карта работ 07–20 (13.09.2026)

Очередь изменена: **сначала продуктовый блок 07–15, потом блокеры Б1–Б4** — `MVP.md` §0,
`DECISIONS.md` 13.09.2026. Статус здесь — не «входит в релиз», а «делаем / отложено»:
в MVP по-прежнему только Б1–Б4.

| № | Статус | Пункт |
|---|---|---|
| 07 | сделано | **Мелкие баги главной** — счётчики у вкладок матчей (убраны ещё на ТЗ 06), скачок страницы при переключении вкладок (`scroll={false}` у вкладок «Баллы»), «Выйти» не работает на главной (работал только мышью: Enter перехватывал Radix, выход переехал с формы на `onSelect`). ТЗ не заводилось: три точечные правки. Четвёртый пункт («написать можно только игроку онлайн») из 07 вынут: формулировка неверна, работа больше этапа — `docs/tasks/21-soobshchenie-igroku-bez-akkaunta.md` |
| 08 | принято | **Верхний бар вместо сайдбара** — бар с главной на все разделы, сайдбар удаляется; служебное уходит на хаб `/admin`, в баре один пункт «Админ»; пункт «LOST cup» переименован в «Турниры» и ведёт на `/tournaments`, из меню аватара убран «Админ» (решения 13.09). `docs/tasks/08-verkhniy-bar.md` |
| 09 | принято | **Раздел «Турниры»** — группировка по серии (свободное поле, LOST первой группой), дивизион своей карточкой, лимит команд у дивизиона, хронология, вход в импорт из редактирования. `docs/tasks/09-razdel-turniry.md` |
| 10 | принято | **Архив матчей лиги** — лента всех встреч по `/series` (`/tournaments/archive` занят хронологией турниров с ТЗ 09), разрезы `?m=`, фильтр `?t=`, пагинация, шестой пункт «Встречи» в баре (решение 14.09). Закрыло «страницы всех встреч нет», «все матчи ведут на один турнир», `/schedule`-заглушку (308) и сдавленный бар на 1024 (`scrollWidth == clientWidth == 1009`, проверил `qa`). `qa` дал один fail — S10, бренд в заголовке при `?t=`; причина была в самом ТЗ, формула исправлена 14.09 (`stripBrand()`), после правки принято. `docs/tasks/10-arkhiv-matchey.md` |
| 11 | принято | **Импорт одним экраном** — `/admin/roster/import` и `/admin/roster/crm-import` сводятся в один экран с двумя механизмами (игроки · составы), после составов — опциональное распределение в турнир. Оба механизма работают, задача про связность. **Граница с 09:** `/admin/tournaments/*` — это 09 (откуда входим и куда возвращаемся), `/admin/roster/import*` — это 11 (сам экран, включая шаг «распределить в турнир»). Шаг записи переиспользует существующее «Назначение» (`writeTeamToRoster` → `setTeamDivision`), нового механизма нет. `docs/tasks/11-import-odnim-ekranom.md` — ready (развилки закрыты 14.09: контекст турнира у «Игроков» = только возврат, дивизион выбирается явно) |
| 12 | делаем | **Графика** — медали рангов настоящими ассетами OpenDota вместо отрисованных: подложка `rank_icon_<N>.png` + звёзды `rank_star_<N>.png` с `https://www.opendota.com/assets/images/dota2/rank_icons/`, забираем скриптом в `public/uploads/`, не хотлинкуем. Плюс значки для баллов и осколков и «осколки» → «Spirit Shards» везде в UI; референсы значков приносит Стас. Конфликта нет: решение 10.09.2026 «рисуем свою медаль» отменено 13.09.2026. ТЗ перед работой |
| 13 | делаем | **Команда и ростер как сущности с рейтингом** — у команды свой рейтинг, топ-1 крупнее в UI; то же для ростера. ТЗ перед работой |
| 14 | ждём | **Личный профиль дополнить** — чем именно, не сказано. Вводных нет к моменту 14 — уходит в later, не угадываем |
| 15 | делаем | **Fearless draft одним экраном** по референсу драфт-комнаты WSS/lastbattle: слоты банов и пиков колонками у своей стороны с номерами хода 1–16, переключатель карты серии сверху, карточки капитанов снизу, статусная строка вместо модалок, смена роли «это не я» справа. Берём раскладку и логику, не палитру. ТЗ перед работой |
| 16 | later | **Локализация, полный EN** — переводить имеет смысл устоявшийся UI: до конца 08–15 каждый этап стоил бы дважды, экран и его перевод |
| 17 | later | **Тёмная тема** — та же причина: палитра переопределяется по устоявшимся экранам, а не по меняющимся. Строка ниже, в «Продукте» |
| 18 | later | **1x1** — формат в разработке у Стаса; пока он сам не опишет, что хочет видеть, заводить нечего. В `tools.ts` стоит заглушкой `soon` |
| 19 | later | **LLM-помощник по стате** — общение по статистике внутри проекта; не названы ни вопрос, на который он отвечает, ни источник данных |
| 21 | ждём | **Сообщение игроку, которого нет на сайте** — «Написать» стоит у любого игрока, написанное хранится до его регистрации. Идёт после 15 (номера 16–20 заняты later-пунктами). Развилка не решена: спящий аккаунт против очереди писем на `playerId`, плюс правила приватности доставки. `docs/tasks/21-soobshchenie-igroku-bez-akkaunta.md` |
| 20 | later | **ТГ бот: доработка** — бот работает (флоу, напоминания, заявки, вход кодом); что доделывать, не определено. Пункт принесён одной строкой |

## Открытые вопросы (нужен выбор Стаса)

| Статус | Вопрос |
|---|---|
| MVP | **Какой VPS** — что нужен живой Node решено, площадка нет. `docs/tasks/01-vps-i-baza.md` |
| MVP | **Кто главный по базе** после публикации: `web/prisma/dev.db` в репозитории разойдётся с боевым. `docs/tasks/01-vps-i-baza.md` |
| MVP | **Пять страниц-заглушек** — `/about`, `/partners`, `/media-kit`, `/contact`, `/privacy`: написать или убрать. Было шесть: `/schedule` закрыт этапом 10 — страница удалена, адрес отдаёт 308 на `/series?m=next`. `docs/tasks/03-seo-bazovyi.md` |
| later | **На что тратятся осколки** — списка трат нет; модель к нему готова (истина — события), списанное не должно понижать ступень |
| 21 | **Как хранится сообщение игроку без аккаунта** — спящий аккаунт (цена: склейка при регистрации, а это вход в лигу) против очереди писем на `playerId` (цена: второе хранилище переписки). Плюс правила доставки и приватности. `docs/tasks/21-soobshchenie-igroku-bez-akkaunta.md` |
| icebox | **Формального «сезона» в модели нет** — поэтому лимит смены ника скользящим окном 120 дней |
| icebox | **Возврат писем** — почтовый флоу удалён сознательно; появится `RESEND_API_KEY` — вернуть из git |

## Долги в коде

| Статус | Долг |
|---|---|
| later | **Карточка турнира живёт в двух вёрстках** — раздел переехал на общий `app/_components/tournament-card.tsx` (ТЗ 09), блок турниров на главной (`(home)/tournaments-block.tsx:43`) остался на своей. Пока обе живы, правка карточки требует двух правок |
| later | **`Grid` из Кита меряет окно, а не контейнер** — единственный порог 900px, поэтому вложенные сетки (дивизионы внутри карточки турнира) собраны классами карточки. Нужна промежуточная ступень и/или контейнерные запросы (`pouf/layout.tsx`, отступление зафиксировано в ТЗ 09 §7.3) |
| later | **Заголовок группы серии берёт написание у случайного турнира** — `lib/tournaments.ts:379` подставляет `series` первого элемента (список идёт `startAt desc`), и «LOST» / «lost» на одном экране дают заголовок строчными при чипе заглавными |
| later | **`/tournaments/loading.tsx` накрывает вложенные страницы турнира** — у `[slug]` своего нет, поэтому при переходе на `/tournaments/<slug>` сперва рисуется скелет *списка* (110–150 мс на тёплом dev). Лечится своим `loading.tsx` в сегменте |
| — | ~~`/admin/roster/crm-import` не читает `?tournament=`~~ и ~~берёт чужого родителя в крошке~~ — закрыто слиянием экранов, ТЗ 11 принято 14.09: адрес отдаёт 308 с сохранением `?tournament=`, крошка одна — «Админ / Импорт» |
| later | **Экран управления турнирами зовётся двумя именами** — плитка хаба и `UI-GUIDELINES` §5 дают «Управление турнирами», H1 самого `/admin/tournaments` — «Турниры». §9 стандарта требует разных имён у разных «Турниров» (приёмка 08) |
| later | **Ряд пилюль на карточке турнира ломается на 390** — четыре пилюли по 128–166px при колонке 358 занимают две строки (`(admin)/admin/tournaments/[slug]/page.tsx:85–89`, измерил `design` на приёмке DESIGN 11). Территория 09, в объём 11 не входит |
| later | **Крошка «Заявленные команды» врёт о статусе** — `(public)/tournaments/[slug]/entrants/page.tsx:45` зашивает eyebrow как «<турнир> · приём заявок» мимо `TOURNAMENT_STATUS_LABELS`, и у идущего турнира страница пишет «приём заявок» (приёмка 09) |
| later | **«Анкеты» — имя двух разных инструментов** — механизм импорта на `/admin/roster/import?mode=players` и плитка `/admin/quizzes` в том же хабе (`app/_components/tools.ts`). `UI-GUIDELINES` §9 требует разных имён у разного (приёмка 11) |
| later | **Два `<main>` на странице** — layout группы маршрутов и сама страница рисуют по одному; воспроизводится и на публичной `/tournaments`. Ориентир скринридера должен быть один (приёмка 11) |
| later | **Артборд «Карточка турнира» в Ките не заведён** — в живой витрине `/admin/kit` есть только «Счётчик мест»; правка артефакта, не кода |
| later | **Заполненность дивизиона проверяется только кнопкой** — серверной проверки в экшене заявки нет, отправка мимо UI дойдёт. ТЗ 09 требовало неотправляющую кнопку, это сверх него |
| later | **Двойной «Админ» на главной** — пилюля в баре и кнопка в мини-профиле (`(home)/mini-profile.tsx:95`); там же две лишние двери в `/me` у гостя. Тело страницы, пришло из ТЗ 06, §2 стандарта такой дубль запрещает |
| later | **Пилюля «Админ» висит на `perms.length > 0`** (`_components/app-shell.tsx:83`), а не на наличии инструмента: право `roster.delete` не использует ни один инструмент `tools.ts`, и админ только с ним попадёт на хаб с плашкой «прав пока нет» |
| 21 | **Написать можно только игроку с активным аккаунтом** — `players/[key]/page.tsx:116`; к присутствию кнопка не привязана вовсе, беседа связывает два аккаунта (`chat.ts:47`). Не долг, а недостающее звено модели: `docs/tasks/21-soobshchenie-igroku-bez-akkaunta.md` |
| MVP | **Восстановление пароля отсутствует** — забыл и всё. `docs/tasks/02-sbros-parolya.md` |
| MVP | **`TG_CHAT_ID` не заведён в `web/.env`** — действие Стаса, кодом не закрыть; без него суточный дайджест встреч слать некуда |
| MVP | **`STEAM_API_KEY` не заведён** — без него мёртв список матчей лиги в архиве и обогащение составов ссылками Steam; ключ бесплатный |
| MVP | **Позиция теряется, когда сервер отклоняет анкету** — `FormSelect` неуправляемый, React возвращает `defaultValue` момента монтирования |
| MVP | **`pattern` у `DateField` браузер игнорирует** — голый `-` в классе символов невалиден под флагом `v`, проверка не работает вовсе |
| later | **`/admin/kit` открыт любому вошедшему** — витрина компонентов не проверяет права; данных лиги не отдаёт, поэтому не блокер, но в смоке Б4 («админ без права») выглядит как дыра |
| later | **Dev-сервер на `127.0.0.1` отдаёт мёртвую разметку** — либо `allowedDevOrigins`, либо строка в `web/README.md` |
| later | **Три разных `Field` на проект** — китовый плюс две локальные копии в `me/`; решать вместе с ревизией форм |
| later | **Состояния загрузки закрыты не везде** — без скелета встреча, отчёт матча, служебные экраны |
| later | **Статистика игрока не разрезана по турнирам** — `/players/<slug>` суммирует все турниры разом |
| later | **Реестр баллов без UI** — `PointsEntry` работает только на TP, места/касты/стримы правятся руками в БД |
| later | **OG-картинки при шеринге** — генератор постгейма клиентский, под серверную OG нужна своя реализация |
| later | **`isAdmin()` берёт роль из куки** без похода в базу — вкладка «Админ» видна у куки удалённого админа (инструменты закрыты правами) |
| later | **Опознание в боте по телеграм-хендлу** — хендл меняют, освободившийся может занять посторонний |
| later | **Архивные команды не спрятаны из пула драфта/студии** — `listTeams` без аргумента отдаёт архивные |
| later | **Файлы лого/фото не чистятся при `?mode=purge`** — осиротевшие файлы копятся |
| later | **Нет входа по паролю у пришедших из бота** — `loginWithPassword` ищет по почте, а её у такого аккаунта нет; нужна «привязка почты», а не «задать пароль» |
| later | **Артборд «Осколки» в Ките не заведён** — атом `pouf/shards.tsx` написан, в канвасе его нет |
| later | **Артборд скорборда в Ките не заведён** — единственный источник вида `report-scoreboard.tsx` |
| — | ~~Семь атомов Кита не перенесены в код~~ и ~~крошки живут по месту~~ — закрыто: `docs/tasks/05-kit-v-kod.md` сделано, все семь файлов лежат в `pouf/`, крошки переехали (потребители импортируют `@/components/pouf/breadcrumbs`). У `Pagination` до сих пор нет ни одного потребителя в продукте — первым станет ТЗ 10 |
| later | **Состояния 38 реализованных листов Кита не сверены с кодом** — hover / press / disabled / focus у кнопок, полей, меню, чипов, карточек; ревизия, а не перенос, поэтому вне ТЗ 05 |

### Принятые отступления (не долг, не трогать без причины)

- **Зеркала имени дивизиона** — `Team.group`, `Series.division`, `GroupEntry.division` остались
  строками: по ним фильтруют полтора десятка выборок. Источник правды — `TournamentEntry`,
  единственный писатель зеркал — `src/lib/tournaments.ts`.
- **Аватар и ник из OpenDota не сохраняем** — ранг пишем в `Player.rank`, ник человек выбирает сам,
  `Player.photo` у нас всегда локальный файл.
- **Заявка не пишет в ростер** — `TeamApplication` лежит очередью, `Team`/`Player`/`RosterSpot`
  заводит апрув. Исключение — импорт таблицы оператором.

## Продукт

| Статус | Пункт |
|---|---|
| later | **Свободные агенты отдельным блоком** в разрезе «Игроки» на `/roster` |
| later | **Главная по макету `design/home/`** — витрина в коде и в макете разошлись. `docs/tasks/06-glavnaya-po-maketu.md` |
| later | **Название турнира на карточке встречи на главной** — сейчас дивизион и стадия; блок показывает один текущий турнир, подпись дублировалась бы в каждой строке. Вернуть, когда активных турниров станет два |
| later | **Макет `design/home/` расходится со словарём статусов турнира** — в артборде «Регистрация открыта» / «Завершён», в продукте `TOURNAMENT_STATUS_LABELS` даёт «Приём заявок» / «Сыгран». Править макет, не код |
| later | **Раздела «Новости» нет** — макет главной рисует пункт меню и герой-новость; нужны модель, редактор и страница, это отдельный продукт |
| later | **Колокольчик уведомлений** — вопрос закрыт 12.09: панель это витрина служебного канала `/chat/system`, группа «Личные» не реализуется. Внесено в `docs/tasks/06-glavnaya-po-maketu.md`, подэтап A; решение 04.09 в силе |
| later | **Страница топа осколков** — вкладка «Shards» на главной есть, ссылки «весь топ» у неё нет: страницы нет. Заводить вместе с тратами |
| later | **Блок осколков снят с главной** — код `(home)/shards.tsx` и атом `pouf/shards.tsx` остаются; вернуть показ, если решится, на что осколки тратятся |
| later | **Студия по Киту** — атом «двухпанельный редактор» + шесть экранов `/studio` |
| later (17) | **Тёмная тема** второй палитрой — токены заведены так, чтобы стоила одного блока переопределений. Отложена до конца 08–15: палитра переопределяется по устоявшимся экранам |
| later | **Вебхук вместо long polling** у бота — упирается в тот же невыбранный VPS |
| later | **Чат: вторая итерация** — уведомление в телеграм о непрочитанном, модерация переписки, общий канал |
| later | **Обновлённый брендбук** — сначала система генерации, потом сам брендбук |
| later | **Полное оформление сезона** под команды |
| icebox | **Генеративный пайплайн** — логотипы, оформление команд, превью и баннеры, граффити, обработка фото, презентации партнёрам |
| icebox | **Контент-цикл игрового дня** — анонсы, лайв со студийным деском, highlights и shorts, инфографика по OpenDota, баллы кастеру и стримеру |
| icebox | **HUD LHM.gg** — лайв-статы готовым инструментом; проверить цену, лицензию, кастомизацию |
| icebox | **Прочие блоки** — маркетинг и контент, инфраструктура, монетизация и партнёрка |

## Кандидаты в скиллы

Действующие — `.claude/skills/`. Кандидаты (все icebox): логотип команды · картинка → баннер по
брендбуку · оформление команды · элементы граффити · обработка фото · презентация для партнёра ·
анонс игры и анонс результата · инфографика по игре через API · бот: MVP и баллы командам ·
бот: баллы кастеру и стримеру.
