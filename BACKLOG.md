# BACKLOG — что осталось, что отложили, что не решили

Одно место для всего, что впереди. Собран 26.08.2026 из `ROADMAP.md`, `HOTSKILLS.md`,
`CLAUDE.md` §9, `TOURNAMENTS-PLAN.md` §5 и `docs/archive/ACCOUNTS-PLAN.md` §8 — раньше это лежало
в пяти файлах и нигде не было видно целиком.

Что уже сделано — в `WORKLOG.md` (журнал по этапам). Почему сделано именно так — в `DECISIONS.md`.
Где что лежит — в `MAP.md`.

---

## 1. Открытые вопросы (решения нет, нужен выбор Стаса)

| Вопрос | Суть | Откуда |
|---|---|---|
| **Какой VPS** | Что нужен живой Node — решено (`DECISIONS.md`, «стек»), площадка — нет. Варианты и требования — `DEPLOY.md` | CLAUDE.md §9 |
| **Кто главный по базе** | Как только публичный сервер заработает, `web/prisma/dev.db` в репозитории разойдётся с боевым. Развилка описана в `DEPLOY.md`, но не выбрана | CLAUDE.md §9 |
| **Формального «сезона» в модели нет** | Из-за этого лимит смены ника — скользящее окно 120 дней, а не «раз в сезон». Турниры в БД появились; можно привязать кулдаун к ним | ACCOUNTS-PLAN §8 |
| **Возврат писем** | Почтовый флоу удалён сознательно. Появится `RESEND_API_KEY` — вернуть файлы из git и включить подтверждение почты шагом воронки (`draft` → анкета → подтверждение → `pending`) | ACCOUNTS-PLAN §8 |
| **Быстрый вход в текущий сезон из сайдбара** | Макет Кита «Сайдбар» рисует в секции «Лига» пять пунктов: Турниры · Ростер · Таблицы · Встречи · TP-зачёт. В коде их два. Причина: таблица, встречи и TP — этапы **внутри** турнира (уровень L3, строка турнира), и их копия в глобальной колонке — «один уровень, две модели» (UI-GUIDELINES §2, принцип 3). Плюс `/standings` и `/tp` — редиректы в текущий турнир, то есть подсветиться такой пункт не может никогда. Если Стасу важнее короткий путь к таблице сезона с любой страницы, чем чистота уровней, — пункты вернуть с оговоркой в §2 | Э4b |
| **Подвал раздаёт семь заглушек** | `/schedule`, `/playoffs`, `/tournaments/archive`, `/apply`, `/about`, `/partners`, `/media-kit` — все семь `ComingSoon`, и подвал единственный, кто на них ссылается. К релизу решить по каждой: собрать, увести на существующий раздел или убрать ссылку | разбор навигации 08.09 |
| **Три ряда фильтров на `/roster`** | режим (сквозной/по турнирам) · пул/архив · селект турнира — формально разные разрезы, читаются как «два ряда за один выбор» из §9. Нужен взгляд Стаса: какой из них правда нужен на витрине | разбор навигации 08.09 |
| **Скорборда нет в Ките** | Э6 собрал экран `/match/<id>` по месту из существующих атомов (подушка, `TeamMark`, `Chip`, `Meter`, вдавленные лунки) — своего рисунка не добавлял, но и артборда не заводил: рисовать новый артборд не входило в этап. Пока артборда нет, единственный источник вида скорборда — `app/(admin)/match/_components/report-scoreboard.tsx`. Заведём артборд — заменять один файл | Э6 |

## 2. Долги в коде (решение есть, руки не дошли)

- [ ] **Состояния загрузки.** `loading.tsx` нет ни у одного раздела, хотя чек-лист §8 их требует;
      404 и граница ошибок закрыты 08.09 (`_components/stub-page.tsx`), загрузка — нет. Начинать
      стоит с тяжёлых витрин: таблица дивизиона, ростер, статистика.

- [ ] **В сайдбаре нет переключателя тёмной темы**, хотя макет Кита его рисует. Не долг вёрстки:
      самой тёмной темы в продукте нет (решение 01.09 — после релиза, одним блоком переопределений
      токенов). Переключатель заводится вместе с ней, не раньше — иначе это тумблер в никуда.

- [ ] **На `/me` два «Выйти»** — своё в карточке профиля и общее в низу сайдбара. Кабинет
      переезжает на компоненты Кита на Э8; лишнее уйдёт там, вместе с `<h1>` классами на этой же
      странице.

- [ ] **`TG_CHAT_ID` не заведён в `web/.env`** — а без него суточный дайджест встреч без времени
      слать некуда: `tickSchedule` его молча пропускает (`telegramConfigured()`). Напоминания и
      переносы игрокам это не трогает — им бот пишет в личку, `TG_CHAT_ID` нужен только служебному
      чату. **Осталось действие Стаса:** вписать в `web/.env` свой user id или id канала (образец —
      `web/.env.example`) и перезапустить приложение. Кодом закрыть нельзя, ключи в репозиторий не
      едут. На Э4 молчание сделано видимым: `/admin/bot?tab=settings` показывает предупреждение,
      пока переменные не заданы.

- [ ] **Статистика игрока не разрезана по турнирам.** `/players/<slug>` суммирует все турниры
      разом — последняя незакрытая кросс-турнирная витрина (остальные переехали под
      `/tournaments/<slug>/…` и фильтруются по `divisionId`).
- [x] ~~**Импорт таблицы сезона узнаёт человека по нику, а не по account_id.**~~ Закрыто 04.09.2026:
      `scripts/import-roster.ts` перед записью считает account_id по полю **и по ссылкам** на
      Dotabuff/Stratz/Steam (`playerAccountId` — то же место правды, что у апрува заявки), ищет
      человека сперва по нему и только потом по слагу ника. Смена ника больше не заводит второй
      профиль; разобранный из ссылки номер записывается в поле (обогащение). Тёзка с ЧУЖИМ номером
      теперь не перезаписывает существующий профиль, а заводится отдельным со слагом-суффиксом.
      Прогон `--dry` на текущем ростере показал первый настоящий дубль: `miracle` / `mirakl` —
      один account_id, ждут разбора на `/admin/duplicates`.

- [x] ~~**Канвас Кита в репозитории отстал от опубликованного на 7 артбордов.**~~ Сведено
      04.09.2026: артборды Э1 и Э9 (`ProfileAtoms`, `Standings`, `SeriesCard`, `Groups`, `TeamHero`,
      `SiteNav`, `Footer`) вернулись в `design/kit/build.mjs`, все четырнадцать снова генерируются
      из него. Хвосты CSS, нужные одному листу, лежат в `EXTRA` рядом с общим `STYLE`. Правило
      против повторения — в `design/README.md`: правил в канвасе — верни правку в `build.mjs`
      через `seed-canvas.mjs --extract`.

- [ ] **Реестр баллов без UI.** `PointsEntry` работает только на TP (`src/lib/tp.ts`).
      Для мест, кастов и стримов начисления интерфейса нет — только руками в БД.
- [x] ~~**`/login/tg` считает вошедшим по куке, не проверяя аккаунт.**~~ Закрыто на Э4 (02.09):
      страница спрашивает `currentAccount()`, то есть тот же источник, что и `/me`. Осталось
      родственное и более мелкое: `isAdmin()` берёт роль из куки без похода в базу (так сделано ради
      dev-автовхода), поэтому у куки удалённого админа вкладка «Админ» в шапке ещё видна — сами
      инструменты закрыты правами и отдают отказ.
- [ ] **Восстановление пароля.** После удаления почтового флоу его нет.
      Возможное продолжение без писем — «сбросить пароль» в панели команды у владельца.
- [ ] **OG-картинки при шеринге.** У отчёта матча есть постоянная ссылка, а превью для соцсетей нет:
      генератор постгейма клиентский (`modern-screenshot`), под серверную OG нужна отдельная реализация.
- [ ] **Опознание в боте — по телеграм-хендлу.** Работает, но хендл человек меняет: сменил — бот его
      не узнаёт, а освободившийся хендл может занять посторонний. Пока меню только читает публичное,
      это терпимо; для чего-то большего нужна настоящая привязка чата к аккаунту.

- [ ] **Архивные команды не спрятаны из пула драфта/студии.** `Team.archivedAt` прячет команду из таба
      «Ростер», но `listTeams`/`listTeamRosters` без аргумента (жеребьёвка, UNDERBEER, студия) архивные
      всё ещё отдают. Если мешает — добавить `archivedAt: null` в безаргументную ветку этих выборок
      (в вариант с `divisionIds` — не трогать: там историческая витрина турнира).
- [ ] **Файлы лого/фото не чистятся при полном удалении команды.** `?mode=purge` сносит запись из БД,
      но `public/uploads/teams/<slug>-*` остаются. Безвредно (осиротевшие файлы), но копятся.

### Принятые отступления (не долг, не трогать без причины)

- **Зеркала имени дивизиона.** `Team.group`, `Series.division`, `GroupEntry.division` остались
  строками: по ним фильтруют полтора десятка выборок. Источник правды — `TournamentEntry`,
  единственный писатель зеркал — `src/lib/tournaments.ts`.
- **Аватар и ник из OpenDota не сохраняем.** Ранг пишем в `Player.rank`, ник в лиге человек выбирает
  сам, аватар Steam живёт по чужой ссылке (`Player.photo` у нас всегда локальный файл).
  И то, и другое показываем оператору справкой.

## 3. Продуктовый роадмап

### Из списка правок 04.09.2026 — не сделано, каждое своим этапом
- [ ] **Свободные агенты отдельным блоком** в разрезе «Игроки» на `/roster` — игроки, не стоящие
      ни в одном составе. Отложено 04.09.2026: делаем после разреза «По турнирам / Сквозной»

- [x] ~~**Веб-страница парсинга составов — куда парсим: турнир или общий ростер.**~~ Идея 04.09.2026:
      выяснилось, что мастер парсинга уже был — `/admin/tournaments/<slug>/import`
      (`src/app/(admin)/admin/tournaments/[slug]/import/`), три шага Источник → Разбор → Запись,
      с модерацией на шаге «Разбор» (замечания block/warn/info по каждой команде и игроку —
      `applicationProblems`, `src/lib/team-application.ts`) и чекбоксами «включить/исключить» команду
      перед записью. Единственного не хватало: шаг «Запись» требовал дивизион, общего пула не было.
      Закрыто 04.09.2026: `writeTeamToRoster` принимает `divisionId: number | null` (не трогает
      `TournamentEntry`, если пул), в селекте назначения первым пунктом «Общий ростер — без турнира».
      Дивизион состава и раньше не был параметром импорта, а следствием `TournamentEntry`
      (`entry?.divisionId ?? null`) — значит «выбор назначения» это создать/не создать `TournamentEntry`
      перед заливкой, отдельного поля никуда добавлять не пришлось. Проверено на живой БД: команда
      без `TournamentEntry`, `RosterSpot.divisionId: null`.
      Кнопка в общем меню тоже была не заведена (сам мастер лежал только внутри карточки турнира) —
      закрыто, пункт «Импорт составов» в реестре `tools.ts` группы «Модерация», виден в сайдбаре
      и на хабе `/admin` всем с правом `tournaments.edit`.

      Донастроено 04.09.2026, тем же днём: мастер жил по адресу карточки турнира
      (`/admin/tournaments/<slug>/import`) и на шаге «Запись» предлагал дивизионы только ЭТОГО
      турнира — назначение фактически выбиралось до конца просмотра, самим тем, откуда зашли.
      Перенесён на свой адрес `/admin/roster/import`, разбор турнира не касается вовсе — на шаге
      «Запись» список дивизионов **всех** турниров сразу (`<optgroup>` на турнир) плюс «Общий
      ростер», решение принимается уже после разбора и модерации, а не заранее заходом со страницы
      конкретного турнира. Пилюля «Импорт составов» на карточке турнира ведёт туда же с подсказкой
      `?tournament=<slug>` — она только предвыбирает первый дивизион в списке, ничего не ограничивает.
      Мастер создания нового турнира (`admin/tournaments/new/[step]`) использует тот же компонент
      формы с одним турниром в списке — код не задвоен.

- [x] ~~**Веб-страница парсинга игроков (CRM)**~~ — вторая из двух фич 04.09.2026, отдельная от
      парсинга составов (см. выше). Закрыто 04.09.2026: `/admin/roster/crm-import` — веб-версия
      `scripts/import-crm.ts`, тот же мастер в три шага (Источник → Разбор → Запись), что и у
      составов, но короче — свод с базой не ходит в сеть, поэтому разбор и свод объединены в одно
      действие. Только обогащает уже существующих игроков (телеграм, дата рождения, город/страна,
      account_id) — новых не заводит и составы не трогает, поэтому право на запись `roster.edit`,
      а не `tournaments.edit`. Разбор скрипта на переиспользуемые части: `src/lib/crm-import.ts`
      (чистый парсинг — xlsx/csv/tsv/json, без БД) + `src/lib/crm-match.ts` (свод с ростером и
      запись, с prisma) — то же разделение, что у `roster-import.ts` / `team-application.ts` в
      мастере составов. Модерация — построчный дифф «поле: было → станет» с чекбоксом на игрока,
      своё в базе не перезаписывает (кроме галочки «force»). Пункт «Импорт CRM» — в `tools.ts`
      группы «Модерация», сайдбар и хаб `/admin`. Проверено на живой БД: E-sim получил телеграм
      и город из тестовой строки, оба поля откатил после проверки.


Функционал по блокам, к MVP → полноценному продукту. Обязательные блоки — Дизайн и Разработка,
прочие добавим после анализа масштабов.

### Дизайн · MVP
- [ ] Обновлённый брендбук (сначала — продуманная система генерации, потом сам брендбук)
- [ ] Редизайн сайта и админки по Киту — активный план в `RELEASE-PLAN.md` (этапы Э1–Э12)

### Дизайн · после MVP
- [ ] **Студия по Киту** — этап Э10 отложен 02.09.2026: атом «двухпанельный редактор»
      (холст + инспектор) и шесть экранов `/studio`. Что именно делать — «Отложенный Э10»
      в `RELEASE-PLAN.md`. Студия работает и уже на токенах Э2, расходится с Китом только раскладкой
- [ ] Полное оформление сезона под команды
- [ ] Тёмная тема второй палитрой (на релиз идёт только светлый Кит, `DECISIONS.md` 01.09.2026)

**Графика трансляции (HUD, оверлеи, плашки) из плана убрана — ей занимается другой человек**
(решение 01.09.2026).

### Разработка · генеративный пайплайн
- [ ] Генерация логотипов команд
- [ ] Генерация оформления команд
- [ ] Генерация превью и баннеров
- [ ] Генерация отдельных элементов граффити
- [ ] Обработка фото
- [ ] Генерация презентаций для партнёров

### Разработка · контент-цикл игрового дня
Поток: **анонс → лайв со студийным деском → highlights + shorts → пост-матч + MVP**.
- [ ] Анонсы игр
- [ ] Лайв: студийный деск (драфт / аналитика / пост-матч разбор)
- [ ] Highlights + shorts (клипы эпик-моментов — под охваты)
- [ ] Анонсы результатов + корректировка таблиц
- [ ] Инфографика по игре через OpenDota API
- [x] Админка: назначение MVP + начисление баллов — есть на TP (`/admin/tp`)
- [ ] Админка: баллы кастеру / стримеру

### Разработка · после MVP
- [ ] Внедрить HUD **LHM.gg** — лайв-статы (урон, лагеря, золото, Рошан) готовым инструментом,
      не разработкой; проверить цену / лицензию / кастомизацию

- [ ] **Чат: что осталось после первой итерации.** Уведомление в телеграм о непрочитанном, когда
      человека нет на сайте (`presence.isOnline` для этого уже есть); модерация переписки —
      жалоба на сообщение и разбор оператором; общий канал лиги или чат команды (модель комнаты
      к этому готова — добавляется составом участников)

### Прочие блоки
После анализа масштабов: маркетинг/контент, инфраструктура, монетизация и партнёрка.

## 4. Кандидаты в скиллы

Существующие скиллы — `.claude/skills/` (`ship`, `stage`, `roster-sync`, `serve`).

Генерация:
- [ ] Логотип команды (черновик промта был в `HOTSKILLS.md`, см. историю git: эмблема-крест,
      плоский вектор, 2–3 цвета, без текста — чтобы 20 логотипов читались как одна лига)
- [ ] Картинка → баннер / превью по брендбуку
- [ ] Оформление команды (комплект под составы)
- [ ] Элементы граффити
- [ ] Обработка фото
- [ ] Презентация для партнёра

Операции:
- [ ] Анонс игры / анонс результата
- [ ] Инфографика по игре через API
- [ ] Бот: MVP + баллы командам
- [ ] Бот: баллы кастеру / стримеру
