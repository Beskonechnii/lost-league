// «Голая» группа маршрутов: без навигации и обёрток. Нужна для оверлеев, которые встраивают
// снаружи (OBS-сцена шоу-драфта). Публичная — пароль на неё не вешаем (см. needsAdmin): у OBS
// админской куки нет, а показывается только результат драфта, не операторские кнопки.

export default function BareLayout({ children }: { children: React.ReactNode }) {
  return (
    <>
      {/* Фон страницы гасим до прозрачного. После переезда на Light Clay (Э3) `body` красится
          кремовым `--canvas`, и браузерный источник OBS выводил бы поверх картинки игры не
          составы, а сплошной кремовый прямоугольник во весь экран сцены. Правило живёт здесь,
          а не в `globals.css`: прозрачный фон нужен ровно этой группе и ровно по этой причине. */}
      <style>{`body{background:transparent}`}</style>
      {children}
    </>
  );
}
