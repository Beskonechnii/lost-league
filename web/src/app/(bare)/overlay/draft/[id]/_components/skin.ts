import type { CSSProperties } from "react";

/**
 * Кожа эфирного оверлея (решение Стаса 23.09.2026 — отменяет 15.09.2026 «тёмной
 * эфирной кожи не делаем нигде»). Событие с партнёром кладёт под карточки свою
 * картинку (`partner.background`, ТЗ 33/35) — у Eclipse это тёмный космический
 * пейзаж с затмением, и светлая бумага Кита на нём читается наклейкой, а не
 * частью сцены. Кожа не трогает компоненты: она переопределяет ИМЕННО ТЕ
 * переменные, которые Card/StatusPill/cushion-* уже используют, по приёму
 * `postgame/skin.ts` → EXPORT_SKIN — локальная рамка, а не второй набор классов.
 *
 * ВАЖНОЕ ОТЛИЧИЕ от постгейма: там один слой токенов (`--pg-*`, все с fallback
 * в globals.css). Здесь их два, и они переопределяются по-разному:
 *
 *  1. Токены СТРАНИЦЫ (`text-ink`, `text-muted`, `bg-surface-2`) заведены в
 *     globals.css как `var(--lost-*, fallback)` — их и переопределяем через
 *     `--lost-*`, ровно как задумано под панель /admin/theme.
 *  2. Токены самого КИТА (`bg-surface`, `cushion-card`, `cushion-field`,
 *     StatusPill tone="warn") сидят в pouf.css БЕЗ такого fallback — это
 *     литералы в его собственном `@theme`/`:root` (`--color-surface`,
 *     `--sh-raise`, `--sh-carve`, `--grad-warn`, `--color-warn-ink`). Через
 *     `--lost-*` их не достать, поэтому они переопределены напрямую. Хватает
 *     переопределить эти «листья»: `--pouf-card`/`--pouf-field` в pouf.css
 *     объявлены как `var(--sh-raise)`/`var(--sh-carve)` и разворачиваются в
 *     значение того элемента, где их читают (box-shadow), поэтому дочерний
 *     override `--sh-raise` меняет и cushion-card, без отдельной правки алиаса.
 *
 * Акцент оставлен мятным (`--accent-fill`/`--on-accent` не тронуты): это
 * плавающие чипы (КАП, рамка последнего пика) с собственным контрастом внутри
 * себя, фон под ними не участвует — перекрашивать в оранжевый Eclipse незачем.
 * Оранжевый (#FF4D00 из знака Eclipse) отдан пилюле «Ходит»: на тёмной коже
 * янтарный `--grad-warn` Кита садится тускло, а тёплый акцент ровно по цвету
 * события — заметнее и уместнее.
 */
export const OVERLAY_DARK_SKIN = {
  // -- 1. Токены страницы (globals.css, идут через --lost-*) --------------
  "--lost-ink": "#f5f2ec", // тот же светлый ink, что уже держит инверсные плашки Кита
  "--lost-ink-muted": "#b9b2a4",
  "--lost-surface-2": "rgba(255, 255, 255, 0.08)", // лунка игрока — вдавлена в стеклянную карточку

  // -- 2. Примитивы Кита (pouf.css, литералы без fallback) -----------------
  // Бумага карточки: тёмное стекло, а не сплошной чёрный — сквозь него угадывается фон события.
  "--color-surface": "rgba(26, 22, 19, 0.82)",
  // Приподнятая тень (cushion-card): контактная тень бежевой глины на тёмной сцене не видна,
  // весь рецепт пересобран в чёрный отброс + тонкий стеклянный блик по контуру.
  "--sh-raise":
    "0 0 0 1px rgba(255, 255, 255, 0.10)," +
    "inset 2px 3px 4px -1px rgba(255, 255, 255, 0.16)," +
    "inset -3px -4px 7px -2px rgba(0, 0, 0, 0.55)," +
    "inset 0 -3px 0 rgba(0, 0, 0, 0.35)," +
    "0 4px 7px rgba(0, 0, 0, 0.35)," +
    "0 22px 40px -8px rgba(0, 0, 0, 0.5)," +
    "0 58px 88px -24px rgba(0, 0, 0, 0.4)",
  // Вдавленная тень (cushion-field): лунка игрока в карточке — свет уходит внутрь, без белого рефлекса.
  "--sh-carve":
    "inset 2px 3px 5px rgba(0, 0, 0, 0.5)," +
    "inset -1px -1px 1px rgba(255, 255, 255, 0.08)," +
    "inset 0 -1px 0 rgba(255, 255, 255, 0.06)",
  // Пилюля «Ходит» (StatusPill tone="warn"): янтарь Кита → оранжевый Eclipse.
  "--grad-warn": "linear-gradient(135deg, #ffb066, #ff4d00 65%)",
  "--color-warn-ink": "#3a1200",
} as CSSProperties;
