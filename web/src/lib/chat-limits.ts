// Ограничения чата, нужные обоим концам: клиент режет ввод, сервер — принятое. Отдельным файлом,
// потому что chat.ts помечен server-only, а поле ввода должно знать тот же предел, что и роут.

/** Максимум символов в сообщении. Чат — реплики, а не письма; длинное всё равно не читают. */
export const MAX_TEXT = 2000;
