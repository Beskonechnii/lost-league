---
name: ship
description: Закоммитить и запушить изменения репозитория League of Spirit (LOST) на origin. Триггеры — "коммит", "закоммить", "запушь", "залей на гит", "ship". Порядок commit → push.
---

# ship — commit → push (репозиторий LOST)

Коммит целиком делает сабагент `ship` (Haiku): три git-команды и сабж по диффу не стоят
модели посильнее, а сабагент считается отдельно. Скилл — только точка входа.

## Что делать

Вызови сабагента и дождись его:

```
Agent(subagent_type: "ship", run_in_background: false,
      description: "Коммит и пуш",
      prompt: "Закоммить и запушь текущие изменения репозитория LOST.")
```

Сабж он формулирует сам из диффа — **не пиши сообщение за него**, иначе экономии не будет.
Если в сессии было решение, которого по диффу не видно (почему выбран такой подход, что
пробовали и отбросили), добавь это в `prompt` одной фразой — остальное он выведет сам.

Его финальный отчёт пользователю не показывается: перескажи сам — короткий SHA, ветка,
число файлов и сабж коммита, одной строкой.

## Что помнить самому

- **`main` не пушить без явной просьбы.** Сабагент на `main` остановится и вернёт вопрос —
  реши его с пользователем, а не обходи.
- Ветку сабагент не переключает: пушится та, на которой стоит репозиторий.
- Упал хук — не обходить `--no-verify`, разбираться с причиной.
- `web/prisma/dev.db` коммитится **на компе** (данные лиги); в облачной сессии база не коммитится —
  бинарник не мержится, см. скилл `handoff`. `node_modules` / `.env` / `web/src/generated` — никогда.
- Remote: `origin` → github.com/Beskonechnii/lost-league (приватный).
